{
  "last_updated": "2026-06-23T08:26:43Z",
  "provider_version": "7.37.0",
  "arguments": {
    "deletion_policy": {
      "description": "Whether Terraform will be prevented from destroying the instance. Defaults to \"DELETE\".\nWhen a 'terraform destroy' or 'terraform apply' would delete the instance,\nthe command will fail if this field is set to \"PREVENT\" in Terraform state.\nWhen set to \"ABANDON\", the command will remove the resource from Terraform\nmanagement without updating or deleting the resource in the API.\nWhen set to \"DELETE\", deleting the resource is allowed.\n",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "This argument has a security impact because it controls whether the Access Policy can be deleted, abandoned, or protected from deletion. Preventing or allowing deletion directly affects the security and management of the resource."
    },
    "parent": {
      "description": "The parent of this AccessPolicy in the Cloud Resource Hierarchy.\nFormat: 'organizations/{{organization_id}}'",
      "required": true,
      "type": "string",
      "security_impact": true,
      "rationale": "This argument has a security impact because it specifies the parent organization that owns the Access Policy. The selected organization determines where the policy is applied and which resources it governs."
    },
    "scopes": {
      "description": "Folder or project on which this policy is applicable.\nFormat: 'folders/{{folder_id}}' or 'projects/{{project_number}}'",
      "required": false,
      "type": "list(string)",
      "security_impact": true,
      "rationale": "This argument has a security impact because it defines the folders or projects where the Access Policy is applied. The configured scope determines which resources are governed by the policy."
    },
    "title": {
      "description": "Human readable title. Does not affect behavior.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "This argument does not have a security impact because it is used only as a human-readable name for the Access Policy and does not affect its behavior or security settings."
    }
  }
}
