# Legacy assessment migration report

- Source: `docs/gcp`  →  Target: `docs_v2/gcp`

## Summary

- Source args carried (≥1 placement): **3132**
  - placements by exact dotted-key match: 3123
  - placements by unique leaf-name fallback: 465
  - docs_v2 files updated: 477
- Dropped:
  - assessments in resources absent from docs_v2: 254
  - args unmatched in a matched resource (renamed/removed/now-block): 1365
  - args with leaf-name clash (ambiguous): 374
  - non-boolean `security_impact` strings dropped: 50
  - legacy files that failed to parse: 1

## Resources absent from docs_v2 (whole-file dropped)

- `api_gateway_api` — 4 assessment(s)
- `api_gateway_api_config` — 26 assessment(s)
- `api_gateway_api_config_iam` — 6 assessment(s)
- `api_gateway_api_iam` — 5 assessment(s)
- `api_gateway_gateway` — 6 assessment(s)
- `api_gateway_gateway_iam` — 6 assessment(s)
- `artifact_registry_vpcsc_config` — 3 assessment(s)
- `beyondcorp_application` — 14 assessment(s)
- `beyondcorp_application_iam` — 10 assessment(s)
- `bigquery_analytics_hub_data_exchange_subscription` — 1 assessment(s)
- `cloud_quotas_quota_adjuster_settings` — 2 assessment(s)
- `dataflow_flex_template_job` — 27 assessment(s)
- `discovery_engine_engine_assistant` — 9 assessment(s)
- `firebase_android_app` — 7 assessment(s)
- `firebase_apple_app` — 7 assessment(s)
- `firebase_project` — 1 assessment(s)
- `firebase_storage_bucket` — 2 assessment(s)
- `firebase_web_app` — 4 assessment(s)
- `folder` — 4 assessment(s)
- `folder_organization_policy` — 12 assessment(s)
- `network_security_authorization_policy` — 4 assessment(s)
- `organization_iam_custom_role` — 6 assessment(s)
- `project` — 11 assessment(s)
- `project_default_service_accounts` — 3 assessment(s)
- `project_service` — 5 assessment(s)
- `service_account` — 6 assessment(s)
- `service_account_key` — 6 assessment(s)
- `tpu_v2_queued_resource` — 20 assessment(s)
- `tpu_v2_vm` — 37 assessment(s)

## Dropped arguments in matched resources

### `apigee_addons_config`
- `addons_config` — arg_unmatched (security_impact=True, rationale='Certain addons_config arguments provide security impact')
- `addons_config.advanced_api_ops_config` — arg_unmatched (security_impact=True, rationale='advanced_api_ops_config detect, analyze, and alert on API traffic anomalies in r')
- `addons_config.api_security_config` — arg_unmatched (security_impact=True, rationale='It should be enabled to detect API security risks, flag abnormal traffic')
- `addons_config.connectors_platform_config` — arg_unmatched (security_impact=False, rationale='It enable or disable the Integration Connectors platform within an Apigee organi')
- `addons_config.integration_config` — arg_unmatched (security_impact=False, rationale='It allows Apigee to integrate with other Google Cloud services and third-party a')
- `addons_config.monetization_config` — arg_unmatched (security_impact=False, rationale='It is used to enable or disable the Apigee Monetization add-on for a specific Ap')

### `apigee_api_product`
- `attributes` — arg_unmatched (security_impact=False, rationale='It is used to specify the metadat and does not have any security impact')

### `apigee_dns_zone`
- `peering_config` — arg_unmatched (security_impact=False, rationale='This configuration allows the Apigee runtime instances to resolve internal hostn')

### `apigee_environment`
- `client_ip_resolution_config` — arg_unmatched (security_impact=True, rationale='Incorrect client IP resolution can allow malicious clients to spoof their IP add')
- `client_ip_resolution_config.header_index_algorithm` — arg_unmatched (security_impact=True, rationale='The header index algorithm determines which IP in the X-Forwarded-For header is ')
- `node_config` — arg_unmatched (security_impact=False, rationale='It sets the node scaling configuration and does not have any security impact.')

### `apigee_instance`
- `access_logging_config` — arg_unmatched (security_impact=True, rationale='Enabling access logging is critical for security monitoring, audit trails, and i')

### `apigee_organization`
- `properties` — arg_unmatched (security_impact=False, rationale='It sets custom key-value properties and does not have any direct security impact')
- `properties.property` — arg_unmatched (security_impact=False, rationale='Properties are custom key-value metadata fields used for organizational configur')

### `apihub_api_hub_instance`
- `config` — arg_unmatched (security_impact=False, rationale='Config block is required but security impact depends on its nested arguments.')

### `apihub_curation`
- `application_integration_endpoint_details` — arg_unmatched (security_impact=False, rationale='Arguments inside may impact security')
- `display_name` — nonbool_string (security_impact='Purely informational field with no effect on security. Should not contain sensitive information.')
- `endpoint` — arg_unmatched (security_impact=False, rationale='Endpoint configuration determines where API metadata is routed')
- `endpoint.application_integration_endpoint_details` — arg_unmatched (security_impact=False, rationale='Controls workflow routing for API metadata.')

### `apihub_plugin`
- `actions_config` — arg_unmatched (security_impact=False, rationale='Action metadata only, no secrets or sensitive data.')
- `additional_config_template` — arg_unmatched (security_impact=False, rationale='Template object. Only sensitive if value_type is SECRET.')
- `additional_config_template.description` — ambiguous (security_impact=False, rationale='Metadata only. Not sensitive.')
- `additional_config_template.enum_options` — arg_unmatched (security_impact=False, rationale='Option metadata only. Not sensitive.')
- `additional_config_template.id` — ambiguous (security_impact=False, rationale='Identifier only. Not sensitive.')
- `additional_config_template.multi_select_options` — arg_unmatched (security_impact=False, rationale='Option metadata only. Not sensitive.')
- `auth_config_template` — arg_unmatched (security_impact=False, rationale='Container for auth configuration. Sensitive data handled inside child attributes')
- `config_template` — arg_unmatched (security_impact=False, rationale='Template structure only. Sensitivity comes from nested attributes.')
- `config_template.additional_config_template` — arg_unmatched (security_impact=False, rationale='Additional metadata. Security depends on nested values.')
- `config_template.auth_config_template` — arg_unmatched (security_impact=False, rationale='Container for auth config. Not directly sensitive.')
- `documentation` — arg_unmatched (security_impact=False, rationale='Documentation references only. Not security-relevant.')
- `enum_options` — arg_unmatched (security_impact=False, rationale='Option metadata only. Not security relevant.')
- `enum_options.description` — ambiguous (security_impact=False, rationale='Metadata only. Not sensitive.')
- `enum_options.display_name` — ambiguous (security_impact=False, rationale='Display only. Not sensitive.')
- `enum_options.id` — ambiguous (security_impact=False, rationale='Identifier only. Not sensitive.')
- `hosting_service` — arg_unmatched (security_impact=False, rationale='Defines where service is hosted. Not a secret.')
- `multi_select_options` — arg_unmatched (security_impact=False, rationale='Option metadata only. Not security relevant.')
- `multi_select_options.description` — ambiguous (security_impact=False, rationale='Metadata only. Not sensitive.')
- `multi_select_options.display_name` — ambiguous (security_impact=False, rationale='Display only. Not sensitive.')
- `multi_select_options.id` — ambiguous (security_impact=False, rationale='Identifier only. Not sensitive.')

### `apihub_plugin_instance`
- `actions` — arg_unmatched (security_impact=False, rationale='Not security related.')
- `actions.curation_config` — arg_unmatched (security_impact=False, rationale='Configuration. Not security relevant.')
- `actions.hub_instance_action` — arg_unmatched (security_impact=False, rationale='Arguments inside could be security relevant however.')
- `api_key` — arg_unmatched (security_impact=False, rationale='Parent.')
- `api_key.secret_version` — ambiguous (security_impact=True, rationale='Potentially security relevant, points to the location where secrets are stored.')
- `api_key_config` — arg_unmatched (security_impact=False, rationale='Parent.')
- `api_key_config.api_key` — arg_unmatched (security_impact=True, rationale='Potentially security relevant. Holds api key for auth.')
- `auth_config` — arg_unmatched (security_impact=False, rationale='Parent, nested functions may be security-related.')
- `auth_config.api_key_config` — arg_unmatched (security_impact=False, rationale='Parameter, not security related.')
- `auth_config.google_service_account_config` — arg_unmatched (security_impact=False, rationale='Parameter, not security related.')
- `auth_config.oauth2_client_credentials_config` — arg_unmatched (security_impact=False, rationale='Parameter, not security related.')
- `auth_config.user_password_config` — arg_unmatched (security_impact=False, rationale='Parameter, not security related.')
- `client_secret` — arg_unmatched (security_impact=False, rationale='Parent')
- `client_secret.secret_version` — ambiguous (security_impact=True, rationale='Could be security relevant. Points to secrets.')
- `curation_config` — arg_unmatched (security_impact=False, rationale='Curation configuration.')
- `curation_config.custom_curation` — arg_unmatched (security_impact=False, rationale='Configuration settings. Not security relevant.')
- `custom_curation` — arg_unmatched (security_impact=False)
- `google_service_account_config` — arg_unmatched (security_impact=False, rationale='Arguments inside could be security relevant.')
- `oauth2_client_credentials_config` — arg_unmatched (security_impact=False)
- `oauth2_client_credentials_config.client_secret` — arg_unmatched (security_impact=True, rationale='Potentially security relevant, points to the location where secrets are stored.')
- `password` — arg_unmatched (security_impact=False, rationale='Arguments inside are security relevant.')
- `password.secret_version` — ambiguous (security_impact=True, rationale='Potentiall security relevant, points to the location where secrets are stored.')
- `user_password_config` — arg_unmatched (security_impact=False, rationale='Arguments inside are security relevant.')
- `user_password_config.password` — arg_unmatched (security_impact=True, rationale='Potentially security relevant. Credentials-relevant and related to secrets manag')

### `apikeys_key`
- `restrictions` — arg_unmatched (security_impact=False)
- `restrictions.android_key_restrictions` — arg_unmatched (security_impact=False)
- `restrictions.android_key_restrictions.allowed_applications` — arg_unmatched (security_impact=False)
- `restrictions.api_targets` — arg_unmatched (security_impact=False)
- `restrictions.browser_key_restrictions` — arg_unmatched (security_impact=False)
- `restrictions.ios_key_restrictions` — arg_unmatched (security_impact=False)
- `restrictions.server_key_restrictions` — arg_unmatched (security_impact=False)

### `app_engine_application`
- `feature_settings` — arg_unmatched (security_impact=True, rationale='to enforce the use of modern split health checks to ensure precise monitoring of')
- `iap` — arg_unmatched (security_impact=True, rationale='Enforces Identity-Aware Proxy to establish a Zero Trust security layer, ensuring')

### `app_engine_application_url_dispatch_rules`
- `dispatch_rules` — arg_unmatched (security_impact=True, rationale='Establishes centralized routing logic to ensure requests are directed to the cor')

### `app_engine_domain_mapping`
- `ssl_settings` — arg_unmatched (security_impact=True, rationale='Mandates the use of managed SSL settings to guarantee that all custom domain tra')
- `ssl_settings.pending_managed_certificate_id` — arg_unmatched (security_impact=False, rationale='Is a read-only output attribute managed by Google Cloud, represents a transient ')

### `app_engine_flexible_app_version`
- `api_config` — arg_unmatched (security_impact=False, rationale='Is a legacy configuration for Google Cloud Endpoints Frameworks, this block woul')
- `automatic_scaling` — arg_unmatched (security_impact=True, rationale='Enforced to ensure that every service can respond to traffic fluctuations while ')
- `automatic_scaling.cpu_utilization` — arg_unmatched (security_impact=True, rationale='Enforced to establish a standardized trigger for horizontal scaling, which ensur')
- `automatic_scaling.disk_utilization` — arg_unmatched (security_impact=False, rationale='Typically bottlenecked by CPU or memory rather than storage.')
- `automatic_scaling.network_utilization` — arg_unmatched (security_impact=False, rationale='Scaling based on data throughput can be highly inconsistent while CPU-based scal')
- `automatic_scaling.request_utilization` — arg_unmatched (security_impact=False, rationale='Can be unreliable if request processing times vary whereas relying on CPU utilis')
- `deployment` — arg_unmatched (security_impact=True, rationale='Is enforced to ensure that every application version is derived from a verified ')
- `deployment.cloud_build_options` — arg_unmatched (security_impact=False, rationale='Build-time configuration is an operational concern distinct from the application')
- `deployment.container` — arg_unmatched (security_impact=False, rationale='To prevent configuration overlap, as in workflows where source code is the prima')
- `deployment.files` — arg_unmatched (security_impact=False, rationale='individual file-level declarations are redundant when deploying via comprehensiv')
- `deployment.zip` — arg_unmatched (security_impact=True, rationale='Ensure that source-based deployments utilize versioned objects stored in Google ')
- `endpoints_api_service` — arg_unmatched (security_impact=False, rationale='API management via Cloud Endpoints is a separate service layer with its own life')
- `entrypoint` — arg_unmatched (security_impact=True, rationale='Enforced to ensure that the application starts using a predefined command-string')
- `flexible_runtime_settings` — arg_unmatched (security_impact=False, rationale='The settings are highly specific to individual language runtimes and typically g')
- `handlers` — arg_unmatched (security_impact=False, rationale='Flexible Environment is container-based where routing and static file handling a')
- `liveness_check` — arg_unmatched (security_impact=True, rationale='Enforces the configuration of liveness probes to detect deadlocked/zombie proces')
- `manual_scaling` — arg_unmatched (security_impact=False, rationale='Lacks the ability to adjust to real-time traffic changes, which can lead to unex')
- `network` — arg_unmatched (security_impact=False, rationale="App Engine Flexible automatically defaults to the 'default' VPC with managed set")
- `readiness_check` — arg_unmatched (security_impact=True, rationale='Mandates the configuration of health probes to ensure that the load balancer onl')
- `resources` — arg_unmatched (security_impact=False, rationale="The hardware requirements are tied to the specific application's performance pro")
- `vpc_access_connector` — arg_unmatched (security_impact=False, rationale='To allow for architectural flexibility, while Serverless VPC Access is required ')
- `zip` — arg_unmatched (security_impact=True, rationale='Ensure that source-based deployments utilize versioned objects stored in Google ')
- `zip.source_url` — ambiguous (security_impact=True, rationale="To ensure that the application's source code is retrieved from a managed version")

### `app_engine_service_network_settings`
- `network_settings` — arg_unmatched (security_impact=True, rationale='Enforced to ensure the definition of the fundamental trust boundary of the appli')

### `app_engine_service_split_traffic`
- `split` — arg_unmatched (security_impact=True, rationale='To ensure that traffic distribution is managed as code and providing an automate')

### `app_engine_standard_app_version`
- `automatic_scaling` — arg_unmatched (security_impact=False, rationale='To prevent the application from scaling up to aggressively during minor traffic ')
- `basic_scaling` — arg_unmatched (security_impact=False, rationale='Avoid the latency delays in relation with starting instances from zero after per')
- `deployment` — arg_unmatched (security_impact=True, rationale='To define the specific source code and files that constitute the application ver')
- `deployment.files` — arg_unmatched (security_impact=False, rationale='To simplify the configuration and ensure that the application package is deploye')
- `deployment.zip` — arg_unmatched (security_impact=True, rationale="Provides a direct way to package the application's source code.")
- `entrypoint` — arg_unmatched (security_impact=True, rationale='To provide the specific command required to start the application, ensuring that')
- `handlers` — arg_unmatched (security_impact=False, rationale='For a more flexible and unified approach to request handling without platform-sp')
- `libraries` — arg_unmatched (security_impact=False, rationale='Uses a second-generation runtime that manages dependencies through standard pack')
- `manual_scaling` — arg_unmatched (security_impact=False, rationale='To ensure the system can instead respond dynamically to traffic changes without ')
- `vpc_access_connector` — arg_unmatched (security_impact=False, rationale='interacts with public APIs/managed services that do not require a private connec')
- `zip` — arg_unmatched (security_impact=True, rationale="Provides a direct way to package the application's source code.")
- `zip.source_url` — ambiguous (security_impact=True, rationale='Ensuring that the deployment process uses a verified artifact that is consistent')

### `apphub_application`
- `attributes` — arg_unmatched (security_impact=False, rationale='attributes has no impact on the security of the resource or data contained.')
- `attributes.criticality` — arg_unmatched (security_impact=False, rationale='The policy whitelists criticality.type to a known set so risk/criticality report')
- `attributes.environment` — arg_unmatched (security_impact=False, rationale='The policy whitelists environment.type to a known set to keep environment labels')
- `criticality.type` — ambiguous (security_impact=None, rationale='Must be one of: MISSION_CRITICAL, HIGH, MEDIUM, LOW (whitelist).')
- `environment.type` — ambiguous (security_impact=None, rationale='Must be one of: DEVELOPMENT, TEST, STAGING, PRODUCTION (whitelist).')

### `apphub_service`
- `attributes` — arg_unmatched (security_impact=False, rationale='attributes has no impact on the security of the resource or data contained.')
- `attributes.criticality` — arg_unmatched (security_impact=False, rationale='The policy whitelists criticality.type to a known set so risk/criticality report')
- `attributes.environment` — arg_unmatched (security_impact=False, rationale='The policy whitelists environment.type to a known set to keep environment labels')
- `criticality.type` — ambiguous (security_impact=None, rationale='Must be one of: MISSION_CRITICAL, HIGH, MEDIUM, LOW (whitelist).')
- `environment.type` — ambiguous (security_impact=None, rationale='Must be one of: DEVELOPMENT, TEST, STAGING, PRODUCTION (whitelist).')

### `apphub_workload`
- `attributes` — arg_unmatched (security_impact=None, rationale='attributes has no impact on the security of the resource or data contained.')
- `attributes.criticality` — arg_unmatched (security_impact=False, rationale='The policy whitelists criticality.type to a known set so risk/criticality report')
- `attributes.environment` — arg_unmatched (security_impact=False, rationale='The policy whitelists environment.type to a known set to keep environment labels')
- `criticality.type` — ambiguous (security_impact=None, rationale='Must be one of: MISSION_CRITICAL, HIGH, MEDIUM, LOW (whitelist).')
- `environment.type` — ambiguous (security_impact=None, rationale='Must be one of: DEVELOPMENT, TEST, STAGING, PRODUCTION (whitelist).')

### `artifact_registry_repository`
- `apt_repository` — arg_unmatched (security_impact=False, rationale='This is a structural grouping entry used to document nested attributes and does ')
- `apt_repository.public_repository` — ambiguous (security_impact=False, rationale='This selects a provider-supported public upstream preset; current security check')
- `cleanup_policies` — arg_unmatched (security_impact=False, rationale='This is a parent configuration block only; any security impact is captured by it')
- `cleanup_policies.condition` — arg_unmatched (security_impact=True, rationale='Guardrail policy specifically checks condition.tag_state for DELETE policies and')
- `cleanup_policies.most_recent_versions` — arg_unmatched (security_impact=True, rationale='Guardrail policy requires KEEP policies to retain at least 3 recent versions via')
- `common_repository` — arg_unmatched (security_impact=False, rationale='This is a structural grouping entry used to document nested attributes and does ')
- `common_repository.uri` — ambiguous (security_impact=True, rationale='The approved_remote_upstreams policy allows only approved URI patterns and appro')
- `condition` — arg_unmatched (security_impact=False, rationale='This is a structural grouping entry used to document nested attributes and does ')
- `condition.package_name_prefixes` — ambiguous (security_impact=False, rationale='This is only a matching selector for cleanup or retention scope and has no direc')
- `custom_repository` — arg_unmatched (security_impact=False, rationale='This is a structural grouping entry used to document nested attributes and does ')
- `custom_repository.uri` — ambiguous (security_impact=False, rationale='This URI belongs to a deprecated custom_repository block, while the current appr')
- `docker_config` — arg_unmatched (security_impact=False, rationale='This is a parent configuration block only; any security impact is captured by it')
- `docker_repository` — arg_unmatched (security_impact=False, rationale='This is a structural grouping entry used to document nested attributes and does ')
- `docker_repository.custom_repository` — arg_unmatched (security_impact=False, rationale='This is a deprecated remote upstream configuration block; current upstream secur')
- `docker_repository.public_repository` — ambiguous (security_impact=False, rationale='This selects a provider-supported public upstream preset; current security check')
- `maven_config` — arg_unmatched (security_impact=False, rationale='This is a parent configuration block only; any security impact is captured by it')
- `maven_repository` — arg_unmatched (security_impact=False, rationale='This is a structural grouping entry used to document nested attributes and does ')
- `maven_repository.custom_repository` — arg_unmatched (security_impact=False, rationale='This is a deprecated remote upstream configuration block; current upstream secur')
- `maven_repository.public_repository` — ambiguous (security_impact=False, rationale='This selects a provider-supported public upstream preset; current security check')
- `most_recent_versions` — arg_unmatched (security_impact=False, rationale='This is a structural grouping entry used to document nested attributes and does ')
- `most_recent_versions.package_name_prefixes` — ambiguous (security_impact=False, rationale='This is only a matching selector for cleanup or retention scope and has no direc')
- `npm_repository` — arg_unmatched (security_impact=False, rationale='This is a structural grouping entry used to document nested attributes and does ')
- `npm_repository.custom_repository` — arg_unmatched (security_impact=False, rationale='This is a deprecated remote upstream configuration block; current upstream secur')
- `npm_repository.public_repository` — ambiguous (security_impact=False, rationale='This selects a provider-supported public upstream preset; current security check')
- `public_repository` — ambiguous (security_impact=False, rationale='This is a structural grouping entry used to document nested attributes and does ')
- `public_repository.repository_base` — ambiguous (security_impact=False, rationale='This selects the base of a provider-supported public package repository and does')
- `public_repository.repository_path` — ambiguous (security_impact=False, rationale='This narrows the provider-supported public repository path and does not directly')
- `python_repository` — arg_unmatched (security_impact=False, rationale='This is a structural grouping entry used to document nested attributes and does ')
- `python_repository.custom_repository` — arg_unmatched (security_impact=False, rationale='This is a deprecated remote upstream configuration block; current upstream secur')
- `python_repository.public_repository` — ambiguous (security_impact=False, rationale='This selects a provider-supported public upstream preset; current security check')
- `remote_repository_config` — arg_unmatched (security_impact=False, rationale='This is a parent configuration block only; any security impact is captured by it')
- `remote_repository_config.apt_repository` — arg_unmatched (security_impact=False, rationale='This attribute is informational or structural and does not directly affect acces')
- `remote_repository_config.common_repository` — arg_unmatched (security_impact=True, rationale='The approved_remote_upstreams policy allows only approved upstream Artifact Regi')
- `remote_repository_config.docker_repository` — arg_unmatched (security_impact=False, rationale='This attribute is informational or structural and does not directly affect acces')
- `remote_repository_config.maven_repository` — arg_unmatched (security_impact=False, rationale='This attribute is informational or structural and does not directly affect acces')
- `remote_repository_config.npm_repository` — arg_unmatched (security_impact=False, rationale='This attribute is informational or structural and does not directly affect acces')
- `remote_repository_config.python_repository` — arg_unmatched (security_impact=False, rationale='This attribute is informational or structural and does not directly affect acces')
- `remote_repository_config.upstream_credentials` — arg_unmatched (security_impact=False, rationale='This is only a credentials container; any security-sensitive credential details ')
- `remote_repository_config.yum_repository` — arg_unmatched (security_impact=False, rationale='This attribute is informational or structural and does not directly affect acces')
- `upstream_credentials` — arg_unmatched (security_impact=False, rationale='This is a structural grouping entry used to document nested attributes and does ')
- `upstream_credentials.username_password_credentials` — arg_unmatched (security_impact=False, rationale='This is only a credentials container and does not expose or validate credentials')
- `upstream_policies` — arg_unmatched (security_impact=False, rationale='This is a structural grouping entry used to document nested attributes and does ')
- `upstream_policies.id` — ambiguous (security_impact=False, rationale='This is only a user-defined identifier for the block and does not directly chang')
- `username_password_credentials` — arg_unmatched (security_impact=False, rationale='This is a structural grouping entry used to document nested attributes and does ')
- `virtual_repository_config` — arg_unmatched (security_impact=False, rationale='This is a parent configuration block only; any security impact is captured by it')
- `virtual_repository_config.upstream_policies` — arg_unmatched (security_impact=True, rationale='The virtual_upstream_priority_range policy requires upstream_policies.priority t')
- `vulnerability_scanning_config` — arg_unmatched (security_impact=False, rationale='This is a parent configuration block only; any security impact is captured by it')
- `vulnerability_scanning_config.enablement_state` — arg_unmatched (security_impact=False, rationale='This is an output-only status field and cannot be configured to change vulnerabi')
- `vulnerability_scanning_config.enablement_state_reason` — arg_unmatched (security_impact=False, rationale='This is an output-only explanation field and cannot be configured to change vuln')
- `yum_repository` — arg_unmatched (security_impact=False, rationale='This is a structural grouping entry used to document nested attributes and does ')
- `yum_repository.public_repository` — ambiguous (security_impact=False, rationale='This selects a provider-supported public upstream preset; current security check')

### `assured_workloads_workload`
- `kms_settings` — arg_unmatched (security_impact=True, rationale='KMS settings control how encryption keys are managed. Proper key rotation reduce')
- `partner_permissions` — arg_unmatched (security_impact=True, rationale='Partner permissions control what the partner can access. Enabling appropriate mo')
- `resource_settings` — arg_unmatched (security_impact=False, rationale='Resource settings are used to customise project and keyring identifiers and do n')
- `workload_options` — arg_unmatched (security_impact=False, rationale='Workload options configure KAJ enrollment type and do not directly affect the ge')

### `backup_dr_backup_plan`
- `backup_rules` — arg_unmatched (security_impact=False, rationale='This is a container block. Explicit controls apply to specific fields inside it,')
- `backup_rules.standard_schedule` — arg_unmatched (security_impact=False, rationale='Security rules apply to fields inside this block, not the block itself.')
- `backup_rules.standard_schedule.backup_window` — arg_unmatched (security_impact=False, rationale='This is conditional schedule logic and is better handled through custom validati')
- `backup_rules.standard_schedule.week_day_of_month` — arg_unmatched (security_impact=False, rationale='This is conditional schedule logic and is better handled through custom validati')
- `disk_backup_plan_properties` — arg_unmatched (security_impact=False, rationale='It is a configuration setting and does not directly impact security.')

### `backup_dr_backup_vault`
- `encryption_config` — arg_unmatched (security_impact=False, rationale='This is a container block. Controls apply to specific fields inside it.')

### `backup_dr_management_server`
- `networks` — arg_unmatched (security_impact=False, rationale='This is a container block. Security controls apply to the specific fields inside')

### `backup_dr_restore_workload`
- `compute_instance_restore_properties` — arg_unmatched (security_impact=False, rationale='This is a container block. Security controls apply to specific fields inside it.')
- `compute_instance_restore_properties.advanced_machine_features` — arg_unmatched (security_impact=False, rationale='Container block for machine behavior settings, not itself a generic explicit sec')
- `compute_instance_restore_properties.allocation_affinity` — arg_unmatched (security_impact=False, rationale='Reservation consumption policy is operational rather than a generic explicit sec')
- `compute_instance_restore_properties.confidential_instance_config` — arg_unmatched (security_impact=False, rationale='Container block. Security controls apply to fields inside it.')
- `compute_instance_restore_properties.disks` — arg_unmatched (security_impact=False, rationale='This is a complex container block and is better handled with custom validation t')
- `compute_instance_restore_properties.display_device` — arg_unmatched (security_impact=False, rationale='Display enablement is not usually treated as a generic explicit security control')
- `compute_instance_restore_properties.guest_accelerators` — arg_unmatched (security_impact=False, rationale='Performance/hardware configuration rather than a generic explicit security contr')
- `compute_instance_restore_properties.instance_encryption_key` — arg_unmatched (security_impact=False, rationale='This is a container block. Security controls apply to the specific encryption fi')
- `compute_instance_restore_properties.labels` — arg_unmatched (security_impact=False, rationale='Labels are metadata rather than a generic explicit security control.')
- `compute_instance_restore_properties.metadata` — arg_unmatched (security_impact=False, rationale='Instance metadata can affect security posture, but the security relevance usuall')
- `compute_instance_restore_properties.network_interfaces` — arg_unmatched (security_impact=False, rationale='This is a container block. Security controls apply to specific fields inside it.')
- `compute_instance_restore_properties.network_interfaces.access_configs` — arg_unmatched (security_impact=False, rationale='This is a container block. Security controls apply to the specific fields inside')
- `compute_instance_restore_properties.network_interfaces.alias_ip_ranges` — arg_unmatched (security_impact=False, rationale='Network address allocation logic is operational and better handled through custo')
- `compute_instance_restore_properties.network_interfaces.ipv6_access_configs` — arg_unmatched (security_impact=True, rationale='IPv6 public access configuration is a clear generic security control because it ')
- `compute_instance_restore_properties.network_performance_config` — arg_unmatched (security_impact=False, rationale='Performance configuration rather than a generic explicit security control.')
- `compute_instance_restore_properties.params` — arg_unmatched (security_impact=False, rationale='Container block for ancillary settings, not itself a generic explicit security c')
- `compute_instance_restore_properties.scheduling` — arg_unmatched (security_impact=False, rationale='Container block for scheduling behavior, not itself a generic explicit security ')
- `compute_instance_restore_properties.service_accounts` — arg_unmatched (security_impact=False, rationale='This is a container block. Security controls apply to the specific service accou')
- `compute_instance_restore_properties.shielded_instance_config` — arg_unmatched (security_impact=False, rationale='Container block. Security controls apply to fields inside it.')
- `compute_instance_restore_properties.tags` — arg_unmatched (security_impact=False, rationale='Tags are metadata rather than a generic explicit security control.')
- `compute_instance_target_environment` — arg_unmatched (security_impact=False, rationale='This is a container block. Security controls apply to specific fields inside it.')
- `disk_restore_properties` — arg_unmatched (security_impact=False, rationale='This is a container block. Security controls apply to specific fields inside it.')
- `disk_restore_properties.disk_encryption_key` — arg_unmatched (security_impact=False, rationale='Disk encryption key is a configuration only, does not inherently provide securit')
- `disk_restore_properties.guest_os_feature` — arg_unmatched (security_impact=False, rationale='Feature compatibility/configuration rather than a generic explicit security cont')
- `disk_restore_properties.labels` — arg_unmatched (security_impact=False, rationale='Labels are metadata rather than a generic explicit security control.')
- `disk_restore_properties.resource_manager_tags` — arg_unmatched (security_impact=False, rationale='Tags are governance metadata but not a generic explicit security control by them')
- `disk_target_environment` — arg_unmatched (security_impact=False, rationale='This is a container block. Security controls apply to specific fields inside it.')
- `region_disk_target_environment` — arg_unmatched (security_impact=False, rationale='This is a container block. Security controls apply to specific fields inside it.')

### `beyondcorp_app_connection`
- `application_endpoint` — arg_unmatched (security_impact=True, rationale='This is the address of the remote application endpoint.')
- `gateway` — arg_unmatched (security_impact=False, rationale='The gateway configuration for this AppConnection is not directly impact security')
- `gateway.ingress_port` — arg_unmatched (security_impact=False, rationale='The ingress port is used to identify the gateway resource. And it should not be ')
- `gateway.uri` — arg_unmatched (security_impact=False, rationale='The URI is used to identify the gateway resource.')

### `beyondcorp_app_connector`
- `principal_info` — arg_unmatched (security_impact=False, rationale='This information is required to establish the identity of the AppConnector for a')
- `principal_info.service_account` — arg_unmatched (security_impact=False, rationale='The service account is used to authenticate the AppConnector with GCP services.')

### `beyondcorp_security_gateway`
- `hubs` — arg_unmatched (security_impact=True, rationale='This mapping allows for tailored configurations and optimizations based on regio')
- `hubs.internet_gateway` — arg_unmatched (security_impact=False, rationale='This configuration allows for internet access and management of IP addresses.')
- `internet_gateway` — arg_unmatched (security_impact=False, rationale='This configuration allows for internet access and management of IP addresses.')
- `internet_gateway.assigned_ips` — arg_unmatched (security_impact=False, rationale='This argument provides visibility into the IP addresses assigned to the Cloud NA')

### `beyondcorp_security_gateway_application`
- `endpoint_matchers` — arg_unmatched (security_impact=True, rationale='Ensures only authorized application endpoints are accessible through the Securit')
- `logging_enabled` — arg_unmatched (security_impact=True, rationale='Logging ensures application access can be monitored.')
- `upstreams` — arg_unmatched (security_impact=True, rationale='Traffic must be routed securely and through approved upstream networks.')
- `upstreams.egress_policy` — arg_unmatched (security_impact=True, rationale='Traffic must stay within approved regions for compliance.')
- `upstreams.network` — arg_unmatched (security_impact=True, rationale='Only approved networks should be used for security reasons.')

### `beyondcorp_security_gateway_application_iam`
- `condition` — arg_unmatched (security_impact=False, rationale='The condition field allows for more granular control over when the IAM policy is')

### `beyondcorp_security_gateway_iam`
- `condition` — arg_unmatched (security_impact=False, rationale='The condition field allows for more granular control over when the IAM policy is')

### `biglake_database`
- `hive_options` — arg_unmatched (security_impact=False)

### `biglake_table`
- `hive_options` — arg_unmatched (security_impact=False)
- `hive_options.storage_descriptor` — arg_unmatched (security_impact=False)

### `bigquery_analytics_hub_listing`
- `restricted_export_config` — arg_unmatched (security_impact=True, rationale='Restricted export helps prevent uncontrolled export of shared data and reduces r')

### `bigquery_analytics_hub_listing_subscription`
- `destination_dataset` — arg_unmatched (security_impact=True, rationale='This dataset is where shared data lands for the subscriber; labels are used for ')

### `bigquery_bi_reservation`
- `preferred_tables` — arg_unmatched (security_impact=False, rationale='This setting only determines which tables use BI capacity and does not change wh')

### `bigquery_connection`
- `credential.password` — nonbool_string (security_impact='high')
- `credential.username` — ambiguous (security_impact='high', rationale='An empty or missing username may allow unauthenticated or unauthorised access to')
- `credential.username` — nonbool_string (security_impact='high')
- `kms_key_name` — nonbool_string (security_impact='high')

### `bigquery_datapolicyv2_data_policy`
- `data_masking_policy` — arg_unmatched (security_impact=True, rationale='Masking rules affect how data is shown.')

### `bigquery_reservation`
- `autoscale` — arg_unmatched (security_impact=False, rationale='Autoscale is mainly for performance scaling and does not directly change access ')
- `autoscale.current_slots` — arg_unmatched (security_impact=False, rationale='This is an output value used for reporting and does not change security behaviou')

### `bigtable_app_profile`
- `data_boost_isolation_read_only` — arg_unmatched (security_impact=False, rationale='Marks the profile as read-only for Data Boost analytics paths; it does not repla')
- `single_cluster_routing` — arg_unmatched (security_impact=False, rationale='A structural block that chooses single-cluster routing; the block tag itself is ')
- `standard_isolation` — arg_unmatched (security_impact=False, rationale='Groups priority-based isolation for noisy-neighbor control; it tunes scheduling ')

### `bigtable_authorized_view`
- `column_family` — arg_unmatched (security_impact=False, rationale='A schema label for which family the view applies to; it is not an IAM role strin')
- `subset_view` — arg_unmatched (security_impact=False, rationale='Terraform grouping only: the block header carries no secret values. Whether data')
- `subset_view.family_subsets` — arg_unmatched (security_impact=False, rationale='Structural block for per-family qualifier rules; the block itself does not grant')

### `bigtable_gc_policy`
- `max_age` — arg_unmatched (security_impact=False, rationale='The block is only a container in Terraform; it holds no secret values. Retention')
- `max_version` — arg_unmatched (security_impact=False, rationale='Structural wrapper only; it does not expose credentials. Version-count limits th')

### `bigtable_instance`
- `cluster` — arg_unmatched (security_impact=False, rationale='A structural block in HCL, not a secret. Security-sensitive values (for example ')
- `cluster.autoscaling_config` — arg_unmatched (security_impact=False, rationale='Container for autoscaling parameters; the block label is not sensitive—only the ')
- `effective_labels` — arg_unmatched (security_impact=False, rationale='Aggregated label snapshot from GCP; informational only and does not replace IAM ')
- `terraform_labels` — arg_unmatched (security_impact=False, rationale='Derived labels for state/plan output; read-only convenience field, not an access')

### `bigtable_instance_iam`
- `condition` — arg_unmatched (security_impact=False, rationale='The parent block for optional conditional IAM; the block being present does not ')

### `bigtable_instance_iam_binding`
- `condition` — arg_unmatched (security_impact=False, rationale='Parent block for conditional IAM; impact depends on the nested `expression` and ')

### `bigtable_schema_bundle`
- `proto_schema` — arg_unmatched (security_impact=False, rationale='Structural wrapper for protobuf material; the block itself does not assign IAM—a')

### `bigtable_table`
- `automated_backup_policy` — arg_unmatched (security_impact=True, rationale='Scheduled backups support recovery and incident response for table data.')
- `column_family` — arg_unmatched (security_impact=False, rationale='Declares schema families for the table; schema blocks alone do not attach IAM me')

### `bigtable_table_iam_binding`
- `condition` — arg_unmatched (security_impact=False, rationale='Parent block for conditional IAM; impact depends on the nested `expression` and ')
- `condition.description` — arg_unmatched (security_impact=False, rationale='Optional prose documentation; ignored by IAM enforcement engines.')
- `condition.expression` — arg_unmatched (security_impact=False, rationale='Constrains when a binding is active; the expression text is policy logic, not a ')
- `condition.title` — arg_unmatched (security_impact=False, rationale='UI metadata for operators; not used in authorization decisions.')

### `billing_budget`
- `all_updates_rule` — arg_unmatched (security_impact=False, rationale='Defines how budget update notifications are delivered. This supports billing vis')
- `amount` — arg_unmatched (security_impact=True, rationale='Defines the monetary budget limit for a billing period. Enforcing a specified am')
- `amount.specified_amount` — arg_unmatched (security_impact=True, rationale='Specifies a fixed budget value. Using a specified amount rather than last_period')
- `budget_filter` — arg_unmatched (security_impact=False, rationale='Defines the scope of resources included in budget calculations. This supports co')
- `budget_filter.custom_period` — arg_unmatched (security_impact=False, rationale='Allows flexible time range configuration for budget tracking. This impacts budge')
- `custom_period` — arg_unmatched (security_impact=False, rationale='Defines a fixed time range for budget monitoring. This supports billing and repo')
- `custom_period.end_date` — arg_unmatched (security_impact=False, rationale='Defines when the custom budget tracking period ends. This affects the duration o')
- `custom_period.end_date.day` — ambiguous (security_impact=False, rationale='Specifies the day component of the end date. This supports valid date definition')
- `custom_period.end_date.month` — ambiguous (security_impact=False, rationale='Specifies the month component of the end date. This is part of date configuratio')
- `custom_period.end_date.year` — ambiguous (security_impact=False, rationale='Defines when the custom budget tracking period ends. This affects reporting dura')
- `custom_period.start_date` — arg_unmatched (security_impact=False, rationale='Defines when the custom budget tracking period begins. This is a reporting and b')
- `custom_period.start_date.day` — ambiguous (security_impact=False, rationale='Specifies the day component of the start date. This supports valid date definiti')
- `custom_period.start_date.month` — ambiguous (security_impact=False, rationale='Specifies the month component of the start date. This is part of date configurat')
- `custom_period.start_date.year` — ambiguous (security_impact=False, rationale='Specifies the year component of the start date. This is part of date formatting ')
- `threshold_rules` — arg_unmatched (security_impact=True, rationale='Threshold rules ensure that alerts are triggered before spending exceeds the bud')

### `certificate_manager_trust_config`
- `allowlisted_certificates` — arg_unmatched (security_impact=True, rationale='Allowlisted certificates are trust exceptions and should not be configured direc')

### `chronicle_data_access_scope`
- `allowed_data_access_labels` — arg_unmatched (security_impact=True, rationale='Without any allowed labels or allow_all set to true, the scope becomes ineffecti')

### `chronicle_retrohunt`
- `process_interval` — arg_unmatched (security_impact=False)

### `chronicle_watchlist`
- `entity_population_mechanism` — arg_unmatched (security_impact=True, rationale='Manual population can lead to incomplete or outdated watchlists, increasing risk')
- `entity_population_mechanism.manual` — arg_unmatched (security_impact=True, rationale='Manual entry is error-prone and may miss timely threat intelligence, reducing th')

### `cloud_asset_folder_feed`
- `condition` — arg_unmatched (security_impact=True, rationale='Controls which events are monitored. Incorrect conditions may miss critical even')
- `feed_output_config` — arg_unmatched (security_impact=True, rationale='Determines the destination of asset monitoring data. Incorrect configuration may')
- `pubsub_destination` — arg_unmatched (security_impact=True, rationale='Defines where asset monitoring data is sent.')

### `cloud_quotas_quota_preference`
- `contact_email` — nonbool_string (security_impact='Medium. Restricting contact email improves ownership and accountability.')
- `dimensions` — nonbool_string (security_impact='High. Restricting dimensions such as region enforces locality and environment boundaries.')
- `ignore_safety_checks` — nonbool_string (security_impact='High. Ignoring quota safety checks bypasses built-in safeguards.')
- `justification` — nonbool_string (security_impact='Medium. Requiring approved justification improves governance and auditability.')
- `name` — nonbool_string (security_impact='Low. Primarily an identifier, not the main security control.')
- `parent` — nonbool_string (security_impact='High. This controls where quota changes are allowed to be requested.')
- `quota_config` — arg_unmatched (security_impact='High. This block controls the requested quota value and related metadata.', rationale='Partially implemented. The project implemented a policy for `preferred_value` on')
- `quota_config` — nonbool_string (security_impact='High. This block controls the requested quota value and related metadata.')
- `quota_config.annotations` — nonbool_string (security_impact='Medium. Free-form metadata can create governance and data handling risk.')
- `quota_config.granted_value` — arg_unmatched (security_impact='Low. Informational output only.', rationale='Not implemented. This is an output-only field and not suitable for preventive Te')
- `quota_config.granted_value` — nonbool_string (security_impact='Low. Informational output only.')
- `quota_config.preferred_value` — nonbool_string (security_impact='High. Quota size directly affects blast radius, abuse potential, and cost exposure.')
- `quota_config.request_origin` — arg_unmatched (security_impact='Low. Informational output only.', rationale='Not implemented. This is an output-only field and not suitable for preventive Te')
- `quota_config.request_origin` — nonbool_string (security_impact='Low. Informational output only.')
- `quota_config.state_detail` — arg_unmatched (security_impact='Low. Informational output only.', rationale='Not implemented. This is an output-only field and not suitable for preventive Te')
- `quota_config.state_detail` — nonbool_string (security_impact='Low. Informational output only.')
- `quota_config.trace_id` — arg_unmatched (security_impact='Low. Tracking metadata only.', rationale='Not implemented. This is an output-only field and not suitable for preventive Te')
- `quota_config.trace_id` — nonbool_string (security_impact='Low. Tracking metadata only.')
- `quota_id` — nonbool_string (security_impact='Medium. Restricting quota IDs prevents requests against unapproved quota types.')
- `service` — nonbool_string (security_impact='Medium. Restricting service prevents quota changes from being requested for unapproved GCP services.')

### `cloud_run_domain_mapping`
- `metadata` — arg_unmatched (security_impact=True, rationale='Metadata supports ownership, governance, tracking, and correct project alignment')
- `metadata.effective_annotations` — arg_unmatched (security_impact=False, rationale='This is an output field showing the resulting annotations after creation.')
- `metadata.effective_labels` — arg_unmatched (security_impact=False, rationale='This is an output field showing the resulting labels after creation.')
- `metadata.generation` — arg_unmatched (security_impact=False, rationale='This is an output field generated by the provider and is not directly configured')
- `metadata.resource_version` — arg_unmatched (security_impact=False, rationale='This is an output field generated by the provider and is not suitable for a user')
- `metadata.self_link` — arg_unmatched (security_impact=False, rationale='This is an output field generated by the provider.')
- `metadata.terraform_labels` — arg_unmatched (security_impact=False, rationale='This is an output field generated from configured labels and provider defaults.')
- `metadata.uid` — arg_unmatched (security_impact=False, rationale='This is generated by the server and cannot be directly configured as a preventiv')
- `spec` — arg_unmatched (security_impact=True, rationale='The spec block controls routing, certificate mode, and override behaviour for th')

### `cloud_run_service`
- `config_map_ref` — arg_unmatched (security_impact=False, rationale='Config map references are deprecated and not supported by the Cloud Run API.')
- `config_map_ref.local_object_reference` — arg_unmatched (security_impact=False, rationale='This setting belongs to deprecated config map behaviour and was not included as ')
- `config_map_ref.optional` — ambiguous (security_impact=False, rationale='This setting is related to deprecated config map behaviour and was not included ')
- `containers` — arg_unmatched (security_impact=True, rationale='Container configuration affects image trust, secrets handling, exposed ports, co')
- `containers.env` — arg_unmatched (security_impact=True, rationale='Environment variables can expose sensitive values if secrets are hardcoded in pl')
- `containers.env_from` — arg_unmatched (security_impact=True, rationale='Environment sources can affect how configuration and secrets are loaded into the')
- `containers.liveness_probe` — arg_unmatched (security_impact=False, rationale='Liveness probes are mainly availability controls and do not directly control acc')
- `containers.name` — ambiguous (security_impact=False, rationale='Container name is mainly an identifier and does not directly affect security pos')
- `containers.ports` — arg_unmatched (security_impact=True, rationale='Only approved container ports should be exposed to reduce unintended service exp')
- `containers.resources` — arg_unmatched (security_impact=True, rationale='Resource settings help control cost, capacity, abuse risk, and service stability')
- `containers.startup_probe` — arg_unmatched (security_impact=False, rationale='Startup probes are mainly availability controls and do not directly control acce')
- `containers.volume_mounts` — arg_unmatched (security_impact=True, rationale='Volume mounts can expose secrets or storage paths to the container and should be')
- `csi` — arg_unmatched (security_impact=True, rationale='CSI volumes can expose external storage to the service and should be reviewed wh')
- `csi.read_only` — ambiguous (security_impact=True, rationale='Read-only mounts reduce write risk to mounted storage.')
- `empty_dir` — arg_unmatched (security_impact=False, rationale='EmptyDir is ephemeral storage and was not included in the implemented security p')
- `env` — arg_unmatched (security_impact=True, rationale='Environment variables may contain sensitive values and should use Secret Manager')
- `env.name` — ambiguous (security_impact=False, rationale='Environment variable names are usually identifiers and are not directly sensitiv')
- `env.value` — ambiguous (security_impact=True, rationale='Plaintext environment variable values can expose passwords, tokens, keys, and ot')
- `env.value_from` — arg_unmatched (security_impact=True, rationale='Using Secret Manager references is safer than hardcoding plaintext values when a')
- `env_from` — arg_unmatched (security_impact=True, rationale='Environment sources can load configuration or secrets into the container and sho')
- `env_from.config_map_ref` — arg_unmatched (security_impact=False, rationale='Config map references are deprecated and not supported by the Cloud Run API.')
- `env_from.secret_ref` — arg_unmatched (security_impact=True, rationale='Secret references can expose secrets to the container and should point only to a')
- `grpc` — arg_unmatched (security_impact=False, rationale='gRPC probe settings are health check related and were not treated as core securi')
- `grpc.port` — ambiguous (security_impact=False, rationale='Probe port is operational. Container port exposure is handled separately.')
- `grpc.service` — ambiguous (security_impact=False, rationale='gRPC probe service name is operational and application-specific.')
- `http_get` — arg_unmatched (security_impact=False, rationale='HTTP probe settings are used for health checking and were not treated as core se')
- `http_get.http_headers` — arg_unmatched (security_impact=False, rationale='Health check headers are application-specific and were not treated as a general ')
- `http_get.path` — ambiguous (security_impact=False, rationale='Health check path is operational and application-specific.')
- `http_get.port` — ambiguous (security_impact=False, rationale='Health check port is operational. Container port exposure is handled separately.')
- `http_headers` — arg_unmatched (security_impact=False, rationale='HTTP headers here are used for probes and are application-specific.')
- `http_headers.name` — ambiguous (security_impact=False, rationale='Probe header name is application-specific and not a core Cloud Run security cont')
- `http_headers.value` — ambiguous (security_impact=True, rationale='Header values can contain sensitive data, but probe headers were not included in')
- `items` — arg_unmatched (security_impact=True, rationale='Secret item configuration affects the secret version, mounted file path, and fil')
- `items.key` — ambiguous (security_impact=True, rationale='Only approved secret versions should be mounted.')
- `items.path` — ambiguous (security_impact=True, rationale='Secret item paths should be approved and should not use unsafe or unexpected fil')
- `liveness_probe` — arg_unmatched (security_impact=False, rationale='Liveness probe configuration mainly affects availability and restart behaviour, ')
- `liveness_probe.failure_threshold` — ambiguous (security_impact=False, rationale='Failure Threshold is an availability or health-check setting and was not treated')
- `liveness_probe.grpc` — arg_unmatched (security_impact=False, rationale='Grpc is an availability or health-check setting and was not treated as a core se')
- `liveness_probe.http_get` — arg_unmatched (security_impact=False, rationale='Http Get is an availability or health-check setting and was not treated as a cor')
- `liveness_probe.initial_delay_seconds` — ambiguous (security_impact=False, rationale='Initial Delay Seconds is an availability or health-check setting and was not tre')
- `liveness_probe.period_seconds` — ambiguous (security_impact=False, rationale='Period Seconds is an availability or health-check setting and was not treated as')
- `liveness_probe.timeout_seconds` — ambiguous (security_impact=False, rationale='Timeout Seconds is an availability or health-check setting and was not treated a')
- `local_object_reference` — arg_unmatched (security_impact=False, rationale='Local object reference is mainly an identifier within deprecated config map or s')
- `local_object_reference.name` — ambiguous (security_impact=False, rationale='Name is only an identifier in this context and was not treated as a direct secur')
- `metadata` — arg_unmatched (security_impact=True, rationale='Metadata can include restricted annotations that affect ingress, Binary Authoriz')
- `metadata.effective_annotations` — arg_unmatched (security_impact=False, rationale='Effective annotations are output data showing all annotations present on the res')
- `metadata.effective_labels` — arg_unmatched (security_impact=False, rationale='Effective labels are output data showing all labels present on the resource and ')
- `metadata.generation` — arg_unmatched (security_impact=False, rationale='Generation is an output value used for state tracking and is not a user-controll')
- `metadata.name` — ambiguous (security_impact=False, rationale='Metadata name is mainly used for identification and does not directly control ac')
- `metadata.resource_version` — arg_unmatched (security_impact=False, rationale='Resource version is an output value used for change detection and is not a user-')
- `metadata.self_link` — arg_unmatched (security_impact=False, rationale='Self link is an output value representing the resource path and is not a user-co')
- `metadata.terraform_labels` — arg_unmatched (security_impact=False, rationale='Terraform labels are output-derived label data and are not directly enforced as ')
- `metadata.uid` — arg_unmatched (security_impact=False, rationale='UID is generated by the platform and does not directly affect the configured sec')
- `nfs` — arg_unmatched (security_impact=True, rationale='NFS volumes can expose external file systems to the service and should be review')
- `nfs.path` — ambiguous (security_impact=True, rationale='Only approved exported NFS paths should be mounted.')
- `nfs.read_only` — ambiguous (security_impact=True, rationale='Read-only NFS mounts reduce the risk of unwanted writes to shared storage.')
- `ports` — arg_unmatched (security_impact=True, rationale='Port settings affect how the container receives traffic.')
- `ports.name` — ambiguous (security_impact=False, rationale='The protocol name is mainly operational and was not treated as a core security p')
- `resources` — arg_unmatched (security_impact=True, rationale='CPU and memory settings help control cost, capacity, stability, and abuse risk.')
- `secret` — arg_unmatched (security_impact=True, rationale='Secret volume settings affect which secrets are mounted and how readable the mou')
- `secret.items` — arg_unmatched (security_impact=True, rationale='Secret items should use approved versions, safe file paths, and restrictive perm')
- `secret_key_ref` — arg_unmatched (security_impact=True, rationale='Secret references must point to approved secrets and approved versions.')
- `secret_key_ref.key` — ambiguous (security_impact=True, rationale='Only approved secret versions should be used to avoid referencing old or unmanag')
- `secret_key_ref.name` — ambiguous (security_impact=True, rationale='Only approved Secret Manager secret names should be referenced by Cloud Run serv')
- `secret_ref` — arg_unmatched (security_impact=True, rationale='Secret references can expose sensitive values to the container and should use ap')
- `secret_ref.local_object_reference` — arg_unmatched (security_impact=True, rationale='The referenced secret object should be approved before being exposed to the cont')
- `secret_ref.optional` — ambiguous (security_impact=False, rationale='Optional controls whether the secret must exist. It is operational and was not t')
- `spec` — arg_unmatched (security_impact=True, rationale='The spec defines the runtime security posture of the Cloud Run revision.')
- `spec.containers` — arg_unmatched (security_impact=True, rationale='Containers define the runtime image, environment variables, ports, resources, an')
- `spec.serving_state` — arg_unmatched (security_impact=False, rationale='Serving state is output and deprecated. It is not a supported user-controlled se')
- `spec.timeout_seconds` — ambiguous (security_impact=True, rationale='Excessive timeout values can tie up resources for too long and increase cost or ')
- `spec.volumes` — arg_unmatched (security_impact=True, rationale='Volumes can expose secrets or external storage to the container and should use a')
- `startup_probe` — arg_unmatched (security_impact=False, rationale='Startup probe configuration mainly affects availability and startup behaviour, n')
- `startup_probe.failure_threshold` — ambiguous (security_impact=False, rationale='Failure Threshold is an availability or health-check setting and was not treated')
- `startup_probe.grpc` — arg_unmatched (security_impact=False, rationale='Grpc is an availability or health-check setting and was not treated as a core se')
- `startup_probe.http_get` — arg_unmatched (security_impact=False, rationale='Http Get is an availability or health-check setting and was not treated as a cor')
- `startup_probe.initial_delay_seconds` — ambiguous (security_impact=False, rationale='Initial Delay Seconds is an availability or health-check setting and was not tre')
- `startup_probe.period_seconds` — ambiguous (security_impact=False, rationale='Period Seconds is an availability or health-check setting and was not treated as')
- `startup_probe.tcp_socket` — arg_unmatched (security_impact=False, rationale='Tcp Socket is an availability or health-check setting and was not treated as a c')
- `startup_probe.timeout_seconds` — ambiguous (security_impact=False, rationale='Timeout Seconds is an availability or health-check setting and was not treated a')
- `tcp_socket` — arg_unmatched (security_impact=False, rationale='TCP socket is used for health checking and was not treated as a core Cloud Run s')
- `tcp_socket.port` — ambiguous (security_impact=False, rationale='Probe port is part of health checking. Container port exposure is handled separa')
- `template` — arg_unmatched (security_impact=True, rationale='The template defines the revision configuration, including runtime identity, con')
- `template.metadata` — arg_unmatched (security_impact=True, rationale='Template metadata can include revision-level annotations for scaling, Cloud SQL,')
- `template.spec` — arg_unmatched (security_impact=True, rationale='The revision spec controls runtime identity, timeout, concurrency, containers, r')
- `traffic` — arg_unmatched (security_impact=True, rationale='Traffic settings control how requests are routed between revisions and can affec')
- `traffic.url` — arg_unmatched (security_impact=False, rationale='URL is an output-only status value and is not configured directly in the Terrafo')
- `value_from` — arg_unmatched (security_impact=True, rationale='Value sources can retrieve sensitive values and should use approved Secret Manag')
- `value_from.secret_key_ref` — arg_unmatched (security_impact=True, rationale='Secret key references must point to approved secrets and approved secret version')
- `volume_mounts` — arg_unmatched (security_impact=True, rationale='Volume mounts expose files or secrets inside the container and should be limited')
- `volume_mounts.name` — ambiguous (security_impact=False, rationale='The volume mount name is mainly used to match a declared volume and is not direc')
- `volumes` — arg_unmatched (security_impact=True, rationale='Volumes can expose secrets or external file systems to the container.')
- `volumes.csi` — arg_unmatched (security_impact=True, rationale='CSI volumes can expose external storage to the service. They were considered opt')
- `volumes.empty_dir` — arg_unmatched (security_impact=False, rationale='EmptyDir is ephemeral storage and was not included in the implemented high-impac')
- `volumes.name` — ambiguous (security_impact=False, rationale='Volume name is mainly used to match the volume with the related mount.')
- `volumes.nfs` — arg_unmatched (security_impact=True, rationale='NFS volumes can expose external file systems to the service. They were considere')
- `volumes.secret` — arg_unmatched (security_impact=True, rationale='Secret volumes must use approved secrets, restrictive file permissions, and safe')

### `cloud_run_v2_job`
- `binary_authorization` — arg_unmatched (security_impact=True, rationale='Ensures only trusted, verified container images are deployed, preventing executi')
- `cloud_sql_instance` — arg_unmatched (security_impact=False, rationale='This specifies Cloud SQL connections but does not itself need any security contr')
- `containers` — arg_unmatched (security_impact=True, rationale='Defines the runtime execution unit and enforces secure container configuration t')
- `containers.env` — arg_unmatched (security_impact=True, rationale='Controls environment variables for containers and helps prevent sensitive data e')
- `containers.name` — ambiguous (security_impact=False, rationale='No direct security risk as it only defines a container identifier.')
- `containers.ports` — arg_unmatched (security_impact=False, rationale='No direct security risk as it exposes container ports but does not itself contro')
- `containers.resources` — arg_unmatched (security_impact=False, rationale='No direct security risk as it defines compute allocation')
- `containers.startup_probe` — arg_unmatched (security_impact=False, rationale='No direct security risk as it defines health checks and does not affect access c')
- `containers.volume_mounts` — arg_unmatched (security_impact=False, rationale=' No direct security risk directly as security considerations are handled within ')
- `empty_dir` — arg_unmatched (security_impact=False, rationale='This defines ephemeral storage and does not need any security policy')
- `env` — arg_unmatched (security_impact=True, rationale='Controls environment variables for containers and helps prevent sensitive data e')
- `env.name` — ambiguous (security_impact=False, rationale='No direct security risk as it defines environment variable names without exposin')
- `env.value` — ambiguous (security_impact=True, rationale='Prevents exposure of sensitive data by disallowing plaintext environment variabl')
- `env.value_source` — arg_unmatched (security_impact=True, rationale='Ensures environment variable values are securely sourced instead of being hardco')
- `gcs` — arg_unmatched (security_impact=True, rationale='Controls access to Cloud Storage buckets mounted as volumes and reduces risk of ')
- `gcs.read_only` — ambiguous (security_impact=True, rationale='Ensures Cloud Run services mount Cloud Storage volumes in read-only mode to prev')
- `grpc` — arg_unmatched (security_impact=False, rationale='This defines gRPC probe configuration and is related to health monitoring')
- `grpc.port` — ambiguous (security_impact=False, rationale='This specifies the gRPC port')
- `http_get` — arg_unmatched (security_impact=False, rationale='This defines HTTP probe behavior')
- `http_get.http_headers` — arg_unmatched (security_impact=False, rationale='This allows custom headers for probes')
- `http_get.path` — ambiguous (security_impact=False, rationale='This specifies the endpoint path for health checks and does not introduce securi')
- `http_get.port` — ambiguous (security_impact=False, rationale='This defines the port for HTTP checks and is not related to security control.')
- `http_headers` — arg_unmatched (security_impact=False, rationale='This is used to define headers for requests and does not need any security contr')
- `http_headers.name` — ambiguous (security_impact=False, rationale='This specifies the header name and does not need security measures.')
- `http_headers.value` — ambiguous (security_impact=False, rationale='This specifies the header value and does not need to enforce restrictions')
- `items` — arg_unmatched (security_impact=False, rationale='This maps secret data into files and is more about structure')
- `items.path` — ambiguous (security_impact=False, rationale='This just specifies file location for secrets')
- `items.version` — ambiguous (security_impact=False, rationale='This just selects versions, does not require security policy')
- `network_interfaces` — arg_unmatched (security_impact=False, rationale='This defines network configuration and does not require restriction as it does n')
- `nfs` — arg_unmatched (security_impact=True, rationale='Manages shared file system access and helps enforce safe mounting to prevent una')
- `nfs.path` — ambiguous (security_impact=False, rationale='This defines the shared path and does not need policy as it does not enforce acc')
- `nfs.read_only` — ambiguous (security_impact=True, rationale='Ensures NFS-mounted volumes in Cloud Run services are set to read-only mode to p')
- `node_selector` — arg_unmatched (security_impact=False, rationale='This defines hardware selection and does not require restriction since it does n')
- `ports` — arg_unmatched (security_impact=False, rationale='This configuration defines which port the container listens on and does not cont')
- `ports.name` — ambiguous (security_impact=False, rationale='This only indicates the protocol type for the port and does not impact security')
- `resources` — arg_unmatched (security_impact=False, rationale='This defines compute resource allocation such as CPU and memory for the containe')
- `run_execution_token` — arg_unmatched (security_impact=False, rationale='No direct security risk as it is used for execution tracking and does not affect')
- `secret` — ambiguous (security_impact=False, rationale='This argument enables mounting of secrets within volumes, and no restriction is ')
- `secret.items` — arg_unmatched (security_impact=False, rationale='This maps secret data into files and is more about structure')
- `secret.secret` — ambiguous (security_impact=False, rationale='This argument references a Secret Manager secret, and restriction is not applied')
- `secret_key_ref` — arg_unmatched (security_impact=True, rationale='Enforces retrieval of sensitive values from Secret Manager to prevent exposure o')
- `secret_key_ref.secret` — ambiguous (security_impact=True, rationale='Ensures environment variables source sensitive values from Secret Manager, impro')
- `secret_key_ref.version` — ambiguous (security_impact=False, rationale='No direct security risk as it defines only version details without any effect on')
- `start_execution_token` — arg_unmatched (security_impact=False, rationale='No direct security risk as it is used for execution uniqueness and does not impa')
- `startup_probe` — arg_unmatched (security_impact=False, rationale='This controls container startup health checks and is related to lifecycle manage')
- `startup_probe.grpc` — arg_unmatched (security_impact=False, rationale='This defines gRPC-based health checks and is unrelated to security enforcement.')
- `startup_probe.http_get` — arg_unmatched (security_impact=False, rationale='This defines HTTP-based health checks.')
- `startup_probe.tcp_socket` — arg_unmatched (security_impact=False, rationale='This defines a probe mechanism using TCP and is used for health checking.')
- `tcp_socket` — arg_unmatched (security_impact=False, rationale='This is used for connectivity checks and does not provide any security risks.')
- `tcp_socket.port` — ambiguous (security_impact=False, rationale='This specifies the port used for TCP checks and does not impact security.')
- `template` — arg_unmatched (security_impact=True, rationale='Defines the overall job execution structure and enforces secure configuration bo')
- `template.containers` — arg_unmatched (security_impact=True, rationale='Defines the runtime execution unit and enforces secure container configuration t')
- `template.node_selector` — arg_unmatched (security_impact=False, rationale='ot introduce direct security risk as it specifies hardware preferences')
- `template.template` — arg_unmatched (security_impact=True, rationale='Controls the actual task execution environment, ensuring runtime components are ')
- `template.volumes` — arg_unmatched (security_impact=False, rationale='No direct security risk directly as configuration details are enforced at child ')
- `template.vpc_access` — arg_unmatched (security_impact=True, rationale='Controls network connectivity for Cloud Run Jobs to restrict traffic flow and re')
- `value_source` — arg_unmatched (security_impact=True, rationale='Ensures environment variable values are securely sourced instead of being hardco')
- `value_source.secret_key_ref` — arg_unmatched (security_impact=True, rationale='Enforces retrieval of sensitive values from Secret Manager to prevent exposure o')
- `volume_mounts` — arg_unmatched (security_impact=False, rationale='This maps storage volumes into the container filesystem.')
- `volume_mounts.name` — ambiguous (security_impact=False, rationale='This is just an identifier linking to a defined volume')
- `volumes` — arg_unmatched (security_impact=False, rationale='This defines available storage volumes')
- `volumes.cloud_sql_instance` — arg_unmatched (security_impact=False, rationale='This specifies Cloud SQL connections but does not itself need any security contr')
- `volumes.empty_dir` — arg_unmatched (security_impact=False, rationale='This defines temporary storage and does not involve sensitive data protection me')
- `volumes.gcs` — arg_unmatched (security_impact=True, rationale='Controls access to Cloud Storage buckets mounted as volumes and reduces risk of ')
- `volumes.name` — ambiguous (security_impact=False, rationale='This is just an identifier for the volume')
- `volumes.nfs` — arg_unmatched (security_impact=True, rationale='Manages shared file system access and helps enforce safe mounting to prevent una')
- `volumes.secret` — ambiguous (security_impact=False, rationale='This argument enables mounting of secrets within volumes, and no restriction is ')
- `vpc_access` — arg_unmatched (security_impact=True, rationale='Controls network connectivity for Cloud Run Jobs to restrict traffic flow and re')
- `vpc_access.network_interfaces` — arg_unmatched (security_impact=False, rationale='This defines networking structure and does not need restriction since access con')

### `cloud_run_v2_service`
- `binary_authorization` — arg_unmatched (security_impact=True, rationale='Ensures only verified and trusted container images are deployed, protecting agai')
- `build_config` — arg_unmatched (security_impact=False, rationale='This is a grouping for build settings and does not directly impact security.')
- `build_config.name` — ambiguous (security_impact=False, rationale='This is an output field and does not affect security.')
- `cloud_sql_instance` — arg_unmatched (security_impact=False, rationale='This specifies Cloud SQL connections but does not itself need any security contr')
- `containers` — arg_unmatched (security_impact=True, rationale='Defines the application runtime environment and helps ensure only secure and app')
- `containers.build_info` — arg_unmatched (security_impact=False, rationale='This defines base image reference and does not enforce security.')
- `containers.env` — arg_unmatched (security_impact=True, rationale='Manages environment variables in the container and helps prevent accidental expo')
- `containers.liveness_probe` — arg_unmatched (security_impact=False, rationale='This is for health checks and does not affect security.')
- `containers.name` — ambiguous (security_impact=False, rationale='This only defines container name and does not affect security.')
- `containers.ports` — arg_unmatched (security_impact=False, rationale='This exposes ports but does not itself control network security.')
- `containers.resources` — arg_unmatched (security_impact=False, rationale='This defines compute resources and does not affect security.')
- `containers.startup_probe` — arg_unmatched (security_impact=False, rationale='This controls startup checks and does not impact security.')
- `containers.volume_mounts` — arg_unmatched (security_impact=False, rationale='This mounts volumes but security is controlled at volume level.')
- `empty_dir` — arg_unmatched (security_impact=False, rationale='This defines temporary storage within the container lifecycle, which does not pe')
- `env` — arg_unmatched (security_impact=True, rationale='Manages environment variables in the container and helps prevent accidental expo')
- `env.name` — ambiguous (security_impact=False, rationale='This defines variable name and does not expose sensitive data.')
- `env.value` — ambiguous (security_impact=False, rationale='This is a value field but does not enforce or control security itself.')
- `env.value_source` — arg_unmatched (security_impact=True, rationale='Ensures environment variables are securely sourced from trusted systems instead ')
- `gcs` — arg_unmatched (security_impact=True, rationale='Controls access to Cloud Storage buckets mounted as volumes and reduces risk of ')
- `gcs.read_only` — ambiguous (security_impact=True, rationale='Ensures Cloud Run services mount Cloud Storage volumes in read-only mode to prev')
- `grpc` — arg_unmatched (security_impact=False, rationale='This defines configuration for gRPC-based probes and is used only for health mon')
- `grpc.port` — ambiguous (security_impact=False, rationale='This specifies the port used for gRPC checks.')
- `grpc.service` — ambiguous (security_impact=False, rationale='This defines the service name for health checks and does not introduce any secur')
- `http_get` — arg_unmatched (security_impact=False, rationale='This defines HTTP probe behavior for health checking.')
- `http_get.http_headers` — arg_unmatched (security_impact=False, rationale='This allows adding headers to health check requests, they do not require securit')
- `http_get.path` — ambiguous (security_impact=False, rationale='This just specifies the endpoint path used for checks')
- `http_get.port` — ambiguous (security_impact=False, rationale='This just defines which port to use for probes')
- `http_headers` — arg_unmatched (security_impact=False, rationale='This defines HTTP headers structure and is a generic configuration.')
- `http_headers.name` — ambiguous (security_impact=False, rationale='This is simply the name of a header and does not impact security.')
- `http_headers.value` — ambiguous (security_impact=False, rationale='This is the value of a header used in probes and does not introduce a need for s')
- `items` — arg_unmatched (security_impact=False, rationale='This maps secret data into files and is more about structure')
- `items.path` — ambiguous (security_impact=False, rationale='This just specifies file location for secrets')
- `items.version` — ambiguous (security_impact=False, rationale='This just selects versions, does not require security policy')
- `launch_stage` — nonbool_string (security_impact='This defines the release stage of features and does not directly affect security.')
- `liveness_probe` — arg_unmatched (security_impact=False, rationale='This monitors container health and determines restarts, which is related to avai')
- `liveness_probe.failure_threshold` — ambiguous (security_impact=False, rationale='This defines retry logic for health checks and does not influence security contr')
- `liveness_probe.grpc` — arg_unmatched (security_impact=False, rationale='This defines gRPC-based health checks and does not introduce any situations requ')
- `liveness_probe.http_get` — arg_unmatched (security_impact=False, rationale='This defines how HTTP checks are performed for health monitoring')
- `liveness_probe.initial_delay_seconds` — ambiguous (security_impact=False, rationale='This controls timing of health checks and does not interact with access or sensi')
- `liveness_probe.period_seconds` — ambiguous (security_impact=False, rationale='This sets how frequently checks run, and since it does not affect access or data')
- `liveness_probe.tcp_socket` — arg_unmatched (security_impact=False, rationale='This defines TCP-level checks for availability and does not affect security.')
- `liveness_probe.timeout_seconds` — ambiguous (security_impact=False, rationale='This defines how long to wait for probe responses, which is operational behavior')
- `network_interfaces` — arg_unmatched (security_impact=False, rationale='This is a grouping field and does not directly affect security.')
- `nfs` — arg_unmatched (security_impact=True, rationale='Manages shared file system access and helps enforce safe mounting to prevent una')
- `nfs.path` — ambiguous (security_impact=False, rationale='This defines the shared path and does not need policy as it does not enforce acc')
- `nfs.read_only` — ambiguous (security_impact=True, rationale='Ensures NFS-mounted volumes in Cloud Run services are set to read-only mode to p')
- `node_selector` — arg_unmatched (security_impact=False, rationale='This defines infrastructure placement constraints and does not interact with sec')
- `ports` — arg_unmatched (security_impact=False, rationale='This defines which port the container listens on, and since exposure control is ')
- `ports.name` — ambiguous (security_impact=False, rationale='This specifies the protocol type used on the port, which is a configuration deta')
- `resources` — arg_unmatched (security_impact=False, rationale='This block defines compute resource allocation such as CPU and memory, which is ')
- `scaling` — arg_unmatched (security_impact=False, rationale='This is just a parent grouping for scaling settings and does not directly impact')
- `secret` — ambiguous (security_impact=True, rationale='Ensures secrets mounted as volumes are securely managed and restricted to approv')
- `secret.items` — arg_unmatched (security_impact=False, rationale='This maps secret data into files and is more about structure')
- `secret.secret` — ambiguous (security_impact=True, rationale='Ensures only approved Secret Manager secrets are mounted in Cloud Run service vo')
- `secret_key_ref` — arg_unmatched (security_impact=True, rationale='Ensures sensitive values are securely retrieved from Secret Manager instead of b')
- `secret_key_ref.secret` — ambiguous (security_impact=True, rationale='Ensures sensitive environment variables are securely sourced from Secret Manager')
- `secret_key_ref.version` — ambiguous (security_impact=False, rationale='This selects a version and does not require security policy.')
- `service_mesh` — arg_unmatched (security_impact=False, rationale='This enables service mesh features, but restricting this flag itself does not en')
- `service_mesh.mesh` — arg_unmatched (security_impact=False, rationale='This is just an identifier for the mesh and does not need a security policy.')
- `startup_probe` — arg_unmatched (security_impact=False, rationale='This defines startup health checks and is focused on application readiness, no s')
- `startup_probe.failure_threshold` — ambiguous (security_impact=False, rationale='This defines failure handling logic and does not introduce any security concerns')
- `startup_probe.grpc` — arg_unmatched (security_impact=False, rationale='This defines gRPC-based startup checks and no need for security policy.')
- `startup_probe.http_get` — arg_unmatched (security_impact=False, rationale='This defines HTTP-based startup checks and no need for enforcing secure policy.')
- `startup_probe.initial_delay_seconds` — ambiguous (security_impact=False, rationale='This controls when startup checks begin and does not affect security.')
- `startup_probe.period_seconds` — ambiguous (security_impact=False, rationale='This sets frequency of checks and does not impact security.')
- `startup_probe.tcp_socket` — arg_unmatched (security_impact=False, rationale='This defines TCP-based startup checks and no need for security policy.')
- `startup_probe.timeout_seconds` — ambiguous (security_impact=False, rationale='This defines probe timeout behavior and is not related to security.')
- `tcp_socket` — arg_unmatched (security_impact=False, rationale='This defines TCP probe configuration for health checks and is not related to enf')
- `tcp_socket.port` — ambiguous (security_impact=False, rationale='This specifies the port used for TCP checks and does not impact security.')
- `template` — arg_unmatched (security_impact=True, rationale='Defines how Cloud Run service revisions are created and ensures all workloads ru')
- `template.containers` — arg_unmatched (security_impact=True, rationale='Defines the application runtime environment and helps ensure only secure and app')
- `template.gpu_zonal_redundancy_disabled` — nonbool_string (security_impact='This affects availability and not security.')
- `template.node_selector` — arg_unmatched (security_impact='This only defines hardware requirements and does not impact security.', rationale='This defines hardware requirements, and restricting it does not contribute to en')
- `template.node_selector` — nonbool_string (security_impact='This only defines hardware requirements and does not impact security.')
- `template.revision` — nonbool_string (security_impact='This only defines a revision name and does not impact security or access.')
- `template.scaling` — arg_unmatched (security_impact=False, rationale='This is just a grouping for scaling settings and does not directly affect securi')
- `template.service_mesh` — arg_unmatched (security_impact='This enables service mesh features but does not directly enforce security on its own.', rationale='This enables service mesh features, but restricting this flag itself does not en')
- `template.service_mesh` — nonbool_string (security_impact='This enables service mesh features but does not directly enforce security on its own.')
- `template.session_affinity` — nonbool_string (security_impact='This manages session behavior and does not impact authentication or authorization.')
- `template.volumes` — arg_unmatched (security_impact=True, rationale='Provides storage access to containers and must be controlled to prevent unauthor')
- `template.vpc_access` — arg_unmatched (security_impact=True, rationale='Controls network connectivity for Cloud Run services to restrict outbound traffi')
- `traffic` — arg_unmatched (security_impact=False, rationale='This is just a grouping for traffic distribution and does not directly affect se')
- `value_source` — arg_unmatched (security_impact=True, rationale='Ensures environment variables are securely sourced from trusted systems instead ')
- `value_source.secret_key_ref` — arg_unmatched (security_impact=True, rationale='Ensures sensitive values are securely retrieved from Secret Manager instead of b')
- `volume_mounts` — arg_unmatched (security_impact=False, rationale='This describes how volumes are mounted into the container, but the actual securi')
- `volume_mounts.name` — ambiguous (security_impact=False, rationale='This is just a reference to a volume identifier and does not need security polic')
- `volumes` — arg_unmatched (security_impact=True, rationale='Provides storage access to containers and must be controlled to prevent unauthor')
- `volumes.cloud_sql_instance` — arg_unmatched (security_impact=False, rationale='This specifies connectivity configuration to Cloud SQL, while actual access is c')
- `volumes.empty_dir` — arg_unmatched (security_impact=False, rationale='This defines temporary storage within the container lifecycle, which does not pe')
- `volumes.gcs` — arg_unmatched (security_impact=True, rationale='Controls access to Cloud Storage buckets mounted as volumes and reduces risk of ')
- `volumes.name` — ambiguous (security_impact=False, rationale='This is just an identifier for the volume')
- `volumes.nfs` — arg_unmatched (security_impact=True, rationale='Manages shared file system access and helps enforce safe mounting to prevent una')
- `volumes.secret` — ambiguous (security_impact=True, rationale='Ensures secrets mounted as volumes are securely managed and restricted to approv')
- `vpc_access` — arg_unmatched (security_impact=True, rationale='Controls network connectivity for Cloud Run services to restrict outbound traffi')
- `vpc_access.network_interfaces` — arg_unmatched (security_impact=False, rationale='This is a grouping field and does not directly impact security.')

### `cloud_run_v2_worker_pool`
- `binary_authorization` — arg_unmatched (security_impact=True, rationale='Ensures only trusted and verified container images are deployed, preventing exec')
- `cloud_sql_instance` — arg_unmatched (security_impact=False, rationale='This specifies Cloud SQL connections but does not itself need any security contr')
- `containers` — arg_unmatched (security_impact=True, rationale='Ensures container configurations enforce security best practices to prevent vuln')
- `containers.env` — arg_unmatched (security_impact=True, rationale='Ensures sensitive values are sourced securely, preventing hardcoding and reducin')
- `containers.name` — ambiguous (security_impact=False, rationale='This defines container name and does not impact security.')
- `containers.resources` — arg_unmatched (security_impact=False, rationale='This defines compute resources and does not affect security.')
- `containers.volume_mounts` — arg_unmatched (security_impact=False, rationale='This mounts volumes but security is handled at volume level.')
- `empty_dir` — arg_unmatched (security_impact=True, rationale='Ensures ephemeral storage usage is controlled to prevent resource abuse and unin')
- `env` — arg_unmatched (security_impact=True, rationale='Ensures sensitive values are sourced securely, preventing hardcoding and reducin')
- `env.name` — ambiguous (security_impact=False, rationale='This defines environment variable name and does not expose sensitive data.')
- `env.value` — ambiguous (security_impact=False, rationale='This is a value field but does not enforce or control security itself.')
- `env.value_source` — arg_unmatched (security_impact=True, rationale='Ensures environment variables are securely sourced from trusted systems instead ')
- `gcs` — arg_unmatched (security_impact=True, rationale='Controls access to Cloud Storage buckets mounted as volumes and reduces risk of ')
- `gcs.read_only` — ambiguous (security_impact=True, rationale='Ensures Cloud Run services mount Cloud Storage volumes in read-only mode to prev')
- `instance_splits` — arg_unmatched (security_impact=False, rationale='This is a grouping for traffic distribution and does not impact security.')
- `items` — arg_unmatched (security_impact=False, rationale='This maps secret data into files and is more about structure')
- `items.path` — ambiguous (security_impact=False, rationale='This just specifies file location for secrets')
- `items.version` — ambiguous (security_impact=False, rationale='This just selects versions, does not require security policy')
- `network_interfaces` — arg_unmatched (security_impact=False, rationale='This is a grouping field and does not directly impact security.')
- `nfs` — arg_unmatched (security_impact=True, rationale='Manages shared file system access and helps enforce safe mounting to prevent una')
- `nfs.path` — ambiguous (security_impact=False, rationale='This defines the shared path and does not need policy as it does not enforce acc')
- `nfs.read_only` — ambiguous (security_impact=True, rationale='Ensures NFS-mounted volumes in Cloud Run services are set to read-only mode to p')
- `node_selector` — arg_unmatched (security_impact=False, rationale='This defines infrastructure placement constraints and does not interact with sec')
- `resources` — arg_unmatched (security_impact=False, rationale='This block defines compute resource allocation such as CPU and memory, which is ')
- `scaling` — arg_unmatched (security_impact=False, rationale='This is a grouping for scaling settings and does not directly affect security.')
- `secret` — ambiguous (security_impact=True, rationale='Ensures only authorized secrets are mounted, preventing exposure of sensitive da')
- `secret.items` — arg_unmatched (security_impact=False, rationale='This maps secret data into files and is more about structure')
- `secret.secret` — ambiguous (security_impact=True, rationale='Ensure only approved Secret Manager secrets are mounted in volumes to prevent ex')
- `secret_key_ref` — arg_unmatched (security_impact=True, rationale='Ensures secrets are retrieved securely from Secret Manager to prevent credential')
- `secret_key_ref.secret` — ambiguous (security_impact=True, rationale='Ensures sensitive environment variables are securely retrieved from Secret Manag')
- `secret_key_ref.version` — ambiguous (security_impact=False, rationale='This selects a version and does not require security policy.')
- `template` — arg_unmatched (security_impact=True, rationale='Ensures all revision configurations follow enforced security controls to prevent')
- `template.containers` — arg_unmatched (security_impact=True, rationale='Ensures container configurations enforce security best practices to prevent vuln')
- `template.node_selector` — arg_unmatched (security_impact=False, rationale='This specifies hardware requirements and does not impact security.')
- `template.volumes` — arg_unmatched (security_impact=True, rationale='Ensures mounted storage is securely configured to prevent unauthorized data acce')
- `template.vpc_access` — arg_unmatched (security_impact=True, rationale='Ensures network connectivity is controlled to prevent unauthorized access and re')
- `value_source` — arg_unmatched (security_impact=True, rationale='Ensures environment variables are securely sourced from trusted systems instead ')
- `value_source.secret_key_ref` — arg_unmatched (security_impact=True, rationale='Ensures secrets are retrieved securely from Secret Manager to prevent credential')
- `volume_mounts` — arg_unmatched (security_impact=False, rationale='This describes how volumes are mounted into the container, but the actual securi')
- `volume_mounts.name` — ambiguous (security_impact=False, rationale='This is just a reference to a volume identifier and does not need security polic')
- `volumes` — arg_unmatched (security_impact=True, rationale='Ensures mounted storage is securely configured to prevent unauthorized data acce')
- `volumes.cloud_sql_instance` — arg_unmatched (security_impact=False, rationale='This specifies connectivity configuration to Cloud SQL, while actual access is c')
- `volumes.empty_dir` — arg_unmatched (security_impact=True, rationale='Ensures ephemeral storage usage is controlled to prevent resource abuse and unin')
- `volumes.gcs` — arg_unmatched (security_impact=True, rationale='Controls access to Cloud Storage buckets mounted as volumes and reduces risk of ')
- `volumes.name` — ambiguous (security_impact=False, rationale='This is just an identifier for the volume')
- `volumes.nfs` — arg_unmatched (security_impact=True, rationale='Manages shared file system access and helps enforce safe mounting to prevent una')
- `volumes.secret` — ambiguous (security_impact=True, rationale='Ensures only authorized secrets are mounted, preventing exposure of sensitive da')
- `vpc_access` — arg_unmatched (security_impact=True, rationale='Ensures network connectivity is controlled to prevent unauthorized access and re')
- `vpc_access.network_interfaces` — arg_unmatched (security_impact=False, rationale='This is a grouping field and does not directly impact security.')

### `cloud_scheduler_job`
- `app_engine_http_target` — arg_unmatched (security_impact=True, rationale='Should use a http method that can not be used maliciously, service set so that i')
- `app_engine_http_target.app_engine_routing` — arg_unmatched (security_impact=False, rationale='The configuration of the route for app_engine which does not have an effect on t')
- `app_engine_routing` — arg_unmatched (security_impact=False, rationale='Used to route the request to the specified service and does not have any effect ')
- `http_target` — arg_unmatched (security_impact=False, rationale='The http target itself which would have no impact on security properties')
- `http_target.oauth_token` — arg_unmatched (security_impact=True, rationale='Must be implemented to apply authentication upon the use of this service')
- `http_target.oidc_token` — arg_unmatched (security_impact=True, rationale='Must be implemented to apply authentication upon the use of this service')
- `oauth_token` — arg_unmatched (security_impact=True, rationale='Need to be implemented to add a level of authentication when required')
- `oauth_token.service_account_email` — ambiguous (security_impact=True, rationale='Service account email should be correctly configured and not use overly priviled')
- `oidc_token` — arg_unmatched (security_impact=True, rationale='Need to be implemented to add a level of authentication when required')
- `oidc_token.service_account_email` — ambiguous (security_impact=True, rationale='Service account email should be correctly configured and not use overly priviled')
- `pubsub_target` — arg_unmatched (security_impact=False, rationale='Purpose is just the target of pubsub which would not affect the security of the ')
- `retry_config` — arg_unmatched (security_impact=False, rationale='Used for the configuration for retries thus not security related')

### `cloudbuildv2_connection`
- `bitbucket_cloud_config.read_authorizer_credential` — arg_unmatched (security_impact=True, rationale='This credential must be securely referenced and validated to ensure only authori')
- `bitbucket_data_center_config.authorizer_credential` — arg_unmatched (security_impact=True, rationale='The authorizer credential holds a high-privilege REPO_ADMIN access token stored ')
- `bitbucket_data_center_config.read_authorizer_credential` — arg_unmatched (security_impact=True, rationale='This credential must reference an approved secret version to ensure only trusted')
- `bitbucket_data_center_config.service_directory_config` — arg_unmatched (security_impact=True, rationale='Using Service Directory ensures private and secure connectivity to Bitbucket Dat')
- `github_config.authorizer_credential` — arg_unmatched (security_impact=True, rationale='The OAuth token used in the authorizer credential must be securely stored in an ')

### `clouddeploy_automation`
- `advance_rollout_rule` — arg_unmatched (security_impact=False, rationale='Defines the conditions for advancing a rollout but cannot execute it without the')
- `promote_release_rule` — arg_unmatched (security_impact=False, rationale='Defines the conditions for a promotion but cannot execute it without the permiss')
- `repair_phases` — arg_unmatched (security_impact=False, rationale='A container block that defines one or more repair strategies (e.g., retry, rollb')
- `repair_rollout_rule` — arg_unmatched (security_impact=False, rationale='Defines the conditions and actions for repairing a rollout but cannot execute th')
- `rules` — arg_unmatched (security_impact=False, rationale="Rules define the automation's behavior and logic but do not, by themselves, gran")
- `rules.advance_rollout_rule` — arg_unmatched (security_impact=False, rationale='Defines the conditions for advancing a rollout but cannot execute it without the')
- `rules.promote_release_rule` — arg_unmatched (security_impact=False, rationale='Defines the conditions for a promotion but cannot execute it without the permiss')
- `rules.repair_rollout_rule` — arg_unmatched (security_impact=False, rationale='Defines the conditions and actions for repairing a rollout but cannot execute th')
- `rules.timed_promote_release_rule` — arg_unmatched (security_impact=False, rationale='Defines a schedule for automatic promotions but cannot execute them without the ')
- `selector` — arg_unmatched (security_impact=False, rationale='Defines the scope of targets the automation applies to (e.g., specific targets o')
- `targets.id` — ambiguous (security_impact=False, rationale='Specifies which target(s) to act upon. Using "*" broadens scope but does not gra')
- `targets.labels` — ambiguous (security_impact=False, rationale='Used to select targets based on metadata labels. This is a filtering operation a')
- `timed_promote_release_rule` — arg_unmatched (security_impact=False, rationale='Defines a schedule for automatic promotions but cannot execute them without the ')

### `clouddeploy_custom_target_type`
- `custom_actions` — arg_unmatched (security_impact=True, rationale='Executes arbitrary code through Skaffold actions and can pull code from external')
- `custom_actions.include_skaffold_modules` — arg_unmatched (security_impact=True, rationale='Can include external Skaffold configurations that may contain malicious code or ')
- `include_skaffold_modules` — arg_unmatched (security_impact=True, rationale='Parent block for external module inclusion - inherits security concerns from chi')
- `include_skaffold_modules.git` — arg_unmatched (security_impact=True, rationale='References external Git repositories which could contain malicious Skaffold conf')
- `include_skaffold_modules.google_cloud_build_repo` — arg_unmatched (security_impact=True, rationale='References external Cloud Build repositories which could contain malicious confi')
- `include_skaffold_modules.google_cloud_storage` — arg_unmatched (security_impact=True, rationale='References external Cloud Storage buckets which could contain malicious configur')

### `clouddeploy_delivery_pipeline`
- `deploy_parameters` — arg_unmatched (security_impact=False, rationale=': Environment-specific deployment parameters are typically configuration values,')
- `serial_pipeline` — arg_unmatched (security_impact=False, rationale="Pipeline structure configuration - defines stages but doesn't control execution ")
- `serial_pipeline.stages` — arg_unmatched (security_impact=False, rationale='Defines deployment stage sequence - organizational configuration with no direct ')
- `stages` — arg_unmatched (security_impact=False, rationale='Stage configuration container - security impact comes from nested arguments, not')
- `stages.deploy_parameters` — arg_unmatched (security_impact=True, rationale='Can pass arbitrary parameters to deployment processes. Could be used to inject m')
- `stages.profiles` — ambiguous (security_impact=True, rationale='Skaffold profiles can alter deployment behavior and reference external configura')
- `stages.strategy` — arg_unmatched (security_impact=False, rationale='Deployment strategy configuration (canary vs standard) - affects rollout behavio')
- `strategy` — arg_unmatched (security_impact=False, rationale='Container for deployment strategy options - security impact comes from nested cu')
- `strategy.canary` — arg_unmatched (security_impact=False, rationale='Canary strategy configuration - affects rollout behavior but inherits security c')
- `strategy.standard` — arg_unmatched (security_impact=False, rationale='Standard strategy configuration - affects rollout behavior but inherits security')

### `clouddeploy_deploy_policy`
- `rollout_restriction` — arg_unmatched (security_impact=True, rationale='Defines whether the policy applies to human users (`USER`), automation systems (')
- `rules` — arg_unmatched (security_impact=False, rationale='Terraform schema validation requires this block and throws error if missing: At ')
- `selectors` — arg_unmatched (security_impact=False, rationale='Defines the scope of targets the deployment applies to (e.g., specific targets o')
- `selectors.delivery_pipeline` — arg_unmatched (security_impact=False)
- `selectors.target` — arg_unmatched (security_impact=False)

### `clouddeploy_target`
- `anthos_cluster` — arg_unmatched (security_impact=False, rationale='Define where to deploy Anthos cluster and are therefore connectivity and operati')
- `execution_configs` — arg_unmatched (security_impact=False, rationale='Defines execution environments and service accounts used for deployment. While w')
- `multi_target` — arg_unmatched (security_impact=False, rationale='Enables parallel deployment to multiple targets simultaneously. While misconfigu')
- `run` — arg_unmatched (security_impact=True, rationale='This block defines the Cloud Run service to target. The location of the service ')

### `clouddomains_registration`
- `custom_dns.ds_records` — arg_unmatched (security_impact=True, rationale='DNSSEC signs DNS records with digital signatures, ensuring that the DNS response')
- `management_settings.renewal_method` — arg_unmatched (security_impact=False)
- `postal_address.organization` — ambiguous (security_impact=True, rationale='Enforcing the corporate organization name in contact details ensures legal consi')
- `postal_address.region_code` — ambiguous (security_impact=True, rationale='Restricting the registration region code (e.g., to AU) ensures compliance with l')
- `registrant_contact.email` — ambiguous (security_impact=True, rationale='Restricting contact emails to official organizational domains ensures that criti')

### `cloudfunctions_function`
- `automatic_update_policy` — arg_unmatched (security_impact=True, rationale='automatic updates can patch security misconfigurations and therefore protect the')
- `effective_labels` — arg_unmatched (security_impact=False, rationale='labels has no impact on the security of the resource or the data contained')
- `event_trigger` — arg_unmatched (security_impact=False, rationale='event_trigger does not have any security effect to the resource or the data cont')
- `event_trigger.failure_policy` — arg_unmatched (security_impact=False, rationale='retry behaivour on failure has no impact on the resource security or the data co')
- `failure_policy` — arg_unmatched (security_impact=True, rationale='If a function fails, not restarting it can lead to data being lost or corrupted')
- `on_deploy_update_policy` — arg_unmatched (security_impact=False, rationale='can not be used with automatic update policy')
- `on_deploy_update_policy.runtime_version` — arg_unmatched (security_impact=False, rationale='because automatic update policy is functioning, on deploy update policy cannot a')
- `secret_environment_variables` — arg_unmatched (security_impact=False, rationale='secret environment variables have no impact on the security of the resource or t')
- `secret_volumes` — arg_unmatched (security_impact=False, rationale='Secret volumes have no impact on the security of the resource or the data contai')
- `secret_volumes.versions` — arg_unmatched (security_impact=False, rationale='Secret volumes are not configured and therefore versions have no impact on the s')
- `source_repository` — arg_unmatched (security_impact=False, rationale='The source repository does not have an impact on the security of the resource or')
- `terraform_labels` — arg_unmatched (security_impact=False, rationale='terraform labels has no impact on the security of the resource or the data conta')
- `versions` — arg_unmatched (security_impact=False, rationale='secret versions have no impact on the security of the resource or the data conta')
- `versions.version` — ambiguous (security_impact=False, rationale='The version of secrets have no impact on the security of the resource or the dat')

### `composer_environment`
- `encryption_config` — arg_unmatched (security_impact=True, rationale='Ensuring encryption_config.kms_key_name is set enforces the use of customer-mana')

### `contact_center_insights_analysis_rule`
- `annotator_selector` — arg_unmatched (security_impact=False, rationale='Defines analysis annotator configuration only. No direct security impact.')
- `annotator_selector.qa_config` — arg_unmatched (security_impact=False, rationale='Defines QA configuration only. No direct security impact.')
- `annotator_selector.summarization_config` — arg_unmatched (security_impact=False, rationale='Defines summarization configuration only. No direct security impact.')
- `qa_config` — arg_unmatched (security_impact=False, rationale='Defines QA configuration only. No direct security impact.')
- `qa_config.scorecard_list` — arg_unmatched (security_impact=False, rationale='Defines the scorecard list configuration only. No direct security impact.')
- `scorecard_list` — arg_unmatched (security_impact=False, rationale='Defines scorecard configuration only. No direct security impact.')
- `summarization_config` — arg_unmatched (security_impact=False, rationale='Defines summarization configuration only. No direct security impact.')

### `contact_center_insights_assessment_rule`
- `sample_rule` — arg_unmatched (security_impact=False, rationale='Sampling rules determine which conversations are evaluated by this assessment ru')
- `schedule_info` — arg_unmatched (security_impact=False, rationale='Schedule information is critical for managing the execution of the assessment ru')

### `contact_center_insights_auto_labeling_rule`
- `conditions` — arg_unmatched (security_impact=False, rationale='Conditions define the logic for when the auto-labeling rule should be applied. W')

### `contact_center_insights_qa_question`
- `answer_choices` — arg_unmatched (security_impact=False, rationale='Provides the list of valid answers for the question. No direct security impact.')
- `metrics` — arg_unmatched (security_impact=False, rationale='Represents metrics calculated against a test-set on a LLM. No direct security im')
- `metrics.accuracy` — arg_unmatched (security_impact=False, rationale='Provides the accuracy metric for the model. No direct security impact.')
- `predefined_question_config` — arg_unmatched (security_impact=False, rationale='Provides configuration for a predefined question. No direct security impact.')
- `qa_question_data_options` — arg_unmatched (security_impact=False, rationale='Provides options for configuring the data used to generate the QA question. No d')
- `qa_question_data_options.conversation_data_options` — arg_unmatched (security_impact=False, rationale='Provides options for configuring the conversation data used in QAI and Discovery')
- `tuning_metadata` — arg_unmatched (security_impact=False, rationale='Provides metadata about the tuning operation for the question. No direct securit')

### `container_attached_cluster`
- `authorization` — arg_unmatched (security_impact=True, rationale="Restricting cluster admin groups to verified corporate domains (e.g., '@company.")
- `binary_authorization` — arg_unmatched (security_impact=True, rationale='Enabling Binary Authorization enforcement ensures that only verified, signed ima')
- `component_config` — arg_unmatched (security_impact=False, rationale='Defines the scope of logging and monitoring components enabled in the cluster, e')
- `fleet` — arg_unmatched (security_impact=False, rationale='Specifying the correct Fleet host project ensures proper governance, access cont')
- `fleet.membership` — arg_unmatched (security_impact=False, rationale='Descriptions are metadata and do not directly impact security, though they shoul')
- `kubernetes_secret` — arg_unmatched (security_impact=False, rationale='Ensures sensitive proxy configuration data is stored securely within Kubernetes ')
- `kubernetes_secret.name` — ambiguous (security_impact=False, rationale='Correctly identifying the secret ensures the intended secure proxy configuration')
- `logging_config` — arg_unmatched (security_impact=False, rationale='Proper logging configuration ensures visibility into cluster activities. Without')
- `logging_config.component_config` — arg_unmatched (security_impact=False, rationale='Defining logging components ensures that critical system and workload logs are c')
- `managed_prometheus_config` — arg_unmatched (security_impact=False, rationale='Provides centralized and secure metrics collection for the cluster, improving ob')
- `monitoring_config` — arg_unmatched (security_impact=False, rationale='Ensures visibility into cluster performance and security events, enabling timely')
- `monitoring_config.managed_prometheus_config` — arg_unmatched (security_impact=False, rationale='Enables standardized, secure metrics collection to improve observability and det')
- `oidc_config` — arg_unmatched (security_impact=False, rationale='OIDC configuration governs how identities are validated for workloads interactin')
- `proxy_config` — arg_unmatched (security_impact=False, rationale='Enforces controlled network egress through approved proxies, reducing the risk o')
- `proxy_config.kubernetes_secret` — arg_unmatched (security_impact=False, rationale='Secures proxy configuration data using Kubernetes secrets, preventing exposure o')
- `security_posture_config` — arg_unmatched (security_impact=False, rationale='Provides workload vulnerability insights to identify and remediate security risk')

### `container_aws_cluster`
- `authorization` — arg_unmatched (security_impact=False, rationale='This block groups related settings. Security impact is assessed on specific chil')
- `authorization.admin_groups` — arg_unmatched (security_impact=False, rationale='This block groups related settings. Security impact is assessed on specific chil')
- `authorization.admin_users` — arg_unmatched (security_impact=False, rationale='This block groups related settings. Security impact is assessed on specific chil')
- `binary_authorization` — arg_unmatched (security_impact=False, rationale='This block groups related settings. Security impact is assessed on specific chil')
- `control_plane` — arg_unmatched (security_impact=False, rationale='This block groups related settings. Security impact is assessed on specific chil')
- `control_plane.aws_services_authentication` — arg_unmatched (security_impact=False, rationale='This block groups related settings. Security impact is assessed on specific chil')
- `control_plane.config_encryption` — arg_unmatched (security_impact=False, rationale='This block groups related settings. Security impact is assessed on specific chil')
- `control_plane.database_encryption` — arg_unmatched (security_impact=False, rationale='This block groups related settings. Security impact is assessed on specific chil')
- `control_plane.instance_placement` — arg_unmatched (security_impact=False, rationale='This block groups related settings. Security impact is assessed on specific chil')
- `control_plane.instance_placement.tenancy` — arg_unmatched (security_impact=False, rationale='Tenancy affects instance placement but does not directly grant access or permiss')
- `control_plane.main_volume` — arg_unmatched (security_impact=False, rationale='This block groups related settings. Security impact is assessed on specific chil')
- `control_plane.proxy_config` — arg_unmatched (security_impact=False, rationale='This block groups related settings. Security impact is assessed on specific chil')
- `control_plane.root_volume` — arg_unmatched (security_impact=False, rationale='This block groups related settings. Security impact is assessed on specific chil')
- `control_plane.ssh_config` — arg_unmatched (security_impact=False, rationale='This block groups related settings. Security impact is assessed on specific chil')
- `fleet` — arg_unmatched (security_impact=False, rationale='This block groups related settings. Security impact is assessed on specific chil')
- `fleet.membership` — arg_unmatched (security_impact=False, rationale='Membership identifies the Fleet association for the cluster but does not directl')
- `logging_config` — arg_unmatched (security_impact=False, rationale='This block groups related settings. Security impact is assessed on specific chil')
- `logging_config.component_config` — arg_unmatched (security_impact=False, rationale='This block groups related settings. Security impact is assessed on specific chil')
- `logging_config.component_config.enable_components` — arg_unmatched (security_impact=True, rationale='Enabled logging components determine which cluster logs are collected and should')
- `networking` — arg_unmatched (security_impact=False, rationale='This block groups related settings. Security impact is assessed on specific chil')

### `container_aws_node_pool`
- `autoscaling` — arg_unmatched (security_impact=False, rationale='This block groups related settings. Security impact is assessed on specific chil')
- `config` — arg_unmatched (security_impact=False, rationale='This block groups related settings. Security impact is assessed on specific chil')
- `config.autoscaling_metrics_collection` — arg_unmatched (security_impact=False, rationale='CloudWatch metrics collection supports observability of the auto scaling group b')
- `config.config_encryption` — arg_unmatched (security_impact=False, rationale='This block groups related settings. Security impact is assessed on specific chil')
- `config.image_type` — arg_unmatched (security_impact=False, rationale='This beta-only field is not assessed in the current policy scope. Node security ')
- `config.instance_placement` — arg_unmatched (security_impact=False, rationale='Instance placement controls tenancy and physical placement but does not directly')
- `config.proxy_config` — arg_unmatched (security_impact=False, rationale='This block groups related settings. Security impact is assessed on specific chil')
- `config.root_volume` — arg_unmatched (security_impact=False, rationale='This block groups related settings. Security impact is assessed on specific chil')
- `config.spot_config` — arg_unmatched (security_impact=False, rationale='Spot instance configuration affects availability and cost characteristics but do')
- `config.ssh_config` — arg_unmatched (security_impact=False, rationale='This block groups related settings. Security impact is assessed on specific chil')
- `config.taints` — arg_unmatched (security_impact=False, rationale='Taints affect workload scheduling but do not directly control access, encryption')
- `kubelet_config` — arg_unmatched (security_impact=False, rationale='This block groups related settings. Security impact is assessed on specific chil')
- `management` — arg_unmatched (security_impact=False, rationale='This block groups related settings. Security impact is assessed on specific chil')
- `max_pods_constraint` — arg_unmatched (security_impact=False, rationale='This block groups related settings. Security impact is assessed on specific chil')
- `update_settings` — arg_unmatched (security_impact=False, rationale='This block groups related settings. Security impact is assessed on specific chil')
- `update_settings.surge_settings` — arg_unmatched (security_impact=False, rationale='This block groups related settings. Security impact is assessed on specific chil')

### `data_catalog_entry`
- `gcs_fileset_spec` — arg_unmatched (security_impact=False, rationale='This block describes GCS fileset metadata and does not control access to the und')
- `gcs_fileset_spec.file_path` — arg_unmatched (security_impact=False, rationale='This is output or metadata information from the provider and is not a configurab')
- `gcs_fileset_spec.sample_gcs_file_specs` — arg_unmatched (security_impact=False, rationale='This is output or metadata information from the provider and is not a configurab')
- `gcs_fileset_spec.size_bytes` — arg_unmatched (security_impact=False, rationale='This is output or metadata information from the provider and is not a configurab')

### `data_catalog_tag`
- `fields` — arg_unmatched (security_impact=False, rationale='This block defines metadata fields and values for cataloging, but it does not di')
- `fields.display_name` — arg_unmatched (security_impact=False, rationale='This is only a human-readable display name and does not control access to the re')
- `fields.order` — arg_unmatched (security_impact=False, rationale='This controls display or ordering behavior and does not affect resource security')

### `data_catalog_tag_template`
- `allowed_values` — arg_unmatched (security_impact=False, rationale='This defines the type or allowed metadata values for a tag template field and do')
- `allowed_values.display_name` — ambiguous (security_impact=False, rationale='This is only a human-readable display name and does not control access to the re')
- `enum_type` — arg_unmatched (security_impact=False, rationale='This defines the type or allowed metadata values for a tag template field and do')
- `enum_type.allowed_values` — arg_unmatched (security_impact=False, rationale='This defines the type or allowed metadata values for a tag template field and do')
- `fields` — arg_unmatched (security_impact=False, rationale='This block defines metadata fields and values for cataloging, but it does not di')
- `fields.name` — arg_unmatched (security_impact=False, rationale='This is a user-defined identifier and does not control access to the resource or')
- `fields.type` — arg_unmatched (security_impact=False, rationale='This argument is not security-related because it does not control access, encryp')
- `type` — arg_unmatched (security_impact=False, rationale='This argument is not security-related because it does not control access, encryp')
- `type.enum_type` — arg_unmatched (security_impact=False, rationale='This defines the type or allowed metadata values for a tag template field and do')

### `data_fusion_instance`
- `accelerators` — arg_unmatched (security_impact=False, rationale='Maintain a standardized cost-effective environment for general purpose workloads')
- `crypto_key_config` — arg_unmatched (security_impact=True, rationale='To ensure that data at rest is protected by Customer-Managed Encryption Keys (CM')
- `event_publish_config` — arg_unmatched (security_impact=True, rationale='enable real-time observability and automation ensuring that lifecycle events fro')
- `network_config` — arg_unmatched (security_impact=True, rationale='Strictly control traffic flow and peering which prevents unauthorized network ex')
- `network_config.private_service_connect_config` — arg_unmatched (security_impact=False, rationale='to prioritize VPC peering as the standardized connectivity method voiding the ad')
- `private_service_connect_config` — arg_unmatched (security_impact=False, rationale='To maintain a simplified network architecture centered on VPC Peering')

### `data_pipeline_pipeline`
- `dataflow_flex_template_request.launch_parameter` — arg_unmatched (security_impact=False, rationale='the launch_parameter field is not a security-impact field because it only specif')
- `dataflow_flex_template_request.location` — ambiguous (security_impact=False, rationale='the location field is not a security-impact field because it only specifies the ')
- `dataflow_flex_template_request.project_id` — ambiguous (security_impact=False, rationale='the project_id field is not a security-impact field because it only specifies th')
- `dataflow_flex_template_request.validate_only` — ambiguous (security_impact=False, rationale='the validate_only field is not a security-impact field because it only specifies')
- `dataflow_launch_template_request` — arg_unmatched (security_impact=False, rationale='the dataflow launch template request is not a security-impact field because it i')
- `dataflow_launch_template_request.launch_parameters` — arg_unmatched (security_impact=False, rationale='the launch parameters field is not a security-impact field because it is used fo')
- `dataflow_launch_template_request.location` — ambiguous (security_impact=False, rationale='the location field is not a security-impact field because it is used for routing')
- `dataflow_launch_template_request.project_id` — ambiguous (security_impact=False, rationale='the project ID is not a security-impact field because it is used for identificat')
- `dataflow_launch_template_request.validate_only` — ambiguous (security_impact=False, rationale='the validate only field is not a security-impact field because it is used for te')
- `environment.additional_experiments` — ambiguous (security_impact=False, rationale='the additional_experiments field is not a security-impact field because it only ')
- `environment.additional_user_labels` — ambiguous (security_impact=False, rationale='the additional_user_labels field is not a security-impact field because it only ')
- `environment.enable_streaming_engine` — ambiguous (security_impact=False, rationale='the enable_streaming_engine field is not a security-impact field because it only')
- `environment.ip_configuration` — ambiguous (security_impact=True, rationale='ip_configuration is a security_impact as configuring worker VMs with public IP a')
- `environment.kms_key_name` — ambiguous (security_impact=True, rationale='kms_key_name is a security_impact field because it controls the customer-managed')
- `environment.machine_type` — ambiguous (security_impact=False, rationale='the machine_type field is not a security-impact field because it only specifies ')
- `environment.max_workers` — ambiguous (security_impact=True, rationale='the max_workers field is a security-impact field because it determines the maxim')
- `environment.network` — ambiguous (security_impact=False, rationale='the network field is not a security-impact field because it only specifies the n')
- `environment.num_workers` — ambiguous (security_impact=True, rationale='the num_workers field is a security-impact field because it determines the numbe')
- `environment.service_account_email` — ambiguous (security_impact=True, rationale='the service_account_email field is a security-impact field because it determines')
- `environment.subnetwork` — ambiguous (security_impact=False, rationale='the subnetwork field is not a security-impact field because it only specifies th')
- `environment.temp_location` — ambiguous (security_impact=False, rationale='the temp_location field is not a security-impact field because it only specifies')
- `environment.worker_region` — ambiguous (security_impact=True, rationale='worker_region is a security_impact as explicitly defining the worker processing ')
- `environment.worker_zone` — ambiguous (security_impact=True, rationale='worker_zone is a security_impact as explicitly defining the worker processing zo')
- `environment.zone` — ambiguous (security_impact=True, rationale='The zone field is security-impacting because it determines the physical location')
- `launch_parameter` — arg_unmatched (security_impact=False, rationale='the launch_parameter field is not a security-impact field because it only specif')
- `launch_parameter.environment` — arg_unmatched (security_impact=False, rationale='the environment field is not a security-impact field because it only specifies t')
- `launch_parameter.job_name` — ambiguous (security_impact=False, rationale='the job_name field is not a security-impact field because it only specifies the ')
- `launch_parameter.parameters` — ambiguous (security_impact=False, rationale='the parameters field is not a security-impact field because it only specifies th')
- `launch_parameter.update` — ambiguous (security_impact=False, rationale='the update field is not a security-impact field because it only specifies whethe')
- `launch_parameters` — arg_unmatched (security_impact=False, rationale='launch Parameters is not a security_impact as c it only controls runtime executi')
- `launch_parameters.environment` — arg_unmatched (security_impact=False, rationale='the environment field is not a security-impact field because it is used for conf')
- `launch_parameters.job_name` — ambiguous (security_impact=False, rationale='the job name field is not a security-impact field because it is used for identif')
- `launch_parameters.update` — ambiguous (security_impact=False, rationale='the update field is not a security-impact field because it is used for updating ')
- `schedule_info` — arg_unmatched (security_impact=False, rationale='the schedule information is not a security-impact field because it is used for j')
- `schedule_info.next_job_time` — arg_unmatched (security_impact=False, rationale='the next job time is not a security-impact field because it is used for informat')
- `workload` — arg_unmatched (security_impact=False, rationale='the workload information is not a security-impact field because it is used for j')
- `workload.dataflow_flex_template_request` — arg_unmatched (security_impact=False, rationale='the dataflow flex template request is not a security-impact field because it is ')
- `workload.dataflow_launch_template_request` — arg_unmatched (security_impact=False, rationale='the dataflow launch template request is not a security-impact field because it i')

### `database_migration_service_connection_profile`
- `alloydb` — arg_unmatched (security_impact=False, rationale='Not Security Related')
- `alloydb.settings` — arg_unmatched (security_impact=False, rationale='Not Security Related')
- `authorized_networks` — arg_unmatched (security_impact=True, rationale='Authorized networks define which external IPs can access the database. Better no')
- `cloudsql` — arg_unmatched (security_impact=True, rationale='Cloud SQL instances must enforce private networking and SSL to prevent exposure.')
- `cloudsql.cloud_sql_id` — ambiguous (security_impact=False, rationale='This is the output')
- `cloudsql.private_ip` — arg_unmatched (security_impact=False, rationale='This is the output')
- `cloudsql.public_ip` — arg_unmatched (security_impact=False, rationale='This is the output')
- `cloudsql.settings` — arg_unmatched (security_impact=True, rationale='Configure SSL and private network')
- `forward_ssh_connectivity` — arg_unmatched (security_impact=True, rationale='Forward SSH introduces risk of misconfigurations and exposure. Configure private')
- `initial_user` — arg_unmatched (security_impact=False, rationale='Not Security Related')
- `ip_config` — arg_unmatched (security_impact=True, rationale='Enable SSL and Private connection')
- `ip_config.authorized_networks` — arg_unmatched (security_impact=True, rationale='Authorized networks define which external IPs can access the database. Better no')
- `machine_config` — arg_unmatched (security_impact=False, rationale='Not Security Related')
- `mysql` — arg_unmatched (security_impact=True, rationale='MySQL profiles may expose sensitive database connection details. Misconfigured h')
- `mysql.password_set` — arg_unmatched (security_impact=False, rationale='This is the output')
- `mysql.ssl` — arg_unmatched (security_impact=True, rationale='SSL ensures data in transit is encrypted, preventing eavesdropping or man-in-the')
- `oracle` — arg_unmatched (security_impact=True)
- `oracle.forward_ssh_connectivity` — arg_unmatched (security_impact=True, rationale='Forward SSH introduces risk of misconfigurations and exposure. Use private conne')
- `oracle.password_set` — arg_unmatched (security_impact=False, rationale='This is the output')
- `oracle.private_connectivity` — arg_unmatched (security_impact=True, rationale='Private connectivity prevents external access over public networks.')
- `oracle.ssl` — arg_unmatched (security_impact=True, rationale='SSL protects sensitive Oracle traffic from interception.')
- `oracle.static_service_ip_connectivity` — arg_unmatched (security_impact=True, rationale='Static service IP connectivity exposes the Oracle database over a fixed public I')
- `postgresql` — arg_unmatched (security_impact=True)
- `postgresql.network_architecture` — arg_unmatched (security_impact=False, rationale='This is the output')
- `postgresql.password_set` — arg_unmatched (security_impact=False, rationale='This is the output')
- `postgresql.ssl` — arg_unmatched (security_impact=True, rationale='SSL is required for encryption of data in transit.')
- `primary_instance_settings` — arg_unmatched (security_impact=False, rationale='Not Security Related')
- `private_connectivity` — arg_unmatched (security_impact=True, rationale='Private connectivity prevents external access over public networks.')
- `private_connectivity.private_connection` — ambiguous (security_impact=True, rationale='Private connectivity prevents external access over public networks.')
- `settings` — arg_unmatched (security_impact=True, rationale='Configure SSL and private network')
- `settings.database_flags` — ambiguous (security_impact=False, rationale='Not Security Related')
- `settings.initial_user` — arg_unmatched (security_impact=False, rationale='Not Security Related')
- `settings.ip_config` — arg_unmatched (security_impact=True, rationale='Enable SSL and Private connection')
- `settings.labels` — ambiguous (security_impact=False, rationale='Not Security Related')
- `settings.primary_instance_settings` — arg_unmatched (security_impact=False, rationale='Not Security Related')
- `settings.root_password_set` — arg_unmatched (security_impact=False, rationale='This is the output')
- `ssl` — arg_unmatched (security_impact=True, rationale='The type of SSL used must be described')
- `ssl.ca_certificate` — ambiguous (security_impact=False, rationale='Depends on SSL Type')
- `ssl.client_certificate` — ambiguous (security_impact=False, rationale='Depends on SSL Type')
- `ssl.client_key` — ambiguous (security_impact=False, rationale='Depends on SSL Type')
- `ssl.type` — ambiguous (security_impact=True, rationale='The SSL type determines the level of encryption and whether client certificates ')

### `database_migration_service_migration_job`
- `dump_flags` — arg_unmatched (security_impact=False, rationale='Not Security Related')
- `performance_config` — arg_unmatched (security_impact=False, rationale='Not Security Related')
- `reverse_ssh_connectivity` — arg_unmatched (security_impact=True, rationale='Reverse SSH provides secure tunneling but must be correctly configured to avoid ')
- `static_ip_connectivity` — arg_unmatched (security_impact=True, rationale='Static IP connectivity exposes migration traffic over the public internet. Must ')
- `vpc_peering_connectivity` — arg_unmatched (security_impact=True, rationale='VPC peering is the preferred secure method to connect source and destination ove')

### `database_migration_service_private_connection`
- `vpc_peering_config` — arg_unmatched (security_impact=False, rationale='This is a required field and doesnot need a Rego policy')

### `dataflow_job`
- `effective_labels` — arg_unmatched (security_impact=False, rationale='All of labels (key/value pairs) present on the resource in GCP, including the la')
- `terraform_labels` — arg_unmatched (security_impact=False, rationale='The combination of labels configured directly on the resource and default labels')

### `deployment_manager_deployment`
- `create_policy` — nonbool_string (security_impact='high')
- `delete_policy` — nonbool_string (security_impact='high')
- `description` — nonbool_string (security_impact='low')
- `name` — nonbool_string (security_impact='low')
- `preview` — nonbool_string (security_impact='medium')
- `target` — arg_unmatched (security_impact='high', rationale='Configuration and templates determine which resources are created and with what ')
- `target` — nonbool_string (security_impact='high')

### `discovery_engine_chat_engine`
- `agent_creation_config` — arg_unmatched (security_impact=False, rationale='This happens in the backgroud')
- `chat_engine_config` — arg_unmatched (security_impact=True, rationale='The internal configurations of this block can change, though some are security r')
- `chat_engine_config.agent_creation_config` — arg_unmatched (security_impact=True, rationale='linking to other agents can be a risk')
- `common_config` — arg_unmatched (security_impact=False, rationale='Basic additional support for LLM that is optional and not that important')

### `discovery_engine_cmek_config`
- `single_region_keys` — arg_unmatched (security_impact=True, rationale='region keys for encryption')

### `discovery_engine_control`
- `display_name ` — arg_unmatched (security_impact=False, rationale='Its the name')

### `discovery_engine_data_store`
- `advanced_site_search_config` — arg_unmatched (security_impact=False, rationale='Logistic issue')
- `document_processing_config` — arg_unmatched (security_impact=True, rationale='Use Native Text can make private info public if not set up correctly')
- `ocr_parsing_config.use_native_text` — ambiguous (security_impact=True, rationale='can make private info public if set to false')

### `discovery_engine_license_config`
- `display_name` — arg_unmatched (security_impact=False, rationale='Its the name')
- `end_date` — arg_unmatched (security_impact=True, rationale='Its the end date before you lose the licence and break TOS or lose acess. Write ')
- `start_date` — arg_unmatched (security_impact=True, rationale='It affects when you can start working. Write a policy.')

### `discovery_engine_search_engine`
- `common_config` — arg_unmatched (security_impact=False, rationale='logistics')
- `search_engine_config` — arg_unmatched (security_impact=False, rationale='Logistics')

### `edgecontainer_cluster`
- `admin_users` — arg_unmatched (security_impact=True, rationale='Admin users should be properly authorized individuals with valid organizational ')
- `authorization` — arg_unmatched (security_impact=True, rationale='Proper RBAC configuration ensures only authorized users have administrative acce')
- `authorization.admin_users` — arg_unmatched (security_impact=True, rationale='Admin users should be properly vetted and authorized individuals with valid orga')
- `control_plane` — arg_unmatched (security_impact=True, rationale='Control plane configuration impacts cluster stability, performance, and security')
- `control_plane.local` — arg_unmatched (security_impact=True, rationale='Local control plane configuration impacts high availability, resource isolation,')
- `control_plane.remote` — arg_unmatched (security_impact=True, rationale='Remote control plane location impacts latency, availability, and data residency.')
- `control_plane_encryption` — arg_unmatched (security_impact=True, rationale='Control plane encryption protects sensitive cluster data at rest and should use ')
- `control_plane_encryption.kms_key_active_version` — arg_unmatched (security_impact=False, rationale='Output-only field indicating current key version, no security impact.')
- `control_plane_encryption.kms_key_state` — arg_unmatched (security_impact=False, rationale='Output-only field indicating key status, no security impact.')
- `control_plane_encryption.kms_status` — arg_unmatched (security_impact=False, rationale='Output-only field for KMS error status, no security impact.')
- `fleet` — arg_unmatched (security_impact=True, rationale='Proper fleet configuration ensures clusters are organized in a secure and manage')
- `fleet.membership` — arg_unmatched (security_impact=False, rationale='Output-only field used for tracking cluster membership, no security impact.')
- `ingress` — arg_unmatched (security_impact=True, rationale='Ingress configuration controls external access to cluster services and significa')
- `local` — arg_unmatched (security_impact=True, rationale='Local control plane configuration impacts high availability, resource isolation,')
- `local.node_location` — ambiguous (security_impact=True, rationale='Local node location should be in approved zones that meet security and complianc')
- `maintenance_exclusions` — arg_unmatched (security_impact=True, rationale='Maintenance exclusions prevent updates during critical business periods but shou')
- `maintenance_exclusions.window` — arg_unmatched (security_impact=True, rationale='Exclusion windows should be properly defined to prevent maintenance during criti')
- `maintenance_policy` — arg_unmatched (security_impact=True, rationale='Proper maintenance windows ensure cluster updates occur during approved times wi')
- `maintenance_policy.maintenance_exclusions` — arg_unmatched (security_impact=True, rationale='Maintenance exclusions prevent updates during critical business periods but shou')
- `maintenance_policy.window` — arg_unmatched (security_impact=True, rationale='Maintenance windows should be scheduled during low-usage periods with proper rec')
- `maintenance_policy.window.recurring_window` — arg_unmatched (security_impact=True, rationale='Recurring windows provide predictable maintenance schedules that can be planned ')
- `networking` — arg_unmatched (security_impact=True, rationale='Proper network segmentation prevents unauthorized access and contains potential ')
- `networking.network_type` — arg_unmatched (security_impact=False, rationale='Output-only field indicating network configuration type, no security impact.')
- `recurring_window` — arg_unmatched (security_impact=True, rationale='Proper recurring window configuration ensures maintenance occurs predictably and')
- `recurring_window.window` — arg_unmatched (security_impact=True, rationale='Window timing should align with business low-usage periods to minimize impact.')
- `remote` — arg_unmatched (security_impact=True, rationale='Remote control plane configuration impacts where control plane components run an')
- `remote.node_location` — ambiguous (security_impact=True, rationale='Remote node location should be in approved zones that meet security and complian')
- `system_addons_config` — arg_unmatched (security_impact=True, rationale='System addons configuration impacts cluster functionality and security posture.')
- `system_addons_config.ingress` — arg_unmatched (security_impact=True, rationale='Ingress configuration controls external access to cluster services and should be')
- `window` — arg_unmatched (security_impact=True, rationale='Proper maintenance windows ensure updates occur during approved times with minim')
- `window.recurring_window` — arg_unmatched (security_impact=True, rationale='Recurring windows provide predictable maintenance schedules that can be planned ')

### `edgecontainer_node_pool`
- `local_disk_encryption` — arg_unmatched (security_impact=True, rationale='Local disk encryption protects sensitive data at rest on node storage and should')
- `local_disk_encryption.kms_key_active_version` — arg_unmatched (security_impact=False, rationale='Output-only field indicating current key version, no security impact.')
- `local_disk_encryption.kms_key_state` — arg_unmatched (security_impact=False, rationale='Output-only field indicating key status, no security impact.')
- `node_config` — arg_unmatched (security_impact=True, rationale='Node configuration impacts security posture, resource allocation, and isolation ')

### `edgecontainer_vpn_connection`
- `vpc_project` — arg_unmatched (security_impact=True, rationale='Cross-project VPC connections require proper authorization and security controls')

### `firebase_app_hosting_backend`
- `codebase` — arg_unmatched (security_impact=True, rationale='Codebase repository must use the proper GCP Developer Connect format to ensure s')

### `firebase_app_hosting_build`
- `codebase` — arg_unmatched (security_impact=False, rationale='Codebase fields are output-only or have no specific security policies.')
- `codebase.author` — arg_unmatched (security_impact=False, rationale='Output field with no security policy.')
- `codebase.commit_message` — arg_unmatched (security_impact=False, rationale='Output field with no security policy.')
- `codebase.commit_time` — arg_unmatched (security_impact=False, rationale='Output field with no security policy.')
- `codebase.email` — arg_unmatched (security_impact=False, rationale='Output field with no security policy.')
- `codebase.hash` — arg_unmatched (security_impact=False, rationale='Output field with no security policy.')
- `codebase.image_uri` — arg_unmatched (security_impact=False, rationale='Output field with no security policy.')
- `codebase.uri` — arg_unmatched (security_impact=False, rationale='Output field with no security policy.')
- `container` — arg_unmatched (security_impact=True, rationale='Container configuration must specify approved image sources for security complia')
- `source` — arg_unmatched (security_impact=True, rationale='Build source must use approved container registries to ensure secure and trusted')
- `source.codebase` — arg_unmatched (security_impact=False, rationale='Codebase source has no specific security policy in place.')
- `source.container` — arg_unmatched (security_impact=True, rationale='Container images must be sourced from approved registries to ensure security and')

### `firebase_app_hosting_domain`
- `redirect` — arg_unmatched (security_impact=False, rationale='Redirect configuration has no specific security policy.')
- `serve` — arg_unmatched (security_impact=False, rationale='Domain serving behavior has no specific security policy.')
- `serve.redirect` — arg_unmatched (security_impact=False, rationale='Domain redirect configuration has no specific security policy.')

### `firebase_app_hosting_traffic`
- `rollout_policy` — arg_unmatched (security_impact=True, rationale='Rollout policy must specify approved source branches to ensure only production-r')
- `rollout_policy.disabled_time` — arg_unmatched (security_impact=False, rationale='Output field with no security policy.')
- `splits` — arg_unmatched (security_impact=False, rationale='Traffic splits configuration has no specific security policy.')
- `target` — arg_unmatched (security_impact=False, rationale='Manual traffic targeting has no specific security policy.')
- `target.splits` — arg_unmatched (security_impact=False, rationale='Traffic splits configuration has no specific security policy.')

### `folder_iam`
- `audit_log_config` — arg_unmatched (security_impact=True, rationale='Audit logs are critical for detecting misuse and maintaining compliance.')
- `audit_log_config.exempted_members` — arg_unmatched (security_impact=True, rationale='Members being excluded from records weakens control and creates blind spots.')
- `audit_log_config.log_type` — arg_unmatched (security_impact=True, rationale='Ensures important access events are captured in logs.')
- `condition` — arg_unmatched (security_impact=True, rationale='To reduce risk by applying time- or context-based constraints to access. Misconf')
- `service` — arg_unmatched (security_impact=True, rationale='Audit logging ensures accountability, whereas misconfiguration can conceal inten')

### `gemini_code_tools_setting`
- `enabled_tool` — arg_unmatched (security_impact=False, rationale='Enabling tools affects functionality but not security.')
- `enabled_tool.config` — arg_unmatched (security_impact=False, rationale='Not security-related.')

### `gemini_repository_group`
- `repositories` — arg_unmatched (security_impact=False, rationale='Not security-related.')

### `gke_backup_backup_plan`
- `backup_config` — arg_unmatched (security_impact=True, rationale='Backup configuration must explicitly define secret handling and encryption.')

### `gkeonprem_bare_metal_admin_cluster`
- `api_server_args.value` — ambiguous (security_impact=False, rationale='value does not have security impact on the service.')
- `authorization.admin_users` — arg_unmatched (security_impact=False, rationale='admin_users itself does not have a security impact.')
- `cluster_operations` — arg_unmatched (security_impact=False, rationale='Cluster operations do not have security impact on the service.')
- `control_plane` — arg_unmatched (security_impact=False, rationale='Control plane itself does not require specific security considerations.')
- `control_plane.api_server_args` — arg_unmatched (security_impact=False, rationale='api_server_args does not have security impact on the service.')
- `control_plane.control_plane_node_pool_config` — arg_unmatched (security_impact=False, rationale='Control plane node pool itself does not require specific security considerations')
- `control_plane_node_pool_config.node_pool_config` — arg_unmatched (security_impact=False, rationale='Node pool configuration itself does not require specific security considerations')
- `load_balancer` — arg_unmatched (security_impact=False, rationale='Load balancer itself does not require specific security considerations.')
- `load_balancer.manual_lb_config` — arg_unmatched (security_impact=False, rationale='manual_lb_config does not require specific security considerations.')
- `load_balancer.port_config` — arg_unmatched (security_impact=False, rationale='port_config itself does not require specific security considerations.')
- `load_balancer.vip_config` — arg_unmatched (security_impact=False, rationale='VIP config itself does not require specific security considerations.')
- `lvp_config.path` — ambiguous (security_impact=False, rationale='path is not important enough to have a security policy.')
- `lvp_config.storage_class` — ambiguous (security_impact=False, rationale='storage_class is not important enough to have a security policy.')
- `lvp_node_mounts_config.path` — ambiguous (security_impact=False, rationale='path is not important enough to have a security policy.')
- `lvp_node_mounts_config.storage_class` — ambiguous (security_impact=False, rationale='storage_class is not important enough to have a security policy.')
- `lvp_share_config.lvp_config` — arg_unmatched (security_impact=False, rationale='lvp_config is not important enough to have a security policy.')
- `maintenance_config` — arg_unmatched (security_impact=False, rationale='Maintenance configuration is not important enough to have security implications.')
- `manual_lb_config.enabled` — ambiguous (security_impact=False, rationale='enabled does not have security impact on the service.')
- `network_config` — arg_unmatched (security_impact=False, rationale='network_config itself does not need security considerations, but its nested fiel')
- `network_config.island_mode_cidr` — arg_unmatched (security_impact=False, rationale='island_mode_cidr is not important enough to require specific security considerat')
- `node_access_config` — arg_unmatched (security_impact=False, rationale='Node access configuration is not important enough to have security implications.')
- `node_config` — arg_unmatched (security_impact=False, rationale='Node configuration is not important enough to have security implications.')
- `node_configs.labels` — ambiguous (security_impact=False, rationale='labels does not have security impact on the service.')
- `node_configs.node_ip` — ambiguous (security_impact=True, rationale='Node IPs are critical for security and must be carefully managed.')
- `node_pool_config.labels` — ambiguous (security_impact=False, rationale='labels does not have security impact on the service.')
- `node_pool_config.node_configs` — arg_unmatched (security_impact=False, rationale='node configs itself is not a security concern.')
- `node_pool_config.operating_system` — ambiguous (security_impact=True, rationale='Operating system selection affects the security posture of nodes.')
- `node_pool_config.taints` — arg_unmatched (security_impact=False, rationale='taints does not have security impact on the service.')
- `proxy` — arg_unmatched (security_impact=False, rationale='Proxy configuration does not require specific security considerations.')
- `security_config` — arg_unmatched (security_impact=False, rationale='Security configuration itself does not require specific security considerations.')
- `security_config.authorization` — arg_unmatched (security_impact=False, rationale='Authorization configuration itself does not require specific security considerat')
- `storage` — arg_unmatched (security_impact=False, rationale='Storage does not have security impact on the service.')
- `storage.lvp_node_mounts_config` — arg_unmatched (security_impact=False, rationale='lvp_node_mounts_config does not have security impact on the service.')
- `storage.lvp_share_config` — arg_unmatched (security_impact=False, rationale='lvp_share_config does not have security impact on the service.')
- `taints` — arg_unmatched (security_impact=False, rationale='taints does not have security impact on the service.')
- `taints.effect` — ambiguous (security_impact=False, rationale='effect does not have security impact on the service.')
- `taints.key` — ambiguous (security_impact=False, rationale='key is not important enough to have security implications.')
- `taints.value` — ambiguous (security_impact=False, rationale='value is not important enough to have security implications.')

### `gkeonprem_bare_metal_cluster`
- `address_pools.addresses` — ambiguous (security_impact=False, rationale='addresses are not security critical.')
- `address_pools.avoid_buggy_ips` — ambiguous (security_impact=False, rationale='avoid_buggy_ips is not security critical.')
- `address_pools.manual_assign` — ambiguous (security_impact=False, rationale='manual_assign is not security critical.')
- `address_pools.pool` — ambiguous (security_impact=False, rationale='address pools are not security critical.')
- `api_server_args.value` — ambiguous (security_impact=False, rationale='API server argument values are not security critical.')
- `authorization.admin_users` — arg_unmatched (security_impact=True, rationale='Admin users have full access to the cluster and should be protected.')
- `bgp_lb_config.address_pools` — arg_unmatched (security_impact=False, rationale='Address pools are not security critical.')
- `bgp_lb_config.asn` — ambiguous (security_impact=False, rationale='BGP ASN is not security critical.')
- `bgp_lb_config.bgp_peer_configs` — arg_unmatched (security_impact=False, rationale='BGP peer configs are not security critical.')
- `bgp_lb_config.load_balancer_node_pool_config` — arg_unmatched (security_impact=False, rationale='load balancer node pool config itself is not security critical.')
- `bgp_peer_configs.asn` — ambiguous (security_impact=False, rationale='BGP ASN is not security critical.')
- `binary_authorization` — arg_unmatched (security_impact=False, rationale='Binary authorization is not security critical.')
- `cluster_operations` — arg_unmatched (security_impact=False, rationale='Cluster operations are not security critical.')
- `control_plane` — arg_unmatched (security_impact=False, rationale='Control plane itself does not require specific security considerations.')
- `control_plane.api_server_args` — arg_unmatched (security_impact=False, rationale='api_server_args does not have security impact on the service.')
- `control_plane.control_plane_node_pool_config` — arg_unmatched (security_impact=False, rationale='Control plane node pool itself does not require specific security considerations')
- `control_plane_node_pool_config.node_pool_config` — arg_unmatched (security_impact=False, rationale='Control plane node pool configuration itself does not need policy.')
- `load_balancer` — arg_unmatched (security_impact=False, rationale='Load balancer itself does not require specific security considerations.')
- `load_balancer.bgp_lb_config` — arg_unmatched (security_impact=False, rationale='BGP LB config does not require specific security considerations.')
- `load_balancer.manual_lb_config` — arg_unmatched (security_impact=False, rationale='Manual LB config does not require specific security considerations.')
- `load_balancer.metal_lb_config` — arg_unmatched (security_impact=False, rationale='Metal LB config does not require specific security considerations.')
- `load_balancer.port_config` — arg_unmatched (security_impact=False, rationale='Load balancer port config itself does not require specific security consideratio')
- `load_balancer.vip_config` — arg_unmatched (security_impact=False, rationale='VIP config itself does not require specific security considerations.')
- `load_balancer_node_pool_config.node_pool_config` — arg_unmatched (security_impact=False, rationale='node pool config itself is not security critical.')
- `lvp_config.path` — ambiguous (security_impact=False, rationale='LVP config path is not security critical.')
- `lvp_config.storage_class` — ambiguous (security_impact=False, rationale='LVP config storage class is not security critical.')
- `lvp_node_mounts_config.path` — ambiguous (security_impact=False, rationale='LVP node mounts path is not security critical.')
- `lvp_node_mounts_config.storage_class` — ambiguous (security_impact=False, rationale='LVP node mounts storage class is not security critical.')
- `lvp_share_config.lvp_config` — arg_unmatched (security_impact=False, rationale='LVP share configuration is not security critical.')
- `maintenance_config` — arg_unmatched (security_impact=False, rationale='Maintenance config is not security critical.')
- `manual_lb_config.enabled` — ambiguous (security_impact=False, rationale='manual load balancing is not security critical.')
- `metal_lb_config.address_pools` — arg_unmatched (security_impact=False, rationale='address pools are not security critical.')
- `metal_lb_config.load_balancer_node_pool_config` — arg_unmatched (security_impact=False, rationale='load balancer node pool config is not security critical.')
- `multiple_network_interfaces_config.enabled` — ambiguous (security_impact=False, rationale='enabled is not security critical.')
- `network_config` — arg_unmatched (security_impact=False, rationale='network_config itself does not need security considerations, but its nested fiel')
- `network_config.island_mode_cidr` — arg_unmatched (security_impact=False, rationale='island_mode_cidr is not important enough to require specific security considerat')
- `network_config.multiple_network_interfaces_config` — arg_unmatched (security_impact=False, rationale='multiple_network_interfaces_config does not have a security impact.')
- `network_config.sr_iov_config` — arg_unmatched (security_impact=False, rationale='sr_iov_config does not have a security impact.')
- `node_access_config` — arg_unmatched (security_impact=False, rationale='Node access config is not security critical.')
- `node_config` — arg_unmatched (security_impact=False, rationale='Node config itself does not have a direct security impact.')
- `node_configs.labels` — ambiguous (security_impact=False, rationale='labels are not security critical.')
- `node_configs.node_ip` — ambiguous (security_impact=False, rationale='Node IP addresses are not important enough to be considered security critical.')
- `node_pool_config.kubelet_config` — arg_unmatched (security_impact=False, rationale='kubelet configurations are not security critical.')
- `node_pool_config.labels` — ambiguous (security_impact=False, rationale='labels are not security critical.')
- `node_pool_config.node_configs` — arg_unmatched (security_impact=False, rationale='Node configurations itself does not need policy.')
- `node_pool_config.operating_system` — ambiguous (security_impact=True, rationale='Outdated Operating system selection can impact security posture.')
- `node_pool_config.taints` — arg_unmatched (security_impact=False, rationale='taints is not security critical.')
- `os_environment_config` — arg_unmatched (security_impact=False, rationale='OS environment config is not security critical.')
- `proxy` — arg_unmatched (security_impact=False, rationale='Proxy is not security important enough to write a policy.')
- `security_config` — arg_unmatched (security_impact=False, rationale='Security config itself is not security critical.')
- `security_config.authorization` — arg_unmatched (security_impact=False, rationale='Authorization itself is not security critical.')
- `sr_iov_config.enabled` — ambiguous (security_impact=False, rationale='SR-IOV operator is not security critical.')
- `storage` — arg_unmatched (security_impact=False, rationale='Storage does not require specific security considerations.')
- `storage.lvp_node_mounts_config` — arg_unmatched (security_impact=False, rationale='LVP node mounts config does not require specific security considerations.')
- `storage.lvp_share_config` — arg_unmatched (security_impact=False, rationale='LVP share config does not require specific security considerations.')
- `taints.effect` — ambiguous (security_impact=False, rationale='taint effects are not security critical.')
- `taints.key` — ambiguous (security_impact=False, rationale='taint keys are not security critical.')
- `taints.value` — ambiguous (security_impact=False, rationale='taint values are not security critical.')
- `upgrade_policy` — arg_unmatched (security_impact=False, rationale='Upgrade policy is not security critical.')

### `gkeonprem_bare_metal_node_pool`
- `node_configs.labels` — ambiguous (security_impact=False, rationale='labels are not security related configurations.')
- `node_pool_config` — arg_unmatched (security_impact=False, rationale='Node pool configuration itself does not require specific security considerations')
- `node_pool_config.node_configs` — arg_unmatched (security_impact=False, rationale='Node configurations itself does not require specific security considerations.')
- `node_pool_config.taints` — arg_unmatched (security_impact=False, rationale='taints are not security related configurations.')

### `gkeonprem_vmware_admin_cluster`
- `addon_node` — arg_unmatched (security_impact=False, rationale='addon_node does not require security considerations')
- `addon_node.auto_resize_config` — arg_unmatched (security_impact=False, rationale='auto_resize_config is not a security sensitive field')
- `anti_affinity_groups` — arg_unmatched (security_impact=False, rationale='anti_affinity_groups does not require security considerations')
- `authorization` — arg_unmatched (security_impact=False, rationale='authorization itself is not sensitive, but may contain sensitive data in its nes')
- `authorization.viewer_users` — arg_unmatched (security_impact=True, rationale='viewer_users contains sensitive user information and should be protected')
- `auto_repair_config` — arg_unmatched (security_impact=False, rationale='auto_repair_config does not require security considerations')
- `auto_resize_config.enabled` — ambiguous (security_impact=False, rationale='auto_resize_config does not require security considerations')
- `control_plane_ip_block.gateway` — ambiguous (security_impact=False, rationale='gateway is not security sensitive enough')
- `control_plane_ip_block.ips` — arg_unmatched (security_impact=False, rationale='ips is not security sensitive enough')
- `control_plane_ip_block.netmask` — ambiguous (security_impact=False, rationale='netmask is not security sensitive enough')
- `control_plane_node` — arg_unmatched (security_impact=False, rationale='control_plane_node does not require security considerations')
- `dhcp_ip_config.enabled` — ambiguous (security_impact=False, rationale='enabled is not a security sensitive field')
- `f5_config.address` — ambiguous (security_impact=False, rationale='address is not security sensitive enough')
- `ha_control_plane_config.control_plane_ip_block` — arg_unmatched (security_impact=False, rationale='control_plane_ip_block is not security sensitive enough')
- `ip_blocks.gateway` — ambiguous (security_impact=False, rationale='gateway is not security sensitive enough')
- `ip_blocks.ips` — arg_unmatched (security_impact=False, rationale='ips is not security sensitive enough')
- `ip_blocks.netmask` — ambiguous (security_impact=False, rationale='netmask is not security sensitive enough')
- `ips.hostname` — ambiguous (security_impact=False, rationale='hostname is not security sensitive enough')
- `ips.ip` — ambiguous (security_impact=False, rationale='ip is not security sensitive enough')
- `load_balancer` — arg_unmatched (security_impact=False, rationale='load_balancer itself is not sensitive, but may contain sensitive data in its nes')
- `load_balancer.f5_config` — arg_unmatched (security_impact=False, rationale='f5_config is not a security sensitive field')
- `load_balancer.manual_lb_config` — arg_unmatched (security_impact=False, rationale='manual_lb_config is not a security sensitive field')
- `load_balancer.metal_lb_config` — arg_unmatched (security_impact=False, rationale='metal_lb_config is not a security sensitive field')
- `load_balancer.vip_config` — arg_unmatched (security_impact=False, rationale='vip_config itself is not sensitive, but may contain sensitive data in its nested')
- `metal_lb_config.enabled` — ambiguous (security_impact=False, rationale='Metal LB does not require security considerations')
- `network_config` — arg_unmatched (security_impact=False, rationale='network config itself is not sensitive, but may contain sensitive data in its ne')
- `network_config.dhcp_ip_config` — arg_unmatched (security_impact=False, rationale='dhcp ip config does not require security considerations')
- `network_config.ha_control_plane_config` — arg_unmatched (security_impact=False, rationale='ha control plane config does not require security considerations')
- `network_config.host_config` — arg_unmatched (security_impact=False, rationale='host config is not security sensitive enough')
- `network_config.static_ip_config` — arg_unmatched (security_impact=False, rationale='static ip config is not security sensitive enough')
- `platform_config` — arg_unmatched (security_impact=False, rationale='platform config itself is not sensitive, but may contain sensitive data in its n')
- `platform_config.bundles` — arg_unmatched (security_impact=False, rationale='bundles is only an output field and does not require security considerations')
- `platform_config.conditions` — arg_unmatched (security_impact=False, rationale='conditions is only an output field and does not require security considerations')
- `platform_config.error_message` — arg_unmatched (security_impact=False, rationale='error_message is only an output field and does not require security consideratio')
- `platform_config.last_transition_time` — arg_unmatched (security_impact=False, rationale='last_transition_time is only an output field and does not require security consi')
- `platform_config.message` — arg_unmatched (security_impact=False, rationale='message is only an output field and does not require security considerations')
- `platform_config.platform_version` — arg_unmatched (security_impact=False, rationale='platform version is only an output field and does not require security considera')
- `platform_config.reason` — arg_unmatched (security_impact=False, rationale='reason is only an output field and does not require security considerations')
- `platform_config.state` — arg_unmatched (security_impact=False, rationale='state is only an output field and does not require security considerations')
- `platform_config.status` — arg_unmatched (security_impact=False, rationale='status is only an output field and does not require security considerations')
- `platform_config.type` — arg_unmatched (security_impact=False, rationale='type is only an output field and does not require security considerations')
- `platform_config.version` — arg_unmatched (security_impact=False, rationale='version is only an output field and does not require security considerations')
- `private_registry_config` — arg_unmatched (security_impact=False, rationale='private registry config does not require security considerations')
- `static_ip_config.ip_blocks` — arg_unmatched (security_impact=False, rationale='static_ip_config is not security sensitive enough')
- `vcenter` — arg_unmatched (security_impact=False, rationale='vcenter is not sensitive information')

### `gkeonprem_vmware_cluster`
- `anti_affinity_groups` — arg_unmatched (security_impact=False, rationale='anti_affinity_groups does not impact the security of the service')
- `authorization` — arg_unmatched (security_impact=False, rationale='authorization itself does not require security compliance, but its sub-arguments')
- `authorization.admin_users` — arg_unmatched (security_impact=False, rationale='admin users itself does not require security compliance, but its sub-arguments m')
- `auto_repair_config` — arg_unmatched (security_impact=False, rationale='auto repair config does not require security compliance')
- `auto_resize_config.enabled` — ambiguous (security_impact=False, rationale='enabling of auto resize config does not require security compliance')
- `control_plane_ip_block.gateway` — ambiguous (security_impact=False, rationale='gateway is not security impactful enough to warrant policy compliance')
- `control_plane_ip_block.ips` — arg_unmatched (security_impact=False, rationale='ips is not security impactful enough to warrant policy compliance')
- `control_plane_ip_block.netmask` — ambiguous (security_impact=False, rationale='netmask is not security impactful enough to warrant policy compliance')
- `control_plane_node` — arg_unmatched (security_impact=False, rationale='control plane node is not security impactful')
- `control_plane_node.auto_resize_config` — arg_unmatched (security_impact=False, rationale='auto resize config does not impact security posture')
- `control_plane_node.vsphere_config` — arg_unmatched (security_impact=False, rationale='vsphere config does not impact security posture')
- `control_plane_v2_config.control_plane_ip_block` — arg_unmatched (security_impact=False, rationale='control plane ip block is not security impactful enough to warrant policy compli')
- `dataplane_v2` — arg_unmatched (security_impact=False, rationale='dataplane v2 config does not require security compliance')
- `dhcp_ip_config.enabled` — ambiguous (security_impact=False, rationale='enabling of dhcp ip config does not require security compliance')
- `ip_blocks.gateway` — ambiguous (security_impact=False, rationale='gateway is not security impactful enough to warrant policy compliance')
- `ip_blocks.ips` — arg_unmatched (security_impact=False, rationale='ips is not security impactful enough to warrant policy compliance')
- `ip_blocks.netmask` — ambiguous (security_impact=False, rationale='netmask is not security impactful enough to warrant policy compliance')
- `ips.hostname` — ambiguous (security_impact=False, rationale='hostname does not impact the security of the service')
- `ips.ip` — ambiguous (security_impact=False, rationale='ip is not security impactful enough to warrant policy compliance')
- `load_balancer` — arg_unmatched (security_impact=False, rationale='load balancer itself does not require security compliance, but its sub-arguments')
- `load_balancer.f5_config` — arg_unmatched (security_impact=False, rationale='f5 config does not require security compliance')
- `load_balancer.manual_lb_config` — arg_unmatched (security_impact=False, rationale='manual load balancer config does not require security compliance')
- `load_balancer.metal_lb_config` — arg_unmatched (security_impact=False, rationale='metal load balancer config does not require security compliance')
- `load_balancer.vip_config` — arg_unmatched (security_impact=False, rationale='vip config itself does not require security compliance, but its sub-arguments ma')
- `metal_lb_config.address_pools` — arg_unmatched (security_impact=False, rationale='address_pools does not impact the security of the service')
- `network_config` — arg_unmatched (security_impact=False, rationale='network_config itself does not require security considerations, but its sub-argu')
- `network_config.control_plane_v2_config` — arg_unmatched (security_impact=False, rationale='control plane v2 config is not security impactful enough to warrant policy compl')
- `network_config.dhcp_ip_config` — arg_unmatched (security_impact=False, rationale='dhcp ip config is not security impactful enough to warrant policy compliance')
- `network_config.host_config` — arg_unmatched (security_impact=False, rationale='host config is not security impactful enough to warrant policy compliance')
- `network_config.static_ip_config` — arg_unmatched (security_impact=False, rationale='static ip config is not security impactful enough to warrant policy compliance')
- `static_ip_config.ip_blocks` — arg_unmatched (security_impact=False, rationale='static_ip_config ip blocks is not security impactful enough to warrant policy co')
- `storage` — arg_unmatched (security_impact=False, rationale='storage is not a security impactful configuration')
- `upgrade_policy` — arg_unmatched (security_impact=False, rationale='upgrade policy does not require security compliance')
- `vcenter` — arg_unmatched (security_impact=False, rationale='vcenter config does not require security compliance')

### `gkeonprem_vmware_node_pool`
- `config` — arg_unmatched (security_impact=False, rationale='config itself does not require security considerations, but some of its nested p')
- `config.taints` — arg_unmatched (security_impact=False, rationale='taints is not a security sensitive field')
- `config.vsphere_config` — arg_unmatched (security_impact=False, rationale='vsphere_config is not a security sensitive field')
- `node_pool_autoscaling` — arg_unmatched (security_impact=False, rationale='node pool autoscaling itself does not require security considerations, but some ')
- `vsphere_config.tags` — arg_unmatched (security_impact=False, rationale='tags is not a security sensitive field')

### `healthcare_dataset`
- `encryption_spec` — arg_unmatched (security_impact=True, rationale='Without a customer-managed encryption key (CMEK), the dataset uses Google-manage')

### `healthcare_dicom_store`
- `bigquery_destination` — arg_unmatched (security_impact=False, rationale='The BigQuery destination block defines where DICOM metadata is exported. Securit')
- `bigquery_destination.table_uri` — arg_unmatched (security_impact=False, rationale='The table_uri is a destination reference only. Security of the target BigQuery t')
- `notification_config` — arg_unmatched (security_impact=True, rationale='Without a Pub/Sub notification configuration, DICOM store operations such as cre')
- `stream_configs` — arg_unmatched (security_impact=False, rationale='Stream configs define BigQuery export destinations and do not directly affect th')
- `stream_configs.bigquery_destination` — arg_unmatched (security_impact=False, rationale='The BigQuery destination is a required field within stream_configs and defines t')

### `healthcare_fhir_store`
- `bigquery_destination` — arg_unmatched (security_impact=False, rationale='The BigQuery destination block defines where FHIR data is exported. Security of ')
- `bigquery_destination.schema_config` — arg_unmatched (security_impact=False, rationale='The schema_config defines the structure of exported data and does not directly a')
- `enable_history_modifications` — arg_unmatched (security_impact=False, rationale='This field controls whether history bundles can overwrite existing versions via ')
- `last_updated_partition_config` — arg_unmatched (security_impact=False, rationale='The last_updated_partition_config block controls BigQuery partition settings and')
- `notification_config` — arg_unmatched (security_impact=False, rationale='This field is deprecated. Use notification_configs instead for Pub/Sub audit str')
- `notification_configs` — arg_unmatched (security_impact=False, rationale='The notification_configs block itself is a container. Security is enforced throu')
- `schema_config` — arg_unmatched (security_impact=False, rationale='The schema_config block defines BigQuery export schema settings and does not dir')
- `schema_config.last_updated_partition_config` — arg_unmatched (security_impact=False, rationale='This field controls BigQuery table partitioning and does not directly affect sec')
- `stream_configs` — arg_unmatched (security_impact=False, rationale='Stream configs define BigQuery export destinations and do not directly affect th')
- `stream_configs.bigquery_destination` — arg_unmatched (security_impact=False, rationale='The BigQuery destination is a required field within stream_configs. Security of ')

### `healthcare_hl7_v2_store`
- `notification_config` — arg_unmatched (security_impact=False, rationale='This field is deprecated. Use notification_configs instead for Pub/Sub audit str')
- `notification_configs` — arg_unmatched (security_impact=True, rationale='Without a Pub/Sub notification configuration, HL7 V2 store operations such as me')
- `parser_config` — arg_unmatched (security_impact=False, rationale='The parser_config block controls HL7 V2 message parsing behaviour and does not d')

### `healthcare_pipeline_job`
- `backfill_pipeline_job` — arg_unmatched (security_impact=False, rationale='The backfill_pipeline_job block defines backfill configuration and does not dire')
- `fhir_streaming_source` — arg_unmatched (security_impact=False, rationale='The fhir_streaming_source block defines the streaming input source and does not ')
- `fhir_streaming_source.description` — ambiguous (security_impact=False, rationale='A descriptive text field only, no security impact.')
- `mapping_config` — arg_unmatched (security_impact=False, rationale='The mapping_config block defines the mapping logic configuration and does not di')
- `mapping_config.description` — ambiguous (security_impact=False, rationale='A descriptive text field only, no security impact.')
- `mapping_config.whistle_config_source` — arg_unmatched (security_impact=False, rationale='The whistle_config_source is a path reference to mapping logic and does not dire')
- `mapping_pipeline_job.fhir_streaming_source` — arg_unmatched (security_impact=False, rationale='The fhir_streaming_source defines the input data source and does not directly af')
- `mapping_pipeline_job.mapping_config` — arg_unmatched (security_impact=False, rationale='The mapping_config is a required reference to mapping logic and does not directl')
- `merge_config` — arg_unmatched (security_impact=False, rationale='The merge_config block defines reconciliation merge logic and does not directly ')
- `merge_config.description` — ambiguous (security_impact=False, rationale='A descriptive text field only, no security impact.')
- `merge_config.whistle_config_source` — arg_unmatched (security_impact=False, rationale='The whistle_config_source is a path reference to reconciliation logic and does n')
- `reconciliation_pipeline_job` — arg_unmatched (security_impact=False, rationale='The reconciliation_pipeline_job block defines reconciliation configuration and d')
- `reconciliation_pipeline_job.merge_config` — arg_unmatched (security_impact=False, rationale='The merge_config is a required reference to reconciliation logic and does not di')
- `whistle_config_source` — arg_unmatched (security_impact=False, rationale='The whistle_config_source block defines GCS paths for Whistle mapping files and ')
- `whistle_config_source.import_uri_prefix` — ambiguous (security_impact=False, rationale='The import_uri_prefix is a GCS directory reference and does not directly affect ')
- `whistle_config_source.uri` — ambiguous (security_impact=False, rationale='The uri is a GCS path reference to the Whistle entrypoint file and does not dire')

### `healthcare_workspace`
- `settings` — arg_unmatched (security_impact=False, rationale='The settings block is a required container for workspace configuration and does ')

### `iap_app_engine_service_iam`
- `condition` — arg_unmatched (security_impact=True, rationale='Reduces blast radius by restricting when/where the binding applies.')

### `iap_app_engine_version_iam`
- `condition` — arg_unmatched (security_impact=True, rationale='Reduces blast radius by restricting when/where the binding applies.')

### `iap_settings`
- `allowed_domains_settings.enable` — ambiguous (security_impact=True, rationale='Allowed Domains must be enabled to take effect.')

### `iap_tunnel_dest_group_iam`
- `condition` — arg_unmatched (security_impact=True, rationale='Reduces blast radius by restricting when/where the binding applies.')

### `iap_tunnel_iam`
- `condition` — arg_unmatched (security_impact=True, rationale='Reduces blast radius by restricting when/where the binding applies.')

### `iap_tunnel_instance_iam`
- `condition` — arg_unmatched (security_impact=True, rationale='Reduces blast radius by restricting when/where the binding applies.')

### `iap_web_backend_service_iam`
- `condition` — arg_unmatched (security_impact=True, rationale='IAM Conditions reduce blast radius by scoping access (time, path/host, device co')

### `iap_web_cloud_run_service_iam`
- `condition` — arg_unmatched (security_impact=True, rationale='Reduces blast radius by restricting when/where the binding applies.')

### `iap_web_iam`
- `condition` — arg_unmatched (security_impact=True, rationale='Reduces blast radius by restricting when/where the binding applies.')

### `iap_web_region_backend_service_iam`
- `condition` — arg_unmatched (security_impact=True, rationale='Reduces blast radius by restricting when/where the binding applies.')

### `iap_web_type_app_engine_iam`
- `condition` — arg_unmatched (security_impact=True, rationale='Reduces blast radius by restricting when/where the binding applies.')

### `iap_web_type_compute_iam`
- `condition` — arg_unmatched (security_impact=True, rationale='Reduces blast radius by restricting when/where the binding applies.')

### `integrations_auth_config`
- `auth_token` — arg_unmatched (security_impact=True, rationale='Auth tokens provide secure access credentials that reduce the risk of exposing s')
- `client_certificate` — arg_unmatched (security_impact=False)
- `decrypted_credential` — arg_unmatched (security_impact=True, rationale='Proper handling and encryption of the credentials ensure they remain confidentia')
- `decrypted_credential.auth_token` — arg_unmatched (security_impact=True, rationale='Auth tokens provide secure access credentials that reduce the risk of exposing s')
- `decrypted_credential.jwt` — arg_unmatched (security_impact=True, rationale='JWT enable secure token-based authentication, allowing reliable verification of ')
- `decrypted_credential.oauth2_authorization_code` — arg_unmatched (security_impact=False)
- `decrypted_credential.oauth2_client_credentials` — arg_unmatched (security_impact=True, rationale='OAuth2 client credentials provide a secure, standardized method for service-to-s')
- `decrypted_credential.oidc_token` — arg_unmatched (security_impact=False)
- `decrypted_credential.service_account_credentials` — arg_unmatched (security_impact=False)
- `decrypted_credential.username_and_password` — arg_unmatched (security_impact=False)
- `entries` — arg_unmatched (security_impact=False)
- `entries.key` — arg_unmatched (security_impact=False)
- `entries.value` — arg_unmatched (security_impact=False)
- `jwt` — arg_unmatched (security_impact=True, rationale='JWT enable secure token-based authentication, allowing reliable verification of ')
- `jwt.jwt` — arg_unmatched (security_impact=False)
- `key` — arg_unmatched (security_impact=False)
- `key.literal_value` — arg_unmatched (security_impact=False)
- `literal_value` — arg_unmatched (security_impact=False)
- `literal_value.string_value` — ambiguous (security_impact=False)
- `oauth2_authorization_code` — arg_unmatched (security_impact=False)
- `oauth2_authorization_code.client_id` — ambiguous (security_impact=False)
- `oauth2_authorization_code.client_secret` — ambiguous (security_impact=False)
- `oauth2_authorization_code.scope` — ambiguous (security_impact=False)
- `oauth2_authorization_code.token_endpoint` — ambiguous (security_impact=False)
- `oauth2_client_credentials` — arg_unmatched (security_impact=True, rationale='OAuth2 client credentials provide a secure, standardized method for service-to-s')
- `oauth2_client_credentials.client_id` — ambiguous (security_impact=False)
- `oauth2_client_credentials.client_secret` — ambiguous (security_impact=True, rationale='Client secrets are critical authentication credentials. Leaving them empty or un')
- `oauth2_client_credentials.scope` — ambiguous (security_impact=False)
- `oauth2_client_credentials.token_endpoint` — ambiguous (security_impact=False)
- `oauth2_client_credentials.token_params` — arg_unmatched (security_impact=False)
- `token_params` — arg_unmatched (security_impact=False)
- `token_params.entries` — arg_unmatched (security_impact=False)
- `username_and_password` — arg_unmatched (security_impact=False)
- `value` — arg_unmatched (security_impact=False)
- `value.literal_value` — arg_unmatched (security_impact=False)

### `integrations_client`
- `cloud_kms_config` — arg_unmatched (security_impact=True, rationale='Cloud KMS configuration ensures that encryption keys are properly defined, locat')

### `logging_billing_account_bucket_config`
- `index_configs` — arg_unmatched (security_impact=False, rationale='Index configuration affects query performance and cost but does not impact secur')

### `logging_billing_account_sink`
- `bigquery_options` — arg_unmatched (security_impact=False, rationale='BigQuery options affect table organization and query performance but have no dir')
- `exclusions` — arg_unmatched (security_impact=True, rationale='Exclusions allow selective filtering of logs before export. Excluding security-c')

### `logging_folder_bucket_config`
- `index_configs` — arg_unmatched (security_impact=False, rationale='Index configuration affects query performance and cost but does not impact secur')

### `logging_folder_sink`
- `bigquery_options` — arg_unmatched (security_impact=False, rationale='BigQuery options affect table organization and query performance but have no dir')
- `exclusions` — arg_unmatched (security_impact=True, rationale='Exclusions allow selective filtering of logs before export. Excluding security-c')

### `logging_log_view_iam`
- `condition` — arg_unmatched (security_impact=True, rationale='Conditions can be used to enforce time-based or attribute-based access controls ')

### `logging_metric`
- `bucket_options` — arg_unmatched (security_impact=False, rationale='Bucket options define histogram bins for distribution metrics. No security impac')
- `bucket_options.explicit_buckets` — arg_unmatched (security_impact=False, rationale='Explicit buckets define histogram bins. No security implications.')
- `bucket_options.exponential_buckets` — arg_unmatched (security_impact=False, rationale='Exponential buckets define histogram bins. No security implications.')
- `bucket_options.linear_buckets` — arg_unmatched (security_impact=False, rationale='Linear buckets define histogram bins. No security implications.')
- `explicit_buckets` — arg_unmatched (security_impact=False, rationale='Deprecated field. No security impact.')
- `exponential_buckets` — arg_unmatched (security_impact=False, rationale='Deprecated field. No security impact.')
- `exponential_buckets.num_finite_buckets` — ambiguous (security_impact=False, rationale='Defines histogram bin count. No security impact.')
- `labels` — arg_unmatched (security_impact=False, rationale='Deprecated field. No security impact.')
- `labels.description` — ambiguous (security_impact=False, rationale='Description is metadata only.')
- `labels.value_type` — ambiguous (security_impact=False, rationale='Value type defines label data format. No security impact.')
- `linear_buckets` — arg_unmatched (security_impact=False, rationale='Deprecated field. No security impact.')
- `linear_buckets.num_finite_buckets` — ambiguous (security_impact=False, rationale='Defines histogram bin count. No security impact.')
- `metric_descriptor` — arg_unmatched (security_impact=False, rationale='Metric descriptor defines how data is measured and stored. Has no direct securit')
- `metric_descriptor.labels` — arg_unmatched (security_impact=False, rationale='Labels add dimensions to metrics. Has no direct security impact.')

### `logging_organization_bucket_config`
- `index_configs` — arg_unmatched (security_impact=False, rationale='Index configuration affects query performance and cost but does not impact secur')

### `logging_organization_sink`
- `bigquery_options` — arg_unmatched (security_impact=False, rationale='BigQuery options affect table organization and query performance but have no dir')
- `exclusions` — arg_unmatched (security_impact=True, rationale='Exclusions allow selective filtering of logs before export at organization level')

### `logging_project_bucket_config`
- `cmek_settings` — arg_unmatched (security_impact=True, rationale='CMEK provides customer-controlled encryption of log data, required for complianc')
- `cmek_settings.kms_key_version_name` — arg_unmatched (security_impact=False, rationale='Key version name is a read-only field that identifies the specific key version. ')
- `cmek_settings.name` — arg_unmatched (security_impact=False, rationale='Name is a system-generated identifier for the CMEK settings. Has no security imp')
- `cmek_settings.service_account_id` — arg_unmatched (security_impact=False, rationale='Service account ID identifies the service account that needs KMS permissions. Ha')
- `index_configs` — arg_unmatched (security_impact=False, rationale='Index configuration affects query performance and cost but does not impact secur')

### `logging_project_sink`
- `bigquery_options` — arg_unmatched (security_impact=False, rationale='BigQuery options affect table organization and query performance but have no dir')
- `exclusions` — arg_unmatched (security_impact=True, rationale='Exclusions allow selective filtering of logs before export. Excluding security-c')

### `managed_kafka_acl`
- `acl_entries` — arg_unmatched (security_impact=True, rationale='ACLs are used to control access to Kafka resources, ensuring that only authorize')

### `managed_kafka_cluster`
- `capacity_config` — arg_unmatched (security_impact=True, rationale='Insufficient capacity may lead to instability and service denial.')
- `gcp_config` — arg_unmatched (security_impact=True, rationale='Defines networking and encryption aspects that directly affect cluster confident')
- `gcp_config.access_config` — arg_unmatched (security_impact=True, rationale='Improper access can expose internal systems.')
- `network_configs` — arg_unmatched (security_impact=True, rationale='Improper subnet configuration can expose cluster externally.')
- `tls_config` — arg_unmatched (security_impact=True, rationale='Protects Kafka clients and brokers with encryption and identity validation.')
- `tls_config.trust_config` — arg_unmatched (security_impact=True, rationale='Validates identities through certificate chains.')

### `managed_kafka_connect_cluster`
- `access_config` — arg_unmatched (security_impact=True, rationale='Manages network exposure of the cluster.')
- `access_config.network_configs` — arg_unmatched (security_impact=True, rationale='Defines network isolation. Insecure subnet increases exposure.')
- `capacity_config` — arg_unmatched (security_impact=False, rationale='Capacity impacts availability but not directly related to security posture.')
- `gcp_config` — arg_unmatched (security_impact=True, rationale='This includes networking config which affects visibility, access, and isolation.')
- `gcp_config.access_config` — arg_unmatched (security_impact=True, rationale='Controls how the Connect cluster is accessed. Improper setup risks exposure.')
- `network_configs` — arg_unmatched (security_impact=True, rationale='Defines networking for PSC interfaces, which affect secure access.')

### `managed_kafka_connector`
- `task_restart_policy` — arg_unmatched (security_impact=False, rationale='Primarily affects availability and reliability, not confidentiality or access co')

### `memcache_instance`
- `maintenance_policy` — arg_unmatched (security_impact=True, rationale='Defining a maintenance policy ensures predictable maintenance windows, allowing ')
- `maintenance_policy.weekly_maintenance_window` — arg_unmatched (security_impact=True, rationale='Setting a weekly maintenance window allows administrators to align system update')
- `maintenance_policy.weekly_maintenance_window.start_time` — arg_unmatched (security_impact=None, rationale='A properly chosen start time ensures updates happen during low-usage hours.')
- `memcache_parameters` — arg_unmatched (security_impact=False)
- `node_config` — arg_unmatched (security_impact=False)

### `model_armor_floorsetting`
- `filter_config` — arg_unmatched (security_impact=True, rationale='Central configuration for malicious-URI, Responsible-AI, sensitive-data and prom')
- `filter_config.malicious_uri_filter_settings` — arg_unmatched (security_impact=True, rationale='Controls detection of malicious links to prevent data exfiltration and phishing.')
- `filter_config.pi_and_jailbreak_filter_settings` — arg_unmatched (security_impact=True, rationale='Blocks malicious attempts to override system instructions or leak data.')
- `filter_config.rai_settings` — arg_unmatched (security_impact=True, rationale='Helps ensure generated content adheres to Responsible AI guidelines.')
- `filter_config.sdp_settings` — arg_unmatched (security_impact=True, rationale='Prevents exposure of sensitive or regulated information.')
- `pi_and_jailbreak_filter_settings.confidence_level` — ambiguous (security_impact=True, rationale='Determines sensitivity to possible prompt injection attempts.')
- `pi_and_jailbreak_filter_settings.filter_enforcement` — ambiguous (security_impact=True, rationale='Disabling increases risk of prompt manipulation.')
- `rai_filters.confidence_level` — ambiguous (security_impact=True, rationale='Higher thresholds reduce false positives but may allow harmful content if too lo')

### `model_armor_template`
- `filter_config` — arg_unmatched (security_impact=True, rationale='Controls which filters (malicious URI, Responsible AI, sensitive data, prompt-in')
- `filter_config.malicious_uri_filter_settings` — arg_unmatched (security_impact=True, rationale='Prevents injection of malicious URLs. Disabling increases phishing or malware ri')
- `filter_config.pi_and_jailbreak_filter_settings` — arg_unmatched (security_impact=True, rationale='Blocks attempts to override safeguards or exfiltrate data via prompt injection.')
- `filter_config.rai_settings` — arg_unmatched (security_impact=True, rationale='Helps block hate speech, harassment, or dangerous content from LLM responses.')
- `filter_config.sdp_settings` — arg_unmatched (security_impact=True, rationale='Detects and masks personal or sensitive data. Disabling risks data leakage and c')
- `rai_filters.confidence_level` — ambiguous (security_impact=True, rationale='Determines sensitivity of detection. Too low can allow harmful content through; ')

### `monitoring_alert_policy`
- `aggregations` — arg_unmatched (security_impact=False, rationale='Data aggregation setting with no direct security impact.')
- `aggregations.alignment_period` — ambiguous (security_impact=False, rationale='Operational timing setting with no direct security impact.')
- `aggregations.cross_series_reducer` — ambiguous (security_impact=False, rationale='Data aggregation setting with no direct security impact.')
- `aggregations.group_by_fields` — ambiguous (security_impact=False, rationale='Functional configuration setting with no direct security impact.')
- `aggregations.per_series_aligner` — ambiguous (security_impact=False, rationale='Data aggregation setting with no direct security impact.')
- `alert_strategy` — arg_unmatched (security_impact=False, rationale='Operational timing setting with no direct security impact.')
- `alert_strategy.notification_channel_strategy` — arg_unmatched (security_impact=False, rationale='Notification routing setting with no direct security impact.')
- `alert_strategy.notification_rate_limit` — arg_unmatched (security_impact=False, rationale='Functional configuration setting with no direct security impact.')
- `boolean_test` — arg_unmatched (security_impact=False, rationale='Functional configuration setting with no direct security impact.')
- `condition_absent` — arg_unmatched (security_impact=False, rationale='Functional configuration setting with no direct security impact.')
- `condition_absent.aggregations` — arg_unmatched (security_impact=False, rationale='Data aggregation setting with no direct security impact.')
- `condition_absent.duration` — ambiguous (security_impact=False, rationale='Operational timing setting with no direct security impact.')
- `condition_absent.filter` — ambiguous (security_impact=False, rationale='Functional filter setting with no direct security impact.')
- `condition_absent.trigger` — arg_unmatched (security_impact=False, rationale='Functional trigger setting with no direct security impact.')
- `condition_matched_log` — arg_unmatched (security_impact=False, rationale='Functional configuration setting with no direct security impact.')
- `condition_matched_log.filter` — ambiguous (security_impact=False, rationale='Functional filter setting with no direct security impact.')
- `condition_monitoring_query_language` — arg_unmatched (security_impact=False, rationale='Functional content setting with no direct security impact.')
- `condition_monitoring_query_language.duration` — ambiguous (security_impact=False, rationale='Operational timing setting with no direct security impact.')
- `condition_monitoring_query_language.evaluation_missing_data` — ambiguous (security_impact=False, rationale='Functional configuration setting with no direct security impact.')
- `condition_monitoring_query_language.query` — ambiguous (security_impact=False, rationale='Functional content setting with no direct security impact.')
- `condition_monitoring_query_language.trigger` — arg_unmatched (security_impact=False, rationale='Functional trigger setting with no direct security impact.')
- `condition_prometheus_query_language` — arg_unmatched (security_impact=False, rationale='Functional content setting with no direct security impact.')
- `condition_prometheus_query_language.duration` — ambiguous (security_impact=False, rationale='Operational timing setting with no direct security impact.')
- `condition_prometheus_query_language.query` — ambiguous (security_impact=False, rationale='Functional content setting with no direct security impact.')
- `condition_sql` — arg_unmatched (security_impact=False, rationale='Functional configuration setting with no direct security impact.')
- `condition_sql.boolean_test` — arg_unmatched (security_impact=False, rationale='Functional configuration setting with no direct security impact.')
- `condition_sql.daily` — arg_unmatched (security_impact=False, rationale='Functional configuration setting with no direct security impact.')
- `condition_sql.hourly` — arg_unmatched (security_impact=False, rationale='Functional configuration setting with no direct security impact.')
- `condition_sql.query` — ambiguous (security_impact=False, rationale='Functional content setting with no direct security impact.')
- `condition_sql.row_count_test` — arg_unmatched (security_impact=False, rationale='Functional configuration setting with no direct security impact.')
- `condition_threshold` — arg_unmatched (security_impact=False, rationale='Performance threshold with no direct security impact.')
- `condition_threshold.aggregations` — arg_unmatched (security_impact=False, rationale='Data aggregation setting with no direct security impact.')
- `condition_threshold.comparison` — ambiguous (security_impact=False, rationale='Functional comparison operator with no direct security impact.')
- `condition_threshold.denominator_aggregations` — arg_unmatched (security_impact=False, rationale='Data aggregation setting with no direct security impact.')
- `condition_threshold.duration` — ambiguous (security_impact=False, rationale='Operational timing setting with no direct security impact.')
- `condition_threshold.evaluation_missing_data` — ambiguous (security_impact=False, rationale='Functional configuration setting with no direct security impact.')
- `condition_threshold.filter` — ambiguous (security_impact=False, rationale='Functional filter setting with no direct security impact.')
- `condition_threshold.forecast_options` — arg_unmatched (security_impact=False, rationale='Functional configuration setting with no direct security impact.')
- `condition_threshold.trigger` — arg_unmatched (security_impact=False, rationale='Functional trigger setting with no direct security impact.')
- `conditions` — arg_unmatched (security_impact=False, rationale='Conditions are application-specific and cannot be enforced generically via polic')
- `conditions.condition_absent` — arg_unmatched (security_impact=False, rationale='Functional configuration setting with no direct security impact.')
- `conditions.condition_matched_log` — arg_unmatched (security_impact=False, rationale='Functional configuration setting with no direct security impact.')
- `conditions.condition_monitoring_query_language` — arg_unmatched (security_impact=False, rationale='Functional content setting with no direct security impact.')
- `conditions.condition_prometheus_query_language` — arg_unmatched (security_impact=False, rationale='Functional content setting with no direct security impact.')
- `conditions.condition_sql` — arg_unmatched (security_impact=False, rationale='Functional configuration setting with no direct security impact.')
- `conditions.condition_threshold` — arg_unmatched (security_impact=False, rationale='Performance threshold with no direct security impact.')
- `conditions.name` — arg_unmatched (security_impact=False, rationale='Identifier or display metadata with no security impact.')
- `daily` — arg_unmatched (security_impact=False, rationale='Functional configuration setting with no direct security impact.')
- `daily.execution_time` — arg_unmatched (security_impact=False, rationale='Functional configuration setting with no direct security impact.')
- `daily.periodicity` — ambiguous (security_impact=False, rationale='Operational timing setting with no direct security impact.')
- `denominator_aggregations` — arg_unmatched (security_impact=False, rationale='Data aggregation setting with no direct security impact.')
- `denominator_aggregations.alignment_period` — ambiguous (security_impact=False, rationale='Operational timing setting with no direct security impact.')
- `denominator_aggregations.cross_series_reducer` — ambiguous (security_impact=False, rationale='Data aggregation setting with no direct security impact.')
- `denominator_aggregations.group_by_fields` — ambiguous (security_impact=False, rationale='Functional configuration setting with no direct security impact.')
- `denominator_aggregations.per_series_aligner` — ambiguous (security_impact=False, rationale='Data aggregation setting with no direct security impact.')
- `documentation` — arg_unmatched (security_impact=False, rationale='Operational documentation with no security impact.')
- `documentation.links` — arg_unmatched (security_impact=False, rationale='Functional configuration setting with no direct security impact.')
- `execution_time` — arg_unmatched (security_impact=False, rationale='Functional configuration setting with no direct security impact.')
- `forecast_options` — arg_unmatched (security_impact=False, rationale='Functional configuration setting with no direct security impact.')
- `hourly` — arg_unmatched (security_impact=False, rationale='Functional configuration setting with no direct security impact.')
- `hourly.periodicity` — ambiguous (security_impact=False, rationale='Operational timing setting with no direct security impact.')
- `links` — arg_unmatched (security_impact=False, rationale='Functional configuration setting with no direct security impact.')
- `links.display_name` — ambiguous (security_impact=False, rationale='Display metadata with no security impact.')
- `minutes.periodicity` — ambiguous (security_impact=False, rationale='Operational timing setting with no direct security impact.')
- `notification_channel_strategy` — arg_unmatched (security_impact=False, rationale='Notification routing setting with no direct security impact.')
- `notification_rate_limit` — arg_unmatched (security_impact=False, rationale='Functional configuration setting with no direct security impact.')
- `row_count_test` — arg_unmatched (security_impact=False, rationale='Functional configuration setting with no direct security impact.')
- `row_count_test.comparison` — ambiguous (security_impact=False, rationale='Functional comparison operator with no direct security impact.')
- `trigger` — arg_unmatched (security_impact=False, rationale='Functional trigger setting with no direct security impact.')
- `trigger.count` — ambiguous (security_impact=False, rationale='Functional configuration setting with no direct security impact.')
- `trigger.percent` — ambiguous (security_impact=False, rationale='Functional configuration setting with no direct security impact.')

### `monitoring_custom_service`
- `telemetry` — arg_unmatched (security_impact=False, rationale='Functional configuration setting with no direct security impact.')

### `monitoring_metric_descriptor`
- `labels` — arg_unmatched (security_impact=False, rationale='Functional or organisational label with no security impact.')
- `metadata` — arg_unmatched (security_impact=False, rationale='Functional configuration setting with no direct security impact.')

### `monitoring_notification_channel`
- `sensitive_labels` — arg_unmatched (security_impact=False, rationale='Functional or organisational label with no security impact.')

### `monitoring_service`
- `basic_service` — arg_unmatched (security_impact=False, rationale='Functional configuration setting with no direct security impact.')

### `monitoring_slo`
- `availability` — arg_unmatched (security_impact=False, rationale='Performance measurement with no security impact.')
- `availability.enabled` — ambiguous (security_impact=False, rationale='Functional configuration setting with no direct security impact.')
- `basic_sli` — arg_unmatched (security_impact=False, rationale='Functional configuration setting with no direct security impact.')
- `basic_sli.availability` — arg_unmatched (security_impact=False, rationale='Performance measurement with no security impact.')
- `basic_sli.latency` — arg_unmatched (security_impact=False, rationale='Performance measurement with no security impact.')
- `basic_sli_performance` — arg_unmatched (security_impact=False, rationale='Functional configuration setting with no direct security impact.')
- `basic_sli_performance.availability` — arg_unmatched (security_impact=False, rationale='Performance measurement with no security impact.')
- `basic_sli_performance.latency` — arg_unmatched (security_impact=False, rationale='Performance measurement with no security impact.')
- `basic_sli_performance.location` — ambiguous (security_impact=False, rationale='Geographic scoping with no security impact.')
- `basic_sli_performance.method` — ambiguous (security_impact=False, rationale='Functional scoping with no security impact.')
- `basic_sli_performance.version` — ambiguous (security_impact=False, rationale='Version scoping with no security impact.')
- `distribution_cut` — arg_unmatched (security_impact=False, rationale='Performance measurement calculation with no security impact.')
- `distribution_cut.distribution_filter` — ambiguous (security_impact=False, rationale='Functional filter setting with no direct security impact.')
- `distribution_cut.range` — arg_unmatched (security_impact=False, rationale='Functional configuration setting with no direct security impact.')
- `good_total_ratio` — arg_unmatched (security_impact=False, rationale='Performance measurement calculation with no security impact.')
- `good_total_ratio.bad_service_filter` — ambiguous (security_impact=False, rationale='Functional filter setting with no direct security impact.')
- `good_total_ratio.good_service_filter` — ambiguous (security_impact=False, rationale='Functional filter setting with no direct security impact.')
- `good_total_ratio.total_service_filter` — ambiguous (security_impact=False, rationale='Functional filter setting with no direct security impact.')
- `good_total_ratio_threshold` — arg_unmatched (security_impact=False, rationale='Performance threshold with no security impact.')
- `good_total_ratio_threshold.basic_sli_performance` — arg_unmatched (security_impact=False, rationale='Functional configuration setting with no direct security impact.')
- `good_total_ratio_threshold.performance` — arg_unmatched (security_impact=False, rationale='Functional configuration setting with no direct security impact.')
- `good_total_ratio_threshold.threshold` — ambiguous (security_impact=False, rationale='Performance threshold with no direct security impact.')
- `latency` — arg_unmatched (security_impact=False, rationale='Performance measurement with no security impact.')
- `latency.threshold` — ambiguous (security_impact=False, rationale='Performance threshold with no direct security impact.')
- `metric_mean_in_range` — arg_unmatched (security_impact=False, rationale='Performance measurement with no security impact.')
- `metric_mean_in_range.range` — arg_unmatched (security_impact=False, rationale='Functional configuration setting with no direct security impact.')
- `metric_mean_in_range.time_series` — ambiguous (security_impact=False, rationale='Functional configuration setting with no direct security impact.')
- `metric_sum_in_range` — arg_unmatched (security_impact=False, rationale='Performance measurement with no security impact.')
- `metric_sum_in_range.range` — arg_unmatched (security_impact=False, rationale='Functional configuration setting with no direct security impact.')
- `metric_sum_in_range.time_series` — ambiguous (security_impact=False, rationale='Functional configuration setting with no direct security impact.')
- `performance` — arg_unmatched (security_impact=False, rationale='Functional configuration setting with no direct security impact.')
- `performance.distribution_cut` — arg_unmatched (security_impact=False, rationale='Performance measurement calculation with no security impact.')
- `performance.good_total_ratio` — arg_unmatched (security_impact=False, rationale='Performance measurement calculation with no security impact.')
- `range` — arg_unmatched (security_impact=False, rationale='Functional configuration setting with no direct security impact.')
- `range.max` — ambiguous (security_impact=False, rationale='Functional configuration setting with no direct security impact.')
- `range.min` — ambiguous (security_impact=False, rationale='Functional configuration setting with no direct security impact.')
- `request_based_sli` — arg_unmatched (security_impact=False, rationale='Functional configuration setting with no direct security impact.')
- `request_based_sli.distribution_cut` — arg_unmatched (security_impact=False, rationale='Performance measurement calculation with no security impact.')
- `request_based_sli.good_total_ratio` — arg_unmatched (security_impact=False, rationale='Performance measurement calculation with no security impact.')
- `windows_based_sli` — arg_unmatched (security_impact=False, rationale='Functional configuration setting with no direct security impact.')
- `windows_based_sli.good_total_ratio_threshold` — arg_unmatched (security_impact=False, rationale='Performance threshold with no security impact.')
- `windows_based_sli.metric_mean_in_range` — arg_unmatched (security_impact=False, rationale='Performance measurement with no security impact.')
- `windows_based_sli.metric_sum_in_range` — arg_unmatched (security_impact=False, rationale='Performance measurement with no security impact.')

### `monitoring_uptime_check_config`
- `accepted_response_status_codes` — arg_unmatched (security_impact=False, rationale='Functional setting with no direct security impact.')
- `cloud_function_v2` — arg_unmatched (security_impact=False, rationale='Functional configuration setting with no direct security impact.')
- `content_matchers` — arg_unmatched (security_impact=False, rationale='Functional matching with no direct security impact.')
- `content_matchers.json_path_matcher` — arg_unmatched (security_impact=False, rationale='Functional configuration setting with no direct security impact.')
- `http_check` — arg_unmatched (security_impact=False, rationale='Functional configuration setting with no direct security impact.')
- `http_check.accepted_response_status_codes` — arg_unmatched (security_impact=False, rationale='Functional setting with no direct security impact.')
- `http_check.auth_info` — arg_unmatched (security_impact=False, rationale='Authentication is handled at the application level; no policy enforced here.')
- `http_check.ping_config` — arg_unmatched (security_impact=False, rationale='Functional configuration setting with no direct security impact.')
- `http_check.service_agent_authentication` — arg_unmatched (security_impact=False, rationale='Authentication method choice with no direct security impact.')
- `json_path_matcher` — arg_unmatched (security_impact=False, rationale='Functional configuration setting with no direct security impact.')
- `monitored_resource` — arg_unmatched (security_impact=False, rationale='Functional configuration setting with no direct security impact.')
- `ping_config` — arg_unmatched (security_impact=False, rationale='Functional configuration setting with no direct security impact.')
- `ping_config.pings_count` — ambiguous (security_impact=False, rationale='Functional configuration setting with no direct security impact.')
- `resource_group` — arg_unmatched (security_impact=False, rationale='Functional configuration setting with no direct security impact.')
- `service_agent_authentication` — arg_unmatched (security_impact=False, rationale='Authentication method choice with no direct security impact.')
- `service_agent_authentication.type` — ambiguous (security_impact=False, rationale='Resource type selector with no direct security impact.')
- `synthetic_monitor` — arg_unmatched (security_impact=False, rationale='Functional configuration setting with no direct security impact.')
- `synthetic_monitor.cloud_function_v2` — arg_unmatched (security_impact=False, rationale='Functional configuration setting with no direct security impact.')
- `tcp_check` — arg_unmatched (security_impact=False, rationale='Functional configuration setting with no direct security impact.')
- `tcp_check.ping_config` — arg_unmatched (security_impact=False, rationale='Functional configuration setting with no direct security impact.')

### `network_security_address_group`
- `purpose` — arg_unmatched (security_impact=True, rationale='Restricting purpose to CLOUD_ARMOR enforces consistent security usage and preven')

### `network_security_client_tls_policy`
- `client_certificate.certificate_provider_instance` — arg_unmatched (security_impact=True, rationale='Restricting plugin_instance to an approved value helps enforce trusted client ce')
- `server_validation_ca` — arg_unmatched (security_impact=True, rationale='Ensures the client validates the server certificate by configuring server_valida')

### `network_security_server_tls_policy`
- `mtls_policy` — arg_unmatched (security_impact=True, rationale='Ensures mtls_policy is defined.')

### `os_config_v2_policy_orchestrator`
- `instance_filter.inventories` — arg_unmatched (security_impact=None, rationale='Provides security controls for tracking and managing system assets.')
- `inventories.os_short_name` — ambiguous (security_impact=None, rationale='Enforces consistent OS platform security across all managed instances.')
- `labels.environment` — arg_unmatched (security_impact=True, rationale='Facilitates environment-specific security policies, ensuring that production env')
- `orchestrated_resource` — arg_unmatched (security_impact=True, rationale='Establishes the top-level security perimeter for all managed resources')
- `orchestrated_resource.os_policy_assignment_v1_payload` — arg_unmatched (security_impact=True, rationale='Maintains security policy structure and prevents malformed configurations')
- `orchestration_scope` — arg_unmatched (security_impact=True, rationale='Establishes the comprehensive security perimeter for all orchestration activitie')
- `orchestration_scope.selectors` — arg_unmatched (security_impact=True, rationale='Provides security controls for determining which resources meet selection criter')
- `os_policies.resource_groups` — arg_unmatched (security_impact=True, rationale='Applies security controls to multiple related resources simultaneously')
- `os_policy_assignment_v1_payload.instance_filter` — arg_unmatched (security_impact=True, rationale='Enables security policies to target specific instance characteristics')
- `os_policy_assignment_v1_payload.os_policies` — arg_unmatched (security_impact=True, rationale='Applies security configurations at the OS layer')
- `repository.apt` — arg_unmatched (security_impact=True, rationale='Enforces consistent package management across Debian systems.')
- `resource_groups.resources` — arg_unmatched (security_impact=True, rationale='Enables fine-grained security controls at the resource level')
- `resources.repository` — arg_unmatched (security_impact=True, rationale='Maintains security through controlled software distribution channels')
- `selectors.location_selector` — arg_unmatched (security_impact=True, rationale='Manages security policies based on physical and logical location constraints.')

### `os_config_v2_policy_orchestrator_for_folder`
- `orchestrated_resource` — arg_unmatched (security_impact=True, rationale='Misconfiguration of the orchestrated resource could lead to non-compliance with ')
- `orchestrated_resource.os_policy_assignment_v1_payload` — arg_unmatched (security_impact=True, rationale='Ensures that the correct OS policies are applied to the targeted VM instances, m')
- `orchestration_scope` — arg_unmatched (security_impact=True, rationale='Specifying the correct orchestration scope is crucial for ensuring that the poli')
- `orchestration_scope.selectors` — arg_unmatched (security_impact=True, rationale='Specifying the correct selectors is crucial for ensuring that the policy is appl')
- `os_policies.resource_groups` — arg_unmatched (security_impact=True, rationale='Defines the specific configurations and resources that will be managed on the ta')
- `os_policy_assignment_v1_payload.instance_filter` — arg_unmatched (security_impact=True, rationale='Ensures that the correct VMs are targeted for the OS policy assignment, maintain')
- `os_policy_assignment_v1_payload.os_policies` — arg_unmatched (security_impact=True, rationale='Defines the specific configurations and policies that will be enforced on the ta')
- `repository.apt` — arg_unmatched (security_impact=True, rationale='Ensures that only trusted package repositories are used for software installatio')
- `resource_groups.resources` — arg_unmatched (security_impact=True, rationale='Defines the specific configurations and resources that will be managed on the ta')
- `resources.repository` — arg_unmatched (security_impact=True, rationale='Ensures that only trusted package repositories are used for software installatio')
- `selectors.location_selector` — arg_unmatched (security_impact=True, rationale='Specifying the correct locations is crucial for ensuring that the policy is appl')

### `os_config_v2_policy_orchestrator_for_organization`
- `instance_filter.inventories` — arg_unmatched (security_impact=True, rationale='Ensures that only VMs with the specified inventory are targeted, reducing the ri')
- `inventories.os_short_name` — ambiguous (security_impact=True, rationale='Specifying the OS short name ensures that policies are applied only to compatibl')
- `orchestrated_resource` — arg_unmatched (security_impact=True, rationale='Creates the primary security perimeter for all managed cloud infrastructure reso')
- `orchestrated_resource.os_policy_assignment_v1_payload` — arg_unmatched (security_impact=True, rationale='Maintains the structural integrity of security policy configurations.')
- `os_policies.resource_groups` — arg_unmatched (security_impact=True, rationale='Applies security controls to multiple related resources simultaneously, ensuring')
- `os_policy_assignment_v1_payload.os_policies` — arg_unmatched (security_impact=True, rationale=' Enforces consistent OS-level security configurations across all targeted virtua')
- `repository.apt` — arg_unmatched (security_impact=True, rationale='Ensures that only approved package sources are used, reducing the risk of malici')
- `resource_groups.resources` — arg_unmatched (security_impact=True, rationale='Enables fine-grained security controls at the resource level, allowing for tailo')
- `resources.repository` — arg_unmatched (security_impact=True, rationale='Enforces consistent package management policies across all targeted virtual mach')

### `privateca_ca_pool`
- `issuance_policy.allowed_key_types` — arg_unmatched (security_impact=True, rationale='CA Pool issuance policy must restrict elliptic curve signing algorithms to appro')

### `privateca_ca_pool_iam`
- `condition` — arg_unmatched (security_impact=False, rationale='Container block for an optional IAM condition; security evaluation applies to ch')

### `privateca_certificate`
- `additional_extensions` — arg_unmatched (security_impact=False, rationale='Container block for custom X.509 extensions; no specific extension values are ma')
- `additional_extensions.critical` — ambiguous (security_impact=False, rationale='Criticality flag on a custom extension is application-specific; no enforcement i')
- `additional_extensions.object_id` — arg_unmatched (security_impact=False, rationale='Container block for the extension OID; no security constraint is mandated on OID')
- `base_key_usage` — arg_unmatched (security_impact=False, rationale='Container block; security evaluation applies to child arguments individually.')
- `ca_options` — arg_unmatched (security_impact=False, rationale='Container block; security evaluation applies to child arguments individually.')
- `config` — arg_unmatched (security_impact=False, rationale='Container block; security evaluation applies to child arguments individually.')
- `config.public_key` — arg_unmatched (security_impact=False, rationale='Container block; security evaluation applies to child arguments individually.')
- `config.subject_config` — arg_unmatched (security_impact=False, rationale='Container block; security evaluation applies to child arguments individually.')
- `config.subject_key_id` — arg_unmatched (security_impact=False, rationale='Optional custom Subject Key Identifier for legacy CA migration; no enforceable s')
- `config.x509_config` — arg_unmatched (security_impact=False, rationale='Container block; security evaluation applies to child arguments individually.')
- `extended_key_usage` — arg_unmatched (security_impact=False, rationale='Container block; security evaluation applies to child arguments individually.')
- `key_usage` — arg_unmatched (security_impact=False, rationale='Container block; security evaluation applies to child arguments individually.')
- `key_usage.base_key_usage` — arg_unmatched (security_impact=False, rationale='Container block; security evaluation applies to child arguments individually.')
- `key_usage.extended_key_usage` — arg_unmatched (security_impact=False, rationale='Container block; security evaluation applies to child arguments individually.')
- `key_usage.unknown_extended_key_usages` — arg_unmatched (security_impact=False, rationale='Custom EKU OIDs are application-specific; no enforcement boundary is mandated.')
- `name_constraints` — arg_unmatched (security_impact=False, rationale='Container block; security evaluation applies to child arguments individually.')
- `name_constraints.critical` — ambiguous (security_impact=False, rationale='Marks the name constraints extension as critical; appropriate value depends on t')
- `object_id` — arg_unmatched (security_impact=False, rationale='Container block for OID path; no security constraint is mandated on OID values.')
- `object_id.object_id_path` — ambiguous (security_impact=False, rationale='OID path components are application-specific identifiers; no enforcement boundar')
- `policy_ids` — arg_unmatched (security_impact=False, rationale='Container block for certificate policy OIDs; no specific OID values are mandated')
- `policy_ids.object_id_path` — ambiguous (security_impact=False, rationale='OID path components are application-specific identifiers; no enforcement boundar')
- `public_key` — arg_unmatched (security_impact=False, rationale='Container block; security evaluation applies to child arguments individually.')
- `subject` — arg_unmatched (security_impact=False, rationale='Container block; security evaluation applies to child arguments individually.')
- `subject_alt_name` — arg_unmatched (security_impact=False, rationale='Container block; security evaluation applies to child arguments individually.')
- `subject_config` — arg_unmatched (security_impact=False, rationale='Container block; security evaluation applies to child arguments individually.')
- `subject_config.subject` — arg_unmatched (security_impact=False, rationale='Container block; security evaluation applies to child arguments individually.')
- `subject_config.subject_alt_name` — arg_unmatched (security_impact=False, rationale='Container block; security evaluation applies to child arguments individually.')
- `subject_key_id` — arg_unmatched (security_impact=False, rationale='Container block for an optional custom Subject Key Identifier; no security polic')
- `unknown_extended_key_usages` — arg_unmatched (security_impact=False, rationale='Container block for custom EKU OIDs; no enforcement boundary is mandated on OID ')
- `unknown_extended_key_usages.object_id_path` — ambiguous (security_impact=False, rationale='OID path components are application-specific identifiers; no enforcement boundar')
- `x509_config` — arg_unmatched (security_impact=False, rationale='Container block; security evaluation applies to child arguments individually.')
- `x509_config.additional_extensions` — arg_unmatched (security_impact=False, rationale='Container block for custom X.509 extensions; no specific extension values are ma')
- `x509_config.ca_options` — arg_unmatched (security_impact=False, rationale='Container block; security evaluation applies to child arguments individually.')
- `x509_config.key_usage` — arg_unmatched (security_impact=False, rationale='Container block; security evaluation applies to child arguments individually.')
- `x509_config.name_constraints` — arg_unmatched (security_impact=False, rationale='Container block; security evaluation applies to child arguments individually.')
- `x509_config.policy_ids` — arg_unmatched (security_impact=False, rationale='Certificate policy OIDs are application-specific; no enforcement boundary is man')

### `privateca_certificate_template`
- `additional_extensions` — arg_unmatched (security_impact=False, rationale='Container block for custom X.509 extensions; no specific extension values are ma')
- `additional_extensions.critical` — ambiguous (security_impact=False, rationale='Criticality flag on a custom extension is application-specific; no enforcement i')
- `additional_extensions.object_id` — arg_unmatched (security_impact=False, rationale='Container block for the extension OID; no security constraint is mandated on OID')
- `additional_extensions.object_id_path` — ambiguous (security_impact=False, rationale='OID path components are application-specific identifiers; no enforcement boundar')
- `base_key_usage` — arg_unmatched (security_impact=False, rationale='Container block; security evaluation applies to child arguments individually.')
- `ca_options` — arg_unmatched (security_impact=False, rationale='Container block; security evaluation applies to child arguments individually.')
- `cel_expression` — arg_unmatched (security_impact=False, rationale='Container block for a CEL validation expression; no specific expression is manda')
- `cel_expression.description` — ambiguous (security_impact=False, rationale='Human-readable description of the CEL expression; no security impact.')
- `cel_expression.location` — ambiguous (security_impact=False, rationale='Error reporting hint for the CEL expression; no security impact.')
- `extended_key_usage` — arg_unmatched (security_impact=False, rationale='Container block; security evaluation applies to child arguments individually.')
- `identity_constraints` — arg_unmatched (security_impact=False, rationale='Container block; security evaluation applies to child arguments individually.')
- `identity_constraints.cel_expression` — arg_unmatched (security_impact=False, rationale='Container block for an optional CEL validation expression; no specific expressio')
- `key_usage` — arg_unmatched (security_impact=False, rationale='Container block; security evaluation applies to child arguments individually.')
- `key_usage.base_key_usage` — arg_unmatched (security_impact=False, rationale='Container block; security evaluation applies to child arguments individually.')
- `key_usage.extended_key_usage` — arg_unmatched (security_impact=False, rationale='Container block; security evaluation applies to child arguments individually.')
- `key_usage.unknown_extended_key_usages` — arg_unmatched (security_impact=False, rationale='Custom EKU OIDs are application-specific; no enforcement boundary is mandated.')
- `name_constraints` — arg_unmatched (security_impact=False, rationale='Container block; security evaluation applies to child arguments individually.')
- `name_constraints.critical` — ambiguous (security_impact=False, rationale='Marks the name constraints extension as critical; appropriate value depends on t')
- `object_id` — arg_unmatched (security_impact=False, rationale='Container block for OID path; no security constraint is mandated on OID values.')
- `object_id.object_id_path` — ambiguous (security_impact=False, rationale='OID path components are application-specific identifiers; no enforcement boundar')
- `passthrough_extensions` — arg_unmatched (security_impact=False, rationale='Container block; security evaluation applies to child arguments individually.')
- `passthrough_extensions.additional_extensions` — arg_unmatched (security_impact=False, rationale='Custom extension OIDs allowed to pass through are application-specific; no enfor')
- `policy_ids` — arg_unmatched (security_impact=False, rationale='Container block for certificate policy OIDs; no specific OID values are mandated')
- `policy_ids.object_id_path` — ambiguous (security_impact=False, rationale='OID path components are application-specific identifiers; no enforcement boundar')
- `predefined_values` — arg_unmatched (security_impact=False, rationale='Container block; security evaluation applies to child arguments individually.')
- `predefined_values.additional_extensions` — arg_unmatched (security_impact=False, rationale='Container block for custom X.509 extensions; no specific extension values are ma')
- `predefined_values.ca_options` — arg_unmatched (security_impact=False, rationale='Container block; security evaluation applies to child arguments individually.')
- `predefined_values.key_usage` — arg_unmatched (security_impact=False, rationale='Container block; security evaluation applies to child arguments individually.')
- `predefined_values.name_constraints` — arg_unmatched (security_impact=False, rationale='Container block; security evaluation applies to child arguments individually.')
- `predefined_values.policy_ids` — arg_unmatched (security_impact=False, rationale='Certificate policy OIDs are application-specific; no enforcement boundary is man')
- `unknown_extended_key_usages` — arg_unmatched (security_impact=False, rationale='Container block for custom EKU OIDs; no enforcement boundary is mandated on OID ')
- `unknown_extended_key_usages.object_id_path` — ambiguous (security_impact=False, rationale='OID path components are application-specific identifiers; no enforcement boundar')

### `privateca_certificate_template_iam`
- `condition` — arg_unmatched (security_impact=False, rationale='Container block for an optional IAM condition; security evaluation applies to ch')

### `pubsub_subscription`
- `avro_config` — arg_unmatched (security_impact=False, rationale='Defines Avro output format settings for Cloud Storage delivery; does not affect ')
- `avro_config.use_topic_schema` — ambiguous (security_impact=False, rationale='Controls schema usage for Avro serialisation; does not affect access controls.')
- `avro_config.write_metadata` — ambiguous (security_impact=False, rationale='Controls whether message metadata is included in Avro output files; does not aff')
- `bigquery_config` — arg_unmatched (security_impact=True, rationale='Configures direct message delivery into BigQuery, creating a persistent data flo')
- `cloud_storage_config` — arg_unmatched (security_impact=True, rationale='Configures persistent message delivery to Cloud Storage; sensitive message conte')
- `cloud_storage_config.avro_config` — arg_unmatched (security_impact=False, rationale='Specifies Avro output format for Cloud Storage files; does not affect access con')
- `cloud_storage_config.state` — arg_unmatched (security_impact=False, rationale='Output-only field indicating subscription health status; does not control access')
- `dead_letter_policy` — arg_unmatched (security_impact=True, rationale='Redirects undeliverable messages to another topic; if the dead letter topic has ')
- `expiration_policy` — arg_unmatched (security_impact=True, rationale='Controls when an inactive subscription expires; subscriptions that never expire ')
- `javascript_udf` — arg_unmatched (security_impact=True, rationale='Contains executable JavaScript code run against every message; poorly reviewed U')
- `message_transforms` — arg_unmatched (security_impact=True, rationale='Message transforms execute code within the data pipeline and can read, alter, or')
- `message_transforms.javascript_udf` — arg_unmatched (security_impact=True, rationale='JavaScript UDFs execute arbitrary code against every message in the pipeline; po')
- `no_wrapper` — arg_unmatched (security_impact=False, rationale='Controls HTTP payload wrapping for push delivery; does not affect authentication')
- `no_wrapper.write_metadata` — ambiguous (security_impact=False, rationale='Controls whether metadata is written as HTTP headers; does not affect authentica')
- `oidc_token` — arg_unmatched (security_impact=True, rationale='Contains OIDC authentication settings for push delivery; the service account and')
- `oidc_token.service_account_email` — ambiguous (security_impact=True, rationale='The service account whose identity is asserted in the OIDC JWT sent with each pu')
- `push_config` — arg_unmatched (security_impact=True, rationale='Configures push delivery to an external HTTP endpoint; the endpoint URL and auth')
- `push_config.no_wrapper` — arg_unmatched (security_impact=False, rationale='Controls HTTP payload wrapping format; does not affect authentication or access ')
- `push_config.oidc_token` — arg_unmatched (security_impact=True, rationale='Configures OIDC JWT authentication for push requests; without this, push message')
- `retry_policy` — arg_unmatched (security_impact=False, rationale='Controls message retry backoff timing; does not affect access controls or encryp')

### `pubsub_topic`
- `aws_kinesis` — arg_unmatched (security_impact=True, rationale='Contains cross-cloud authentication identifiers; the AWS role ARN and GCP servic')
- `aws_kinesis.aws_role_arn` — ambiguous (security_impact=True, rationale='The AWS IAM role delegated to GCP for cross-cloud authentication; overly permiss')
- `aws_kinesis.gcp_service_account` — ambiguous (security_impact=True, rationale='The GCP service account used to assume the AWS role; its bindings determine whic')
- `aws_msk` — arg_unmatched (security_impact=True, rationale='Contains cross-cloud authentication identifiers for AWS MSK federation; the role')
- `aws_msk.aws_role_arn` — ambiguous (security_impact=True, rationale='The AWS IAM role delegated to GCP; overly permissive roles grant unintended AWS ')
- `aws_msk.gcp_service_account` — ambiguous (security_impact=True, rationale='The GCP service account used to assume the AWS MSK role; its bindings determine ')
- `aws_msk.topic` — ambiguous (security_impact=False, rationale='Identifies the Kafka topic to ingest; does not control authentication or access ')
- `azure_event_hubs` — arg_unmatched (security_impact=True, rationale='Contains cross-cloud authentication identifiers for Azure federation; the client')
- `azure_event_hubs.gcp_service_account` — ambiguous (security_impact=True, rationale='The GCP service account used to authenticate with Azure; its bindings determine ')
- `cloud_storage` — arg_unmatched (security_impact=True, rationale="Configures ingestion from a Cloud Storage bucket; the bucket's IAM controls and ")
- `cloud_storage.avro_format` — arg_unmatched (security_impact=False, rationale='Specifies Avro binary format for ingested objects; does not control access, auth')
- `cloud_storage.pubsub_avro_format` — arg_unmatched (security_impact=False, rationale='Specifies Pub/Sub Avro format for restoring previously exported messages; does n')
- `cloud_storage.text_format` — arg_unmatched (security_impact=False, rationale='Specifies text parsing format for ingested objects; does not control access, aut')
- `confluent_cloud` — arg_unmatched (security_impact=True, rationale='Contains federated identity settings for Confluent Cloud ingestion; the identity')
- `confluent_cloud.gcp_service_account` — ambiguous (security_impact=True, rationale='The GCP service account used for Confluent Cloud federation; its bindings contro')
- `confluent_cloud.topic` — ambiguous (security_impact=False, rationale='Identifies the Kafka topic to ingest; does not control authentication or access ')
- `ingestion_data_source_settings` — arg_unmatched (security_impact=True, rationale='Configures cross-cloud data ingestion involving federated identity credentials a')
- `ingestion_data_source_settings.aws_kinesis` — arg_unmatched (security_impact=True, rationale='Configures ingestion from AWS Kinesis using federated IAM credentials; the AWS r')
- `ingestion_data_source_settings.aws_msk` — arg_unmatched (security_impact=True, rationale='Configures cross-cloud ingestion from AWS MSK using federated IAM credentials; t')
- `ingestion_data_source_settings.azure_event_hubs` — arg_unmatched (security_impact=True, rationale='Configures cross-cloud ingestion from Azure using federated identity; the client')
- `ingestion_data_source_settings.cloud_storage` — arg_unmatched (security_impact=True, rationale="Establishes a data flow from a Cloud Storage bucket into Pub/Sub; the bucket's a")
- `ingestion_data_source_settings.confluent_cloud` — arg_unmatched (security_impact=True, rationale='Configures ingestion from Confluent Cloud using a federated identity pool; the i')
- `ingestion_data_source_settings.platform_logs_settings` — arg_unmatched (security_impact=True, rationale='Controls whether audit logs are generated for ingestion operations; disabling or')
- `javascript_udf` — arg_unmatched (security_impact=True, rationale='Contains executable JavaScript code run against every message; poorly reviewed U')
- `message_storage_policy` — arg_unmatched (security_impact=True, rationale='Controls where message data is physically stored, which is critical for enforcin')
- `message_transforms` — arg_unmatched (security_impact=True, rationale='Message transforms execute code within the data pipeline and can read, alter, or')
- `message_transforms.javascript_udf` — arg_unmatched (security_impact=True, rationale='JavaScript UDFs execute arbitrary code against every message in the pipeline; po')
- `platform_logs_settings` — arg_unmatched (security_impact=True, rationale='Controls audit log generation for ingestion operations; insufficient logging red')
- `schema_settings` — arg_unmatched (security_impact=False, rationale='Schema settings enforce message format validation for data integrity; they do no')
- `text_format` — arg_unmatched (security_impact=False, rationale='Defines text parsing settings for Cloud Storage ingestion; does not control acce')

### `scc_event_threat_detection_custom_module`
- `config` — nonbool_string (security_impact='Improper or missing configuration can allow threats to bypass detection or limit visibility into malicious activity.')
- `enablement_state` — nonbool_string (security_impact='If disabled, the module will not detect or report threats, reducing the overall effectiveness of SCC.')
- `type` — nonbool_string (security_impact='Using an incorrect or unapproved type may lead to incomplete threat detection or false positives.')

### `scc_folder_custom_module`
- `custom_config` — arg_unmatched (security_impact='false')
- `custom_output` — arg_unmatched (security_impact='false')
- `enablement_state` — nonbool_string (security_impact='If disabled, the custom module will not generate findings, weakening the effectiveness of SCC monitoring.')
- `predicate` — arg_unmatched (security_impact='false')
- `properties` — arg_unmatched (security_impact='false')
- `resource_selector` — arg_unmatched (security_impact='false')
- `value_expression` — arg_unmatched (security_impact='false')

### `scc_mute_config`
- `filter` — nonbool_string (security_impact='Incorrect or overly broad filters may hide critical security findings, reducing visibility of threats.')
- `parent` — nonbool_string (security_impact='If the parent scope is misconfigured, the mute policy may not apply correctly, leading to gaps in security monitoring.')
- `type` — nonbool_string (security_impact='Not supported in Terraform — no security impact enforced through IaC.')

### `scc_notification_config`
- `config_id` — nonbool_string (security_impact='If config_id is not unique or poorly managed, it may overwrite or conflict with existing notification configurations, leading to loss of alerts.')
- `pubsub_topic` — nonbool_string (security_impact='If the Pub/Sub topic is misconfigured or points to an unapproved destination, security notifications may be lost or exposed to unauthorized subscribers.')
- `streaming_config` — arg_unmatched (security_impact='Improper filters may cause critical findings to be missed or irrelevant alerts to be generated, reducing monitoring effectiveness.', rationale='Streaming configuration must include properly scoped filters to ensure only rele')
- `streaming_config` — nonbool_string (security_impact='Improper filters may cause critical findings to be missed or irrelevant alerts to be generated, reducing monitoring effectiveness.')

### `scc_organization_custom_module`
- `custom_config` — arg_unmatched (security_impact='Not supported in Terraform helpers — cannot be validated through IaC.', rationale='This argument exists in API but is not supported by current Terraform helpers.')
- `custom_config` — nonbool_string (security_impact='Not supported in Terraform helpers — cannot be validated through IaC.')
- `enablement_state` — nonbool_string (security_impact='If disabled, the organization-level custom module will not generate findings, weakening SCC monitoring.')

### `scc_organization_scc_big_query_export`
- `big_query_export_id` — nonbool_string (security_impact='If not unique or not approved, exports may overwrite or conflict, leading to loss or corruption of findings.')
- `dataset` — nonbool_string (security_impact='If dataset is not properly configured or approved, findings may be written to insecure or unauthorized locations.')
- `organization` — nonbool_string (security_impact='If organization is not correctly set or outside the allowed list, exports may expose findings to unauthorized scopes.')

### `scc_project_custom_module`
- `custom_config` — arg_unmatched (security_impact='Not supported in Terraform helpers — cannot be validated through IaC.', rationale='This argument exists in the API but is not supported by current Terraform helper')
- `custom_config` — nonbool_string (security_impact='Not supported in Terraform helpers — cannot be validated through IaC.')
- `enablement_state` — nonbool_string (security_impact='If disabled, the project-level custom module will not generate findings, weakening SCC monitoring.')

### `scc_source_iam`
- `members` — nonbool_string (security_impact='Overly permissive members (e.g., allUsers) can expose the SCC source to the public, leading to serious security risks.')
- `role` — nonbool_string (security_impact='Assigning overly privileged roles (e.g., owner, admin) can lead to unauthorized changes or data exposure.')

### `sourcerepo_repository`
- `pubsub_configs` — arg_unmatched (security_impact=True, rationale='Enabling pubsub_configs allows for monitoring and audit logging of repository ch')

### `spanner_backup_schedule`
- `cron_spec` — arg_unmatched (security_impact=False)
- `encryption_config` — arg_unmatched (security_impact=False)
- `full_backup_spec` — arg_unmatched (security_impact=False)
- `incremental_backup_spec` — arg_unmatched (security_impact=False)
- `spec` — arg_unmatched (security_impact=False)
- `spec.cron_spec` — arg_unmatched (security_impact=False)

### `spanner_database_iam`
- `condition` — arg_unmatched (security_impact=True, rationale='IAM conditions allow fine-grained access control. Without conditions, bindings a')

### `spanner_instance`
- `asymmetric_autoscaling_options` — arg_unmatched (security_impact=False, rationale='Asymmetric autoscaling options affect performance but do not directly introduce ')
- `asymmetric_autoscaling_options.overrides` — arg_unmatched (security_impact=False, rationale='Overrides affect performance but do not directly introduce a security risk.')
- `asymmetric_autoscaling_options.replica_selection` — arg_unmatched (security_impact=False, rationale='Replica selection is required for asymmetric autoscaling but does not directly i')
- `autoscaling_config` — arg_unmatched (security_impact=False, rationale='Autoscaling configuration affects performance and cost but does not directly int')
- `autoscaling_config.asymmetric_autoscaling_options` — arg_unmatched (security_impact=False, rationale='Asymmetric autoscaling options affect performance but do not directly introduce ')
- `autoscaling_config.autoscaling_limits` — arg_unmatched (security_impact=False, rationale='Autoscaling limits affect performance and cost but do not directly introduce a s')
- `autoscaling_config.autoscaling_targets` — arg_unmatched (security_impact=False, rationale='Autoscaling targets affect performance and cost but do not directly introduce a ')
- `autoscaling_limits` — arg_unmatched (security_impact=False, rationale='Autoscaling limits affect performance and cost but do not directly introduce a s')
- `autoscaling_limits.max_nodes` — ambiguous (security_impact=False, rationale='Maximum nodes affect availability but do not directly introduce a security risk.')
- `autoscaling_limits.max_processing_units` — ambiguous (security_impact=False, rationale='Maximum processing units affect performance but do not directly introduce a secu')
- `autoscaling_limits.min_nodes` — ambiguous (security_impact=False, rationale='Minimum nodes affect availability but do not directly introduce a security risk.')
- `autoscaling_limits.min_processing_units` — ambiguous (security_impact=False, rationale='Minimum processing units affect performance but do not directly introduce a secu')
- `autoscaling_targets` — arg_unmatched (security_impact=False, rationale='Autoscaling targets affect performance and cost but do not directly introduce a ')
- `overrides` — arg_unmatched (security_impact=False, rationale='Overrides affect performance and cost but do not directly introduce a security r')
- `overrides.autoscaling_limits` — arg_unmatched (security_impact=False, rationale='Autoscaling limit overrides affect performance but do not directly introduce a s')
- `replica_selection` — arg_unmatched (security_impact=False, rationale='Replica selection affects performance but does not directly introduce a security')

### `spanner_instance_config`
- `replicas` — arg_unmatched (security_impact=False, rationale='Replica placement determines where data is physically stored. No standalone poli')

### `storage_batch_operations_job`
- `bucket_list` — arg_unmatched (security_impact=False, rationale="Bucket list configuration is required for batch operations but doesn't directly ")
- `bucket_list.buckets` — arg_unmatched (security_impact=False, rationale="Bucket configuration is required for batch operations but doesn't directly impac")
- `bucket_list.buckets.manifest` — arg_unmatched (security_impact=True, rationale='Manifest files provide explicit object lists, ensuring operations only affect in')
- `bucket_list.buckets.prefix_list` — arg_unmatched (security_impact=True, rationale='Without proper prefix scoping, operations may affect unintended objects in the b')
- `delete_object` — arg_unmatched (security_impact=True, rationale='Permanent deletion can cause data loss if misused. Policy ensures delete safety.')
- `put_metadata` — arg_unmatched (security_impact=False, rationale="Metadata updates are generally safe operations that don't affect data integrity ")
- `put_object_hold` — arg_unmatched (security_impact=True, rationale='Unsetting holds can weaken data retention and allow premature deletion.')
- `rewrite_object` — arg_unmatched (security_impact=True, rationale='All rewrites must use CMEK to ensure customer-controlled encryption.')

### `storage_bucket`
- `cors` — arg_unmatched (security_impact=True, rationale='Allowing the wildcard * for CORS methods exposes sensitive operations to any web')
- `encryption` — arg_unmatched (security_impact=True, rationale='Encryption to keep the data of bucket secure')
- `ip_filter` — arg_unmatched (security_impact=True, rationale='Make sure that ip exposure is not too broad')

### `storage_bucket_object`
- `customer_encryption` — arg_unmatched (security_impact=True, rationale='Encryption should be present either through customer or using kms for data secur')

### `storage_control_folder_intelligence_config`
- `excluded_cloud_storage_buckets` — arg_unmatched (security_impact=False, rationale='This is a nested block supporting the filter argument. Excluding buckets from mo')
- `excluded_cloud_storage_buckets.bucket_id_regexes` — ambiguous (security_impact=False, rationale='Bucket exclusion regexes are operational scoping patterns. Without defined organ')
- `excluded_cloud_storage_locations` — arg_unmatched (security_impact=False, rationale='This is a nested block supporting the filter argument. Excluding locations from ')
- `excluded_cloud_storage_locations.locations` — ambiguous (security_impact=False, rationale='Location exclusion values are operational scope decisions. They do not enforce d')
- `filter` — arg_unmatched (security_impact=True, rationale='The filter block controls the scope of Storage Intelligence monitoring. Misconfi')
- `filter.excluded_cloud_storage_buckets` — arg_unmatched (security_impact=False)
- `filter.excluded_cloud_storage_locations` — arg_unmatched (security_impact=False, rationale='Excluding locations from Storage Intelligence monitoring is an operational and c')
- `filter.included_cloud_storage_buckets` — arg_unmatched (security_impact=False, rationale='Including specific buckets in Storage Intelligence is an operational scoping dec')
- `filter.included_cloud_storage_locations` — arg_unmatched (security_impact=True, rationale='Restricting Storage Intelligence monitoring to approved regions enforces data so')
- `included_cloud_storage_buckets` — arg_unmatched (security_impact=False, rationale='This is a nested block supporting the filter argument. Including specific bucket')
- `included_cloud_storage_buckets.bucket_id_regexes` — ambiguous (security_impact=False, rationale='Bucket inclusion regexes are operational scoping patterns. Without defined organ')
- `included_cloud_storage_locations` — arg_unmatched (security_impact=True, rationale='This nested block enforces data sovereignty by restricting Storage Intelligence ')
- `included_cloud_storage_locations.locations` — ambiguous (security_impact=True, rationale='Storage Intelligence must only monitor buckets within approved Australian region')

### `storage_control_organization_intelligence_config`
- `excluded_cloud_storage_buckets` — arg_unmatched (security_impact=False, rationale='This is a nested block supporting the filter argument. Bucket exclusions are ope')
- `excluded_cloud_storage_buckets.bucket_id_regexes` — ambiguous (security_impact=False, rationale='Regex patterns for bucket exclusions are operational scoping values. No security')
- `excluded_cloud_storage_locations` — arg_unmatched (security_impact=False, rationale='This is a nested block supporting the filter argument. Location exclusions are o')
- `excluded_cloud_storage_locations.locations` — ambiguous (security_impact=False, rationale='The list of excluded locations is an operational scoping value. No security poli')
- `filter` — arg_unmatched (security_impact=True, rationale='The filter block controls the scope of Storage Intelligence monitoring at the or')
- `filter.excluded_cloud_storage_buckets` — arg_unmatched (security_impact=False, rationale='Excluding specific buckets from Storage Intelligence monitoring is an operationa')
- `filter.excluded_cloud_storage_locations` — arg_unmatched (security_impact=False, rationale='Excluding locations from Storage Intelligence is an operational and cost managem')
- `filter.included_cloud_storage_buckets` — arg_unmatched (security_impact=False, rationale='Including specific buckets is an operational scoping decision and does not direc')
- `filter.included_cloud_storage_locations` — arg_unmatched (security_impact=True, rationale='Restricting Storage Intelligence monitoring to approved regions enforces data so')
- `included_cloud_storage_buckets` — arg_unmatched (security_impact=False, rationale='This is a nested block supporting the filter argument. Bucket inclusions are ope')
- `included_cloud_storage_buckets.bucket_id_regexes` — ambiguous (security_impact=False, rationale='Regex patterns for bucket inclusions are operational scoping values. No security')
- `included_cloud_storage_locations` — arg_unmatched (security_impact=True, rationale='This nested block directly supports the data sovereignty policy. Storage Intelli')
- `included_cloud_storage_locations.locations` — ambiguous (security_impact=True, rationale='The locations list must only contain approved regions. Approved regions are aust')

### `storage_control_project_intelligence_config`
- `excluded_cloud_storage_buckets` — arg_unmatched (security_impact=False, rationale='This is a nested block supporting the filter argument. Bucket exclusions are ope')
- `excluded_cloud_storage_buckets.bucket_id_regexes` — ambiguous (security_impact=False, rationale='Regex patterns for bucket exclusions are operational scoping values. No security')
- `excluded_cloud_storage_locations` — arg_unmatched (security_impact=False, rationale='This is a nested block supporting the filter argument. Location exclusions are o')
- `excluded_cloud_storage_locations.locations` — ambiguous (security_impact=False, rationale='The list of excluded locations is an operational scoping value at the project le')
- `filter` — arg_unmatched (security_impact=False, rationale='At the project level, location filtering is not considered a security policy bec')
- `filter.excluded_cloud_storage_buckets` — arg_unmatched (security_impact=False, rationale='Excluding specific buckets from Storage Intelligence monitoring is an operationa')
- `filter.excluded_cloud_storage_locations` — arg_unmatched (security_impact=False, rationale='Excluding locations is an operational and cost management decision at the projec')
- `filter.included_cloud_storage_buckets` — arg_unmatched (security_impact=False, rationale='Including specific buckets is an operational scoping decision. It does not direc')
- `filter.included_cloud_storage_locations` — arg_unmatched (security_impact=False, rationale='At the project level, location restrictions are inherited from the parent folder')
- `included_cloud_storage_buckets` — arg_unmatched (security_impact=False, rationale='This is a nested block supporting the filter argument. Bucket inclusions are ope')
- `included_cloud_storage_buckets.bucket_id_regexes` — ambiguous (security_impact=False, rationale='Regex patterns for bucket inclusions are operational scoping values. No security')
- `included_cloud_storage_locations` — arg_unmatched (security_impact=False, rationale='This nested block is not considered a security policy at the project level. Loca')
- `included_cloud_storage_locations.locations` — ambiguous (security_impact=False, rationale='Location values at the project level are not enforced as a security policy. Data')

### `storage_insights_dataset_config`
- `cloud_storage_buckets` — arg_unmatched (security_impact=False, rationale='Cloud storage bucket settings are mainly used for resource selection and filteri')
- `cloud_storage_buckets.bucket_name` — ambiguous (security_impact=False, rationale='Bucket names are resource identifiers used for configuration purposes and do not')
- `cloud_storage_buckets.bucket_prefix_regex` — ambiguous (security_impact=False, rationale='Regex patterns are used for bucket matching and filtering purposes and do not di')
- `exclude_cloud_storage_buckets` — arg_unmatched (security_impact=False, rationale='Bucket exclusion settings are mainly used for filtering resources and do not dir')
- `exclude_cloud_storage_buckets.cloud_storage_buckets` — arg_unmatched (security_impact=False, rationale='Excluded bucket configurations are operational filtering settings and do not dir')
- `exclude_cloud_storage_locations` — arg_unmatched (security_impact=False, rationale='Excluded storage locations are mainly used for filtering resources and do not di')
- `identity` — arg_unmatched (security_impact=True, rationale='Identity configuration affects how permissions and access are managed for the Da')
- `identity.name` — arg_unmatched (security_impact=False, rationale='The identity name is an informational output field and does not influence securi')
- `include_cloud_storage_buckets` — arg_unmatched (security_impact=False, rationale='Bucket inclusion settings are mainly used for selecting resources and do not dir')
- `include_cloud_storage_buckets.cloud_storage_buckets` — arg_unmatched (security_impact=False, rationale='Included bucket settings are operational resource selection configurations and d')
- `include_cloud_storage_locations` — arg_unmatched (security_impact=True, rationale='Cloud storage location selection can affect data residency, compliance, and regu')
- `source_folders` — arg_unmatched (security_impact=False, rationale='Source folder settings are mainly used for resource grouping and do not directly')
- `source_projects` — arg_unmatched (security_impact=False, rationale='Source project settings are used for selecting resources and do not directly enf')

### `storage_insights_report_config`
- `csv_options` — arg_unmatched (security_impact=True, rationale='CSV formatting options mainly affect report readability and file structure and d')
- `end_date` — arg_unmatched (security_impact=False, rationale='The end date configuration is used for scheduling report generation and does not')
- `end_date.day` — ambiguous (security_impact=False, rationale='The day value is part of scheduling configuration and does not directly impact s')
- `end_date.month` — ambiguous (security_impact=False, rationale='The month value is used for scheduling purposes only and does not directly affec')
- `end_date.year` — ambiguous (security_impact=False, rationale='The year value is part of report scheduling and does not directly impact access ')
- `frequency_options` — arg_unmatched (security_impact=True, rationale='Report generation frequency can impact monitoring effectiveness, operational vis')
- `frequency_options.end_date` — arg_unmatched (security_impact=False, rationale='The end date is used for scheduling report generation and does not directly impa')
- `frequency_options.start_date` — arg_unmatched (security_impact=False, rationale='The start date is a scheduling configuration and does not directly affect authen')
- `object_metadata_report_options` — arg_unmatched (security_impact=True, rationale='Metadata reporting configurations can impact exposure of sensitive storage infor')
- `object_metadata_report_options.storage_destination_options` — arg_unmatched (security_impact=False, rationale='Report storage destinations can impact data protection, access control, and comp')
- `object_metadata_report_options.storage_filters` — arg_unmatched (security_impact=False, rationale='Storage filter settings are mainly used for resource selection and do not direct')
- `parquet_options` — arg_unmatched (security_impact=False, rationale='Parquet output settings mainly control report file formatting and storage effici')
- `start_date` — arg_unmatched (security_impact=False, rationale='The start date configuration is used for scheduling report generation and does n')
- `start_date.day` — ambiguous (security_impact=False, rationale='The day value is part of report scheduling and does not directly impact security')
- `start_date.month` — ambiguous (security_impact=False, rationale='The month value is used for scheduling purposes only and does not directly affec')
- `start_date.year` — ambiguous (security_impact=False, rationale='The year value is part of scheduling configuration and does not directly impact ')
- `storage_destination_options` — arg_unmatched (security_impact=True, rationale='Storage destination settings affect where inventory reports are stored and can i')
- `storage_destination_options.bucket` — ambiguous (security_impact=True, rationale='Inventory reports should be stored only in approved secure destination buckets.')
- `storage_filters` — arg_unmatched (security_impact=True, rationale='Storage filter settings determine which storage resources are included in invent')
- `storage_filters.bucket` — ambiguous (security_impact=True, rationale='Reports should only be generated from trusted and approved source buckets.')

### `vpc_access_connector`
- `subnet` — arg_unmatched (security_impact=True, rationale='Enforcing specific subnets allows for better network isolation and security cont')

### `workbench_instance`
- `accelerator_configs` — arg_unmatched (security_impact=False, rationale='This is a container block for GPU accelerator configuration. It has no direct se')
- `access_configs` — arg_unmatched (security_impact=False, rationale='This is a container block for external IP configuration. Security impact is dete')
- `boot_disk` — arg_unmatched (security_impact=False, rationale='This is a container block for boot disk configuration. Security impact is determ')
- `boot_disk.disk_encryption` — ambiguous (security_impact=True, rationale='The default (GMEK) provides no key revocation capability during incidents. CMEK ')
- `boot_disk.disk_size_gb` — ambiguous (security_impact=False, rationale='Specifies disk capacity. This is a storage sizing attribute with no security imp')
- `boot_disk.disk_type` — ambiguous (security_impact=False, rationale='Specifies disk performance class. This is a storage performance attribute with n')
- `boot_disk.kms_key` — ambiguous (security_impact=True, rationale='Required when disk_encryption is CMEK. Without a KMS key, CMEK cannot be applied')
- `confidential_instance_config` — arg_unmatched (security_impact=False, rationale='This is a container block for Confidential VM configuration. Security impact is ')
- `container_image` — arg_unmatched (security_impact=False, rationale='This is a container block for container image selection. It has no direct securi')
- `data_disks` — arg_unmatched (security_impact=False, rationale='This is a container block for data disk configuration. Security impact is determ')
- `data_disks.disk_encryption` — ambiguous (security_impact=True, rationale='Data disks store notebooks, datasets, and model outputs which may contain sensit')
- `data_disks.disk_size_gb` — ambiguous (security_impact=False, rationale='Specifies disk capacity. This is a storage sizing attribute with no security imp')
- `data_disks.disk_type` — ambiguous (security_impact=False, rationale='Specifies disk performance class. This is a storage performance attribute with n')
- `data_disks.kms_key` — ambiguous (security_impact=True, rationale='Required when disk_encryption is CMEK. Without a KMS key, CMEK cannot be applied')
- `gce_setup` — arg_unmatched (security_impact=False, rationale='This is a container block for VM configuration. Security impact is determined by')
- `gce_setup.accelerator_configs` — arg_unmatched (security_impact=False, rationale='This is a container block for GPU configuration. It controls hardware accelerati')
- `gce_setup.boot_disk` — arg_unmatched (security_impact=False, rationale='This is a container block for boot disk configuration. Security impact is determ')
- `gce_setup.confidential_instance_config` — arg_unmatched (security_impact=False, rationale='This is a container block for Confidential VM configuration. Security impact is ')
- `gce_setup.container_image` — arg_unmatched (security_impact=False, rationale='This is a container block for container image selection. Image governance is man')
- `gce_setup.data_disks` — arg_unmatched (security_impact=False, rationale='This is a container block for data disk configuration. Security impact is determ')
- `gce_setup.network_interfaces` — arg_unmatched (security_impact=False, rationale='This is a container block for network configuration. Security impact is determin')
- `gce_setup.reservation_affinity` — arg_unmatched (security_impact=False, rationale='This is a container block for capacity reservation scheduling. It has no securit')
- `gce_setup.service_accounts` — arg_unmatched (security_impact=False, rationale='This is a container block for service account configuration. Security impact is ')
- `gce_setup.shielded_instance_config` — arg_unmatched (security_impact=False, rationale='This is a container block for Shielded VM settings. Security impact is determine')
- `gce_setup.vm_image` — arg_unmatched (security_impact=False, rationale='This is a container block for VM image selection. Image governance is managed at')
- `network_interfaces` — arg_unmatched (security_impact=False, rationale='This is a container block for network configuration. Security impact is determin')
- `network_interfaces.access_configs` — arg_unmatched (security_impact=True, rationale='Assigns a static external IP to the instance, making it persistently and predict')
- `reservation_affinity` — arg_unmatched (security_impact=False, rationale='This is a container block for capacity reservation scheduling. It has no securit')
- `service_accounts` — arg_unmatched (security_impact=False, rationale='This is a container block for service account configuration. Security impact is ')
- `service_accounts.scopes` — arg_unmatched (security_impact=False, rationale='This is an output-only attribute set automatically by GCP to cloud-platform scop')
- `shielded_instance_config` — arg_unmatched (security_impact=False, rationale='This is a container block for Shielded VM settings. Security impact is determine')
- `vm_image` — arg_unmatched (security_impact=False, rationale='This is a container block for VM image selection. It has no direct security impa')
- `vm_image.name` — ambiguous (security_impact=False, rationale='Specifies the VM image name. This is a functional attribute for image selection ')
- `vm_image.project` — ambiguous (security_impact=False, rationale='Specifies which project owns the VM image. Image governance is managed at the re')

### `workstations_workstation_cluster`
- `domain_config` — arg_unmatched (security_impact=False, rationale='Custom domain configuration affects HTTP ingress naming, but this policy set doe')
- `private_cluster_config` — arg_unmatched (security_impact=False)
- `private_cluster_config.cluster_hostname` — arg_unmatched (security_impact=False, rationale='This is an output value populated when private endpoint is enabled and is not di')
- `private_cluster_config.service_attachment_uri` — arg_unmatched (security_impact=False, rationale='This is an output value related to private endpoint configuration and is not dir')

### `workstations_workstation_config`
- `container` — arg_unmatched (security_impact=False)
- `encryption_key` — arg_unmatched (security_impact=False, rationale='Customer-managed encryption can improve control over disk encryption, but this p')
- `ephemeral_directories` — arg_unmatched (security_impact=False)
- `gce_instance.accelerators` — arg_unmatched (security_impact=False)
- `gce_instance.boost_configs` — arg_unmatched (security_impact=False)
- `gce_instance.confidential_instance_config` — arg_unmatched (security_impact=False)
- `gce_instance.enable_nested_virtualization` — ambiguous (security_impact=False)
- `gce_instance.shielded_instance_config` — arg_unmatched (security_impact=False)
- `host` — arg_unmatched (security_impact=True, rationale='The host block controls the runtime environment for workstations, including Comp')
- `host.gce_instance` — arg_unmatched (security_impact=True, rationale='GCE instance settings define the machine type and network exposure of the workst')
- `persistent_directories` — arg_unmatched (security_impact=False)
- `readiness_checks` — arg_unmatched (security_impact=False)

## Parse errors

- `Looker_(Google_Cloud_core)/resource_json/looker_instance.json` — 'utf-8' codec can't decode byte 0xff in position 0: invalid start byte
