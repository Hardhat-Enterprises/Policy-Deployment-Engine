{
  "last_updated": "2026-06-23T08:26:44Z",
  "provider_version": "7.37.0",
  "arguments": {
    "deletion_policy": {
      "description": "Whether Terraform will be prevented from destroying the instance. Defaults to \"DELETE\".\nWhen a 'terraform destroy' or 'terraform apply' would delete the instance,\nthe command will fail if this field is set to \"PREVENT\" in Terraform state.\nWhen set to \"ABANDON\", the command will remove the resource from Terraform\nmanagement without updating or deleting the resource in the API.\nWhen set to \"DELETE\", deleting the resource is allowed.\n",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "Controls how Terraform handles removal of the target gRPC proxy. ABANDON removes the live resource from Terraform management without deleting it, which can leave unmanaged infrastructure outside IaC governance. DELETE remains valid for intentional teardown, while PREVENT may be used when retention is required."
    },
    "description": {
      "description": "An optional description of this resource.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "This is descriptive metadata only and does not directly affect the security behaviour or configuration of the resource."
    },
    "name": {
      "description": "Name of the resource. Provided by the client when the resource\nis created. The name must be 1-63 characters long, and comply\nwith RFC1035. Specifically, the name must be 1-63 characters long\nand match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?' which\nmeans the first character must be a lowercase letter, and all\nfollowing characters must be a dash, lowercase letter, or digit,\nexcept the last character, which cannot be a dash.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "The resource name is an identifier and does not directly control the security behaviour of the gRPC proxy."
    },
    "project": {
      "description": "The ID of the project in which the resource belongs. If it is not provided, the provider project is used.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "This identifies the project containing the resource. Project identifiers are team-specific scope references and cannot be generalised into a platform-wide security policy."
    },
    "url_map": {
      "description": "URL to the UrlMap resource that defines the mapping from URL to\nthe BackendService. The protocol field in the BackendService\nmust be set to GRPC.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "This is a reference to a specific UrlMap resource. Resource references are not considered security-impacting because policy should not constrain which specific URL map a team references."
    },
    "validate_for_proxyless": {
      "description": "If true, indicates that the BackendServices referenced by\nthe urlMap may be accessed by gRPC applications without using\na sidecar proxy. This will enable configuration checks on urlMap\nand its referenced BackendServices to not allow unsupported features.\nA gRPC application must use \"xds:///\" scheme in the target URI\nof the service it is connecting to. If false, indicates that the\nBackendServices referenced by the urlMap will be accessed by gRPC\napplications via a sidecar proxy. In this case, a gRPC application\nmust not use \"xds:///\" scheme in the target URI of the service\nit is connecting to",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "Controls whether the target gRPC proxy configuration is validated for proxyless gRPC operation. This is an architectural and functional routing configuration setting rather than a security baseline control, so it is not classified as security impacting."
    }
  }
}
