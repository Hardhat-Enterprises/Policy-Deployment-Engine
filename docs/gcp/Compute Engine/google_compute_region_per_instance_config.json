{
  "last_updated": "2026-06-23T08:26:44Z",
  "provider_version": "7.37.0",
  "arguments": {
    "deletion_policy": {
      "description": "Whether Terraform will be prevented from destroying the instance. Defaults to \"DELETE\".\nWhen a 'terraform destroy' or 'terraform apply' would delete the instance,\nthe command will fail if this field is set to \"PREVENT\" in Terraform state.\nWhen set to \"ABANDON\", the command will remove the resource from Terraform\nmanagement without updating or deleting the resource in the API.\nWhen set to \"DELETE\", deleting the resource is allowed.\n",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "Should be set to PREVENT to ensure that the instance is not accidentally configured to destroy the instance"
    },
    "minimal_action": {
      "description": "The minimal action to perform on the instance during an update.\nDefault is 'NONE'. Possible values are:\n* REPLACE\n* RESTART\n* REFRESH\n* NONE",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Main purpose is to dictate a specific action which none have an affect of the security of the resource thus is mainly configuration controls"
    },
    "most_disruptive_allowed_action": {
      "description": "The most disruptive action to perform on the instance during an update.\nDefault is 'REPLACE'. Possible values are:\n* REPLACE\n* RESTART\n* REFRESH\n* NONE",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Does not have an effect on security principles as the configuration controls allowed for the most disruptive action would not effect the security of the resource"
    },
    "name": {
      "description": "The name for this per-instance config and its corresponding instance.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "Only used for naming conventions so does not have an impact on the security of the resource"
    },
    "project": {
      "description": "The ID of the project in which the resource belongs. If it is not provided, the provider project is used.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Only used as an idenification for the resource thus not related to security aspects"
    },
    "region": {
      "description": "Region where the containing instance group manager is located",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "Determines the region/zone where the resource is created. Policy should restrict it to an approved-region whitelist for data residency; the allowed set is parameterised (a hardcoded example whitelist is acceptable)."
    },
    "region_instance_group_manager": {
      "description": "The region instance group manager this instance config is part of.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "Is used as a refernce for the configuration thus does not have an effect on the security aspects"
    },
    "remove_instance_on_destroy": {
      "description": "When true, deleting this config will immediately remove the underlying instance.\nWhen false, deleting this config will use the behavior as determined by remove_instance_on_destroy.",
      "required": false,
      "type": "bool",
      "security_impact": true,
      "rationale": "Should be set to false to avoid unintentional deletion of the instance"
    },
    "remove_instance_state_on_destroy": {
      "description": "When true, deleting this config will immediately remove any specified state from the underlying instance.\nWhen false, deleting this config will *not* immediately remove any state from the underlying instance.\nState will be removed on the next instance recreation or update.",
      "required": false,
      "type": "bool",
      "security_impact": true,
      "rationale": "Controls whether the state of the instance is removed thus should be set to false to protect from unintended deleation of state to uphold data integrity of state"
    },
    "preserved_state": {
      "type": "block",
      "description": "The preserved state for this instance.",
      "required": false
    },
    "preserved_state.metadata": {
      "description": "Preserved metadata defined for this instance. This is a list of key->value pairs.",
      "required": false,
      "type": "map(string)",
      "security_impact": true,
      "rationale": "Metadata can hold sensitive information in plaintext thus a policy would be needed to enforce credential hygiene by blacklisting keywords that could disclose plaintext secrets"
    },
    "preserved_state.disk": {
      "type": "block",
      "description": "Stateful disks for the instance.",
      "required": false
    },
    "preserved_state.disk.delete_rule": {
      "description": "A value that prescribes what should happen to the stateful disk when the VM instance is deleted.\nThe available options are 'NEVER' and 'ON_PERMANENT_INSTANCE_DELETION'.\n'NEVER' - detach the disk when the VM is deleted, but do not delete the disk.\n'ON_PERMANENT_INSTANCE_DELETION' will delete the stateful disk when the VM is permanently\ndeleted from the instance group. Default value: \"NEVER\" Possible values: [\"NEVER\", \"ON_PERMANENT_INSTANCE_DELETION\"]",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "This is a operational data handling control not a security control as it a preference for the user on how stateful disks are handled on instance deletion"
    },
    "preserved_state.disk.device_name": {
      "description": "A unique device name that is reflected into the /dev/ tree of a Linux operating system running within the instance.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "Only used for a naming convention for disk device name which does not have an effect on the security of the resource"
    },
    "preserved_state.disk.mode": {
      "description": "The mode of the disk. Default value: \"READ_WRITE\" Possible values: [\"READ_ONLY\", \"READ_WRITE\"]",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "The chosen state of the mode for the disk is an operational control for the user not a security control as it would be too restriciting to have READ_ONLY if they need writing access"
    },
    "preserved_state.disk.source": {
      "description": "The URI of an existing persistent disk to attach under the specified device-name in the format\n'projects/project-id/zones/zone/disks/disk-name'.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "Used as an identifier that points to the URI thus is a operational control not a security control"
    },
    "preserved_state.external_ip": {
      "type": "block",
      "description": "Preserved external IPs defined for this instance. This map is keyed with the name of the network interface.",
      "required": false
    },
    "preserved_state.external_ip.auto_delete": {
      "description": "These stateful IPs will never be released during autohealing, update or VM instance recreate operations. This flag is used to configure if the IP reservation should be deleted after it is no longer used by the group, e.g. when the given instance or the whole group is deleted. Default value: \"NEVER\" Possible values: [\"NEVER\", \"ON_PERMANENT_INSTANCE_DELETION\"]",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Purpose is that it allows for the IP reservation to persist after the instance of the group is deleted which is a process that is apart of the IP address life cycle and cleanup settings, resulting in making it operational control not a security control"
    },
    "preserved_state.external_ip.interface_name": {
      "description": "The identifier for this object. Format specified above.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "Purpose is to be an means of identification thus not related to the security of the resource"
    },
    "preserved_state.external_ip.ip_address": {
      "type": "block",
      "description": "Ip address representation",
      "required": false
    },
    "preserved_state.external_ip.ip_address.address": {
      "description": "The URL of the reservation for this IP address.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Purpose is to keep the same external IP address associated with the reservation too for reuse, because it is a configuration pointer not an exposure control we do not have to pin the specfic IP addresses"
    },
    "preserved_state.internal_ip": {
      "type": "block",
      "description": "Preserved internal IPs defined for this instance. This map is keyed with the name of the network interface.",
      "required": false
    },
    "preserved_state.internal_ip.auto_delete": {
      "description": "These stateful IPs will never be released during autohealing, update or VM instance recreate operations. This flag is used to configure if the IP reservation should be deleted after it is no longer used by the group, e.g. when the given instance or the whole group is deleted. Default value: \"NEVER\" Possible values: [\"NEVER\", \"ON_PERMANENT_INSTANCE_DELETION\"]",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Stateful IPs are within the cloud thus are not public so it should not effect the security of the resource or principles"
    },
    "preserved_state.internal_ip.interface_name": {
      "description": "The identifier for this object. Format specified above.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "Purpose is to be an means of identification thus not related to the security of the resource"
    },
    "preserved_state.internal_ip.ip_address": {
      "type": "block",
      "description": "Ip address representation",
      "required": false
    },
    "preserved_state.internal_ip.ip_address.address": {
      "description": "The URL of the reservation for this IP address.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Specifies an internal IP address to reserve for reuse, in which it would not be suitable for policy creation as there would need to be hardcoding for the IP addresses."
    }
  }
}
