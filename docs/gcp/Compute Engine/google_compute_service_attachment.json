{
  "last_updated": "2026-06-23T08:26:44Z",
  "provider_version": "7.37.0",
  "arguments": {
    "connection_preference": {
      "description": "The connection preference to use for this service attachment. Valid\nvalues include \"ACCEPT_AUTOMATIC\", \"ACCEPT_MANUAL\".",
      "required": true,
      "type": "string",
      "security_impact": true,
      "rationale": "Should be set to ACCEPT_MANUAL to ensure that only intentional connections are made for this service "
    },
    "consumer_reject_lists": {
      "description": "An array of projects that are not allowed to connect to this service\nattachment.",
      "required": false,
      "type": "list(string)",
      "security_impact": true,
      "rationale": "Defines a list using specific project identifiers that are denied access to the service which must be intentionally configured correctly in order to prevent network exposure"
    },
    "deletion_policy": {
      "description": "Whether Terraform will be prevented from destroying the instance. Defaults to \"DELETE\".\nWhen a 'terraform destroy' or 'terraform apply' would delete the instance,\nthe command will fail if this field is set to \"PREVENT\" in Terraform state.\nWhen set to \"ABANDON\", the command will remove the resource from Terraform\nmanagement without updating or deleting the resource in the API.\nWhen set to \"DELETE\", deleting the resource is allowed.\n",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "Must be set to PREVENT to stop unintentional or malicious deletion of this instance"
    },
    "description": {
      "description": "An optional description of this resource.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Only purpose is to provide descriptive context of this resource to readers thus it would not have significant effect on the security of this resource"
    },
    "domain_names": {
      "description": "If specified, the domain name will be used during the integration between\nthe PSC connected endpoints and the Cloud DNS. For example, this is a\nvalid domain name: \"p.mycompany.com.\". Current max number of domain names\nsupported is 1.",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "domain_names is a configuration control not a security control as it's purpose is for DNS identification "
    },
    "enable_proxy_protocol": {
      "description": "If true, enable the proxy protocol which is for supplying client TCP/IP\naddress data in TCP connections that traverse proxies on their way to\ndestination servers.",
      "required": true,
      "type": "bool",
      "security_impact": true,
      "rationale": "Preserves original IP client visibility, thus is needed to be enabled in order to allow backend services to client IPs and support auditing and security posture"
    },
    "name": {
      "description": "Name of the resource. The name must be 1-63 characters long, and\ncomply with RFC1035. Specifically, the name must be 1-63 characters\nlong and match the regular expression '[a-z]([-a-z0-9]*[a-z0-9])?'\nwhich means the first character must be a lowercase letter, and all\nfollowing characters must be a dash, lowercase letter, or digit,\nexcept the last character, which cannot be a dash.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "Only purpose is for naming conventions which does not affect the security aspects of the resource"
    },
    "nat_subnets": {
      "description": "An array of subnets that is provided for NAT in this service attachment.",
      "required": true,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "This is a configuration control not a security control as it's whose IP address is used for consumer traffic for the service attachment, meaning the address range is networking choice that does not impact the security of the service"
    },
    "project": {
      "description": "The ID of the project in which the resource belongs. If it is not provided, the provider project is used.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Purpose is for identification for the resource which does not effect the security aspects"
    },
    "propagated_connection_limit": {
      "description": "The number of consumer spokes that connected Private Service Connect endpoints can be propagated to through Network Connectivity Center.\nThis limit lets the service producer limit how many propagated Private Service Connect connections can be established to this service attachment from a single consumer.\n\nIf the connection preference of the service attachment is ACCEPT_MANUAL, the limit applies to each project or network that is listed in the consumer accept list.\nIf the connection preference of the service attachment is ACCEPT_AUTOMATIC, the limit applies to each project that contains a connected endpoint.\n\nIf unspecified, the default propagated connection limit is 250. To explicitly send a zero value, set 'send_propagated_connection_limit_if_zero = true'.",
      "required": false,
      "type": "number",
      "security_impact": true,
      "rationale": "Defines the limit for the number of connected Private Service Connect endpoints thus an upper limit must be configured in order to address resource exhaustion issues and DoS exposure"
    },
    "reconcile_connections": {
      "description": "This flag determines whether a consumer accept/reject list change can reconcile the statuses of existing ACCEPTED or REJECTED PSC endpoints.\n\nIf false, connection policy update will only affect existing PENDING PSC endpoints. Existing ACCEPTED/REJECTED endpoints will remain untouched regardless how the connection policy is modified .\nIf true, update will affect both PENDING and ACCEPTED/REJECTED PSC endpoints. For example, an ACCEPTED PSC endpoint will be moved to REJECTED if its project is added to the reject list.",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "Purpose is to determine if consumers access changes are made from the existing connections thus making it an operational toggle not security control as it determines how the connection states are handled"
    },
    "region": {
      "description": "URL of the region where the resource resides.",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "Determines the region/zone where the resource is created. Policy should restrict it to an approved-region whitelist for data residency; the allowed set is parameterised (a hardcoded example whitelist is acceptable)."
    },
    "send_propagated_connection_limit_if_zero": {
      "description": "Controls the behavior of propagated_connection_limit.\nWhen false, setting propagated_connection_limit to zero causes the provider to use to the API's default value.\nWhen true, the provider will set propagated_connection_limit to zero.\nDefaults to false.",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "Purpose is to designate if the propagated_connection_limit is either 0 or the default value thus is configuration control for connection limits and not a security control"
    },
    "show_nat_ips": {
      "description": "NOTE: This field is temporarily non-functional due to an underlying API issue.\nAny value provided here will be ignored until the API issue is resolved, expected around 2026-03.\n[If true, show NAT IPs of all connected endpoints.]",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "Dictates the display of the NAT IPs which is a descriptive and display preference and not a security related control"
    },
    "target_service": {
      "description": "The URL of a service serving the endpoint identified by this service attachment.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "Used to designate the service's endpoint which is a identifier to another resource not security"
    },
    "consumer_accept_lists": {
      "type": "block",
      "description": "An array of projects that are allowed to connect to this service\nattachment.",
      "required": false
    },
    "consumer_accept_lists.connection_limit": {
      "description": "The number of consumer forwarding rules the consumer project can\ncreate.",
      "required": true,
      "type": "number",
      "security_impact": false,
      "rationale": "Main purpose is to dictate a limit to the amount of forwarding a consumer can create thus is a operation limit control not a security control"
    },
    "consumer_accept_lists.endpoint_url": {
      "description": "The endpoint that is allowed to connect to this service attachment.\nOnly one of project_id_or_num, network_url and endpoint_url may be set.",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "Contains a reference to a consumer endpoint that specifies what external resource is allowed to connect to the service attachment, and it must be non empty to ensure intentional allowlisting due to exposure allowlists having the ability to gate who can reach the privat resource"
    },
    "consumer_accept_lists.network_url": {
      "description": "The network that is allowed to connect to this service attachment.\nOnly one of project_id_or_num and network_url may be set.",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "Defines a list for what consumer networks are allowed to access thus the field should be non-empty to support intentional allowlisting for connection to the service attachment"
    },
    "consumer_accept_lists.project_id_or_num": {
      "description": "A project that is allowed to connect to this service attachment.\nOnly one of project_id_or_num and network_url may be set.",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "This field is a single entry that allows the identified project to connect to the service attachment, thus unintentional values could expand the amount of connections allowed making it so that untended parties are able to gain permission"
    }
  }
}
