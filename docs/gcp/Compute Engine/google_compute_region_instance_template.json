{
  "last_updated": "2026-06-23T08:26:44Z",
  "provider_version": "7.37.0",
  "arguments": {
    "can_ip_forward": {
      "description": "Whether to allow sending and receiving of packets with non-matching source or destination IPs. This defaults to false.",
      "required": false,
      "type": "bool",
      "security_impact": true,
      "rationale": "Allows instances to send or receive packets whose source or destination IP does not match the instance. This can enable routing or packet-forwarding behaviour, so a generic policy can require it to remain false unless explicitly approved."
    },
    "deletion_policy": {
      "description": "Whether Terraform will be prevented from destroying the instance. Defaults to \"DELETE\".\nWhen a 'terraform destroy' or 'terraform apply' would delete the instance,\nthe command will fail if this field is set to \"PREVENT\" in Terraform state.\nWhen set to \"ABANDON\", the command will remove the resource from Terraform\nmanagement without updating or deleting the resource in the API.\nWhen set to \"DELETE\", deleting the resource is allowed.\n",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "Controls whether Terraform can perform destructive deletion of the resource. A platform policy can enforce a generic destroy-blocking value to prevent unintended destructive changes without assuming a specific environment tier."
    },
    "description": {
      "description": "A brief description of this resource.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Human-readable resource metadata only; it does not directly alter access, encryption, network exposure, or another security control."
    },
    "instance_description": {
      "description": "A description of the instance.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Human-readable descriptive metadata for instantiated VMs only; it does not directly alter access, encryption, network exposure, or another security control."
    },
    "key_revocation_action_type": {
      "description": "Action to be taken when a customer's encryption key is revoked. Supports \"STOP\" and \"NONE\", with \"NONE\" being the default.",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "Controls how the VM reacts when a customer-managed encryption key is revoked. Requiring STOP provides an enforceable response to key revocation instead of allowing the instance to continue operating with NONE."
    },
    "labels": {
      "description": "A set of key/value label pairs to assign to instances created from this template,\n\n\t\t\t\t**Note**: This field is non-authoritative, and will only manage the labels present in your configuration.\n\t\t\t\tPlease refer to the field 'effective_labels' for all of the labels present on the resource.",
      "required": false,
      "type": "map(string)",
      "security_impact": false,
      "rationale": "Labels are cosmetic and organisational metadata. Required keys and values are organisation-specific, so they are not a generic security policy target."
    },
    "machine_type": {
      "description": "The machine type to create. To create a machine with a custom type (such as extended memory), format the value like custom-VCPUS-MEM_IN_MB like custom-6-20480 for 6 vCPU and 20GB of RAM.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "Selects compute capacity and hardware sizing. It is primarily a performance and cost choice rather than a generic security control."
    },
    "metadata": {
      "description": "Metadata key/value pairs to make available from within instances created from this template.",
      "required": false,
      "type": "map(string)",
      "security_impact": true,
      "rationale": "Free-form instance metadata can contain plaintext secrets, credentials, tokens, or other sensitive values. A platform policy can restrict unsafe metadata usage to reduce credential exposure and improve credential hygiene."
    },
    "metadata_startup_script": {
      "description": "An alternative to using the startup-script metadata key, mostly to match the compute_instance resource. This replaces the startup-script metadata key on the created instance and thus the two mechanisms are not allowed to be used simultaneously.",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "Startup scripts execute commands when instances initialise and can embed secrets or run unverified commands. A platform policy can restrict inline startup scripts to reduce script-injection and credential-exposure risks."
    },
    "min_cpu_platform": {
      "description": "Specifies a minimum CPU platform. Applicable values are the friendly names of CPU platforms, such as Intel Haswell or Intel Skylake.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Selects a minimum processor platform for compatibility or performance. It is not itself an access or protection control."
    },
    "name": {
      "description": "The name of the instance template. If you leave this blank, Terraform will auto-generate a unique name.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Identifies the template. Naming does not directly change permissions, exposure, encryption, or other security behaviour."
    },
    "name_prefix": {
      "description": "Creates a unique name beginning with the specified prefix. Conflicts with name.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Controls resource naming only. It does not directly affect security behaviour."
    },
    "project": {
      "description": "The ID of the project in which the resource belongs. If it is not provided, the provider project is used.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Identifies the owning project. Approved project IDs are organisation-specific references and should not be hardcoded by a generic platform policy."
    },
    "region": {
      "description": "The region in which the instance template is located. If it is not provided, the provider region is used.",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "Determines the region/zone where the resource is created. Policy should restrict it to an approved-region whitelist for data residency; the allowed set is parameterised (a hardcoded example whitelist is acceptable)."
    },
    "resource_manager_tags": {
      "description": "A map of resource manager tags.\n\t\t\t\tResource manager tag keys and values have the same definition as resource manager tags. Keys must be in the format tagKeys/{tag_key_id}, and values are in the format tagValues/456. The field is ignored (both PUT & PATCH) when empty.",
      "required": false,
      "type": "map(string)",
      "security_impact": true,
      "rationale": "Resource Manager tags can support attribute-based access control and security baselines. A platform policy can require mandatory security tags to be present without hardcoding organisation-specific tag values."
    },
    "resource_policies": {
      "description": "A list of self_links of resource policies to attach to the instance. Currently a max of 1 resource policy is supported.",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "References other resource-policy objects. Which policy resource is appropriate is organisation- and workload-specific, so specific identifiers should not be pinned generically."
    },
    "tags": {
      "description": "Tags to attach to the instance.",
      "required": false,
      "type": "set(string)",
      "security_impact": true,
      "rationale": "Instance network tags can act as firewall targets. Unconstrained or overly broad tags can expose instances to unintended traffic or bypass security boundaries, so a platform policy can restrict their use."
    },
    "advanced_machine_features": {
      "type": "block",
      "description": "Controls for advanced machine-related behavior features.",
      "required": false
    },
    "advanced_machine_features.enable_nested_virtualization": {
      "description": "Whether to enable nested virtualization or not.",
      "required": false,
      "type": "bool",
      "security_impact": true,
      "rationale": "Nested virtualization runs a hypervisor inside the VM and increases attack surface and potential privilege-escalation paths. A platform policy can require it to remain disabled by default unless explicitly necessary."
    },
    "advanced_machine_features.enable_uefi_networking": {
      "description": "Whether to enable UEFI networking or not.",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "Controls UEFI networking capability for boot/runtime compatibility. It is not a generic access-control or protection setting."
    },
    "advanced_machine_features.performance_monitoring_unit": {
      "description": "The PMU is a hardware component within the CPU core that monitors how the processor runs code. Valid values for the level of PMU are \"STANDARD\", \"ENHANCED\", and \"ARCHITECTURAL\".",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Controls performance-monitoring hardware exposure and capability. The appropriate level is workload-specific rather than a universal security requirement."
    },
    "advanced_machine_features.threads_per_core": {
      "description": "The number of threads per physical core. To disable simultaneous multithreading (SMT) set this to 1. If unset, the maximum number of threads supported per core by the underlying processor is assumed.",
      "required": false,
      "type": "number",
      "security_impact": false,
      "rationale": "Controls CPU threading and performance characteristics. It is an operational tuning choice rather than a generic security control."
    },
    "advanced_machine_features.turbo_mode": {
      "description": "Turbo frequency mode to use for the instance. Currently supported modes is \"ALL_CORE_MAX\".",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Controls CPU turbo behaviour for performance. It does not directly enforce a security boundary."
    },
    "advanced_machine_features.visible_core_count": {
      "description": "The number of physical cores to expose to an instance. Multiply by the number of threads per core to compute the total number of virtual CPUs to expose to the instance. If unset, the number of cores is inferred from the instance\\'s nominal CPU count and the underlying platform\\'s SMT width.",
      "required": false,
      "type": "number",
      "security_impact": false,
      "rationale": "Controls the amount of CPU capacity exposed to the VM. It is a sizing/performance setting rather than a security control."
    },
    "confidential_instance_config": {
      "type": "block",
      "description": "The Confidential VM config being used by the instance. on_host_maintenance has to be set to TERMINATE or this will fail to create.",
      "required": false
    },
    "confidential_instance_config.confidential_instance_type": {
      "description": "\n\t\t\t\t\t\t\t\tThe confidential computing technology the instance uses.\n\t\t\t\t\t\t\t\tSEV is an AMD feature. TDX is an Intel feature. One of the following\n\t\t\t\t\t\t\t\tvalues is required: SEV, SEV_SNP, TDX. If SEV_SNP, min_cpu_platform =\n\t\t\t\t\t\t\t\t\"AMD Milan\" is currently required.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Chooses among supported confidential-computing technologies. The security control is whether confidential compute is enabled; the specific technology is a compatibility and workload choice."
    },
    "confidential_instance_config.enable_confidential_compute": {
      "description": "Defines whether the instance should have confidential compute enabled. Field will be deprecated in a future release.",
      "required": false,
      "type": "bool",
      "security_impact": true,
      "rationale": "Enables Confidential VM protections that encrypt data while it is being processed. This is an explicit security feature that can be centrally required for workloads that need confidential computing."
    },
    "disk": {
      "type": "block",
      "description": "Disks to attach to instances created from this template. This can be specified multiple times for multiple disks.",
      "required": true
    },
    "disk.architecture": {
      "description": "The architecture of the image. Allowed values are ARM64 or X86_64.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Selects disk/image CPU architecture compatibility. It is not a security control."
    },
    "disk.auto_delete": {
      "description": "Whether or not the disk should be auto-deleted. This defaults to true.",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "Controls disk lifecycle when the instance is deleted. Retention and cleanup are operational data-management choices, distinct from destructive-resource protection."
    },
    "disk.boot": {
      "description": "Indicates that this is a boot disk.",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "Marks a disk as the boot disk. This is a functional configuration choice rather than a generic security control."
    },
    "disk.device_name": {
      "description": "A unique device name that is reflected into the /dev/ tree of a Linux operating system running within the instance. If not specified, the server chooses a default device name to apply to this disk.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Defines the guest-visible device identifier. It does not directly alter permissions or data protection."
    },
    "disk.disk_name": {
      "description": "Name of the disk. When not provided, this defaults to the name of the instance.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Names the disk. Resource naming is not itself a security control."
    },
    "disk.disk_size_gb": {
      "description": "The size of the image in gigabytes. If not specified, it will inherit the size of its base image. For SCRATCH disks, the size must be one of 375 or 3000 GB, with a default of 375 GB.",
      "required": false,
      "type": "number",
      "security_impact": false,
      "rationale": "Controls storage capacity. It is a cost and capacity setting rather than a security control."
    },
    "disk.disk_type": {
      "description": "The Google Compute Engine disk type. Such as \"pd-ssd\", \"local-ssd\", \"pd-balanced\" or \"pd-standard\".",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Selects the storage performance class. It is primarily a performance/cost decision."
    },
    "disk.guest_os_features": {
      "description": "A list of features to enable on the guest operating system. Applicable only for bootable images.",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "Guest OS features are architectural and compatibility settings used to support workload-specific operating-system capabilities. They are operational or functional choices rather than generic platform security controls."
    },
    "disk.interface": {
      "description": "Specifies the disk interface to use for attaching this disk.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Selects the disk interface, such as SCSI or NVMe, based on operating-system compatibility and performance requirements. It is an architectural choice rather than a generic security control."
    },
    "disk.labels": {
      "description": "A set of key/value label pairs to assign to disks,",
      "required": false,
      "type": "map(string)",
      "security_impact": false,
      "rationale": "Disk labels are descriptive and organisational metadata. Required label keys and values are organisation-specific and therefore are not suitable as a generic security policy target."
    },
    "disk.mode": {
      "description": "The mode in which to attach this disk, either READ_WRITE or READ_ONLY. If you are attaching or creating a boot disk, this must read-write mode.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Disk attachment mode, such as READ_WRITE or READ_ONLY, is determined by workload and data-sharing requirements. It is an architectural configuration choice rather than a generic platform security control."
    },
    "disk.provisioned_iops": {
      "description": "Indicates how many IOPS to provision for the disk. This sets the number of I/O operations per second that the disk can handle. For more details, see the [Extreme persistent disk documentation](https://cloud.google.com/compute/docs/disks/extreme-persistent-disk) or the [Hyperdisk documentation](https://cloud.google.com/compute/docs/disks/hyperdisks) depending on the selected disk_type.",
      "required": false,
      "type": "number",
      "security_impact": false,
      "rationale": "Provisioned IOPS controls disk performance capacity and associated cost. It is a workload-sizing and operational setting rather than a generic security policy target."
    },
    "disk.provisioned_throughput": {
      "description": "Indicates how much throughput to provision for the disk, in MB/s. This sets the amount of data that can be read or written from the disk per second. Values must greater than or equal to 1. For more details, see the [Hyperdisk documentation](https://cloud.google.com/compute/docs/disks/hyperdisks).",
      "required": false,
      "type": "number",
      "security_impact": false,
      "rationale": "Provisioned throughput controls disk performance and capacity. It is an operational workload-sizing setting rather than a generic security mechanism."
    },
    "disk.resource_manager_tags": {
      "description": "A map of resource manager tags. Resource manager tag keys and values have the same definition as resource manager tags. Keys must be in the format tagKeys/{tag_key_id}, and values are in the format tagValues/456. The field is ignored (both PUT & PATCH) when empty.",
      "required": false,
      "type": "map(string)",
      "security_impact": true,
      "rationale": "Disk Resource Manager tags can support data classification and compliance controls. A platform policy can require mandatory security-tag presence while leaving organisation-specific tag keys and values configurable."
    },
    "disk.resource_policies": {
      "description": "A list (short name or id) of resource policies to attach to this disk. Currently a max of 1 resource policy is supported.",
      "required": false,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "Resource policies attached to disks typically govern workload-specific snapshots, backups, replication, or scheduling behaviour. These are operational data-management choices and the referenced policies are environment-specific rather than generic security controls."
    },
    "disk.source": {
      "description": "The name (not self_link) of the disk (such as those managed by google_compute_disk) to attach. ~> Note: Either source or source_image is required when creating a new instance except for when creating a local SSD.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "References an existing disk. Which disk is trusted is organisation-specific and should not be hardcoded by a generic policy."
    },
    "disk.source_image": {
      "description": "The image from which to initialize this disk. This can be one of: the image's self_link, projects/{project}/global/images/{image}, projects/{project}/global/images/family/{family}, global/images/{image}, global/images/family/{family}, family/{family}, {project}/{family}, {project}/{image}, {family}, or {image}. ~> Note: Either source or source_image is required when creating a new instance except for when creating a local SSD.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "References an image used to create the disk. Approved image identifiers are organisation-specific references rather than a universal value policy."
    },
    "disk.source_snapshot": {
      "description": "The source snapshot to create this disk. When creating\na new instance, one of initializeParams.sourceSnapshot,\ninitializeParams.sourceImage, or disks.source is\nrequired except for local SSD.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "References a snapshot used to create the disk. Approved snapshot identifiers are organisation-specific."
    },
    "disk.storage_pool": {
      "description": "The self_link or ID of the Storage Pool to create this disk in.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "References a storage-pool resource. The valid pool is an environment-specific resource reference."
    },
    "disk.type": {
      "description": "The type of Google Compute Engine disk, can be either \"SCRATCH\" or \"PERSISTENT\".",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Selects SCRATCH or PERSISTENT storage based on workload requirements. It is primarily an operational storage-lifecycle choice."
    },
    "disk.disk_encryption_key": {
      "type": "block",
      "description": "Encrypts or decrypts a disk using a customer-supplied encryption key.",
      "required": false
    },
    "disk.disk_encryption_key.kms_key_self_link": {
      "description": "The self link of the encryption key that is stored in Google Cloud KMS.",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "Specifies the Cloud KMS key used for disk encryption. A platform policy can require this value to be set and non-blank to enforce customer-managed encryption while avoiding pinning any specific key identifier."
    },
    "disk.disk_encryption_key.kms_key_service_account": {
      "description": "The service account being used for the encryption request for the given KMS key. If absent, the Compute Engine default service account is used.",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "Specifies the service account used for KMS encryption operations. A platform policy can require an explicit, non-default run-as service account to support least privilege without pinning a specific account."
    },
    "disk.source_image_encryption_key": {
      "type": "block",
      "description": "The customer-supplied encryption key of the source\nimage. Required if the source image is protected by a\ncustomer-supplied encryption key.\n\nInstance templates do not store customer-supplied\nencryption keys, so you cannot create disks for\ninstances in a managed instance group if the source\nimages are encrypted with your own keys.",
      "required": false
    },
    "disk.source_image_encryption_key.kms_key_self_link": {
      "description": "The self link of the encryption key that is stored in\nGoogle Cloud KMS. Only one of kms_key_self_link, rsa_encrypted_key and raw_key may be set.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "References the key required to decrypt an already-encrypted source image. It does not itself determine whether the resulting disk uses a secure encryption policy and the key identifier is environment-specific."
    },
    "disk.source_image_encryption_key.kms_key_service_account": {
      "description": "The service account being used for the encryption\nrequest for the given KMS key. If absent, the Compute\nEngine default service account is used.",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "Specifies the run-as service account used for source-image KMS operations. A platform policy can require an explicit, non-default service account for least privilege without hardcoding a specific email address."
    },
    "disk.source_image_encryption_key.raw_key": {
      "description": "Specifies a 256-bit customer-supplied encryption key, encoded in RFC 4648 base64 to either encrypt or decrypt this resource.  Only one of kms_key_self_link, rsa_encrypted_key and raw_key may be set.",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "Places a raw customer-supplied encryption key in configuration. Raw key material is sensitive and can be exposed through configuration or state, so a policy can prohibit raw keys and prefer managed or wrapped key mechanisms."
    },
    "disk.source_image_encryption_key.rsa_encrypted_key": {
      "description": "Specifies an RFC 4648 base64 encoded, RSA-wrapped 2048-bit customer-supplied encryption key to either encrypt or decrypt this resource.  Only one of kms_key_self_link, rsa_encrypted_key and raw_key may be set.",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "Uses a statically wrapped customer-supplied encryption key for the source image. A platform policy can discourage customer-supplied wrapping keys in favour of platform-managed CMEK to reduce secret-handling risk."
    },
    "disk.source_snapshot_encryption_key": {
      "type": "block",
      "description": "The customer-supplied encryption key of the source snapshot.",
      "required": false
    },
    "disk.source_snapshot_encryption_key.kms_key_self_link": {
      "description": "The self link of the encryption key that is stored in\nGoogle Cloud KMS. Only one of kms_key_self_link, rsa_encrypted_key and raw_key may be set.",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "Specifies the Cloud KMS key used with the encrypted source snapshot. A platform policy can require a non-blank customer-managed key reference without pinning a specific key name."
    },
    "disk.source_snapshot_encryption_key.kms_key_service_account": {
      "description": "The service account being used for the encryption\nrequest for the given KMS key. If absent, the Compute\nEngine default service account is used.",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "Specifies the run-as service account used for source-snapshot KMS operations. A platform policy can require an explicit, non-default service account for least privilege without pinning a specific identity."
    },
    "disk.source_snapshot_encryption_key.raw_key": {
      "description": "Specifies a 256-bit customer-supplied encryption key, encoded in RFC 4648 base64 to either encrypt or decrypt this resource. Only one of kms_key_self_link, rsa_encrypted_key and raw_key may be set.",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "Places a raw customer-supplied encryption key in configuration. Raw key material is sensitive and can be exposed through configuration or state, so a policy can prohibit raw keys and prefer managed or wrapped key mechanisms."
    },
    "disk.source_snapshot_encryption_key.rsa_encrypted_key": {
      "description": "Specifies an RFC 4648 base64 encoded, RSA-wrapped 2048-bit customer-supplied encryption key to either encrypt or decrypt this resource.  Only one of kms_key_self_link, rsa_encrypted_key and raw_key may be set.",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "Uses a statically wrapped customer-supplied encryption key for the source snapshot. A platform policy can discourage customer-supplied wrapping keys in favour of platform-managed CMEK to reduce secret-handling risk."
    },
    "guest_accelerator": {
      "type": "block",
      "description": "List of the type and count of accelerator cards attached to the instance.",
      "required": false
    },
    "guest_accelerator.count": {
      "description": "The number of the guest accelerator cards exposed to this instance.",
      "required": true,
      "type": "number",
      "security_impact": false,
      "rationale": "Controls the quantity of accelerator hardware attached to the instance. Hardware sizing and quantity are operational, performance, and cost-management choices rather than generic security controls."
    },
    "guest_accelerator.type": {
      "description": "The accelerator type resource to expose to this instance. E.g. nvidia-tesla-k80.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "Selects accelerator hardware. It is a workload/performance choice rather than a generic security control."
    },
    "network_interface": {
      "type": "block",
      "description": "Networks to attach to instances created from this template. This can be specified multiple times for multiple networks.",
      "required": false
    },
    "network_interface.igmp_query": {
      "description": "Indicates whether igmp query is enabled on the network interface or not. If enabled, also indicates the version of IGMP supported.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Controls multicast/IGMP behaviour for specific networking use cases. Whether it is needed is network-design specific rather than universally insecure."
    },
    "network_interface.network": {
      "description": "The name or self_link of the network to attach this interface to. Use network attribute for Legacy or Auto subnetted networks and subnetwork for custom subnetted networks.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "References the VPC network to attach. Approved network names are organisation-specific resource references."
    },
    "network_interface.network_attachment": {
      "description": "The URL of the network attachment that this interface should connect to in the following format: projects/{projectNumber}/regions/{region_name}/networkAttachments/{network_attachment_name}.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "References a network attachment resource. The correct attachment is environment-specific and should not be hardcoded generically."
    },
    "network_interface.network_ip": {
      "description": "The private IP address to assign to the instance. If empty, the address will be automatically assigned.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Specifies a private IP address. Address selection is environment-specific and the value is not inherently insecure."
    },
    "network_interface.nic_type": {
      "description": "The type of vNIC to be used on this interface. Possible values:GVNIC, VIRTIO_NET, MRDMA, IRDMA and IDPF",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Selects the virtual NIC implementation for compatibility and performance. It is not a generic security control."
    },
    "network_interface.queue_count": {
      "description": "The networking queue count that's specified by users for the network interface. Both Rx and Tx queues will be set to this number. It will be empty if not specified.",
      "required": false,
      "type": "number",
      "security_impact": false,
      "rationale": "Controls networking queue capacity and performance. It is an operational tuning setting."
    },
    "network_interface.stack_type": {
      "description": "The stack type for this network interface to identify whether the IPv6 feature is enabled or not. If not specified, IPV4_ONLY will be used.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Selects IPv4-only or dual-stack capability. IPv6 support itself is not inherently insecure; external exposure is controlled separately."
    },
    "network_interface.subnetwork": {
      "description": "The name of the subnetwork to attach this interface to. The subnetwork must exist in the same region this instance will be created in. Either network or subnetwork must be provided.",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "Controls which subnetwork the instance joins. Restricting deployment to approved, compliant subnets is a core network-exposure and isolation control. A platform policy can enforce approved subnet paths or patterns without pinning a specific subnet."
    
    "network_interface.subnetwork_project": {
      "description": "The ID of the project in which the subnetwork belongs. If it is not provided, the provider project is used.",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "Controls the project from which the attached subnetwork is selected. Cross-project network attachment can cross security boundaries, so a platform policy can restrict subnet projects to approved environments without pinning a specific project identifier."
    },
    "network_interface.vlan": {
      "description": "VLAN tag of a dynamic network interface, must be an integer in the range from 2 to 255 inclusively.",
      "required": false,
      "type": "number",
      "security_impact": false,
      "rationale": "Specifies a VLAN identifier for a dynamic network interface. It is a network configuration identifier and topology choice rather than a generic security control."
    },
    "network_interface.access_config": {
      "type": "block",
      "description": "Access configurations, i.e. IPs via which this instance can be accessed via the Internet. Omit to ensure that the instance is not accessible from the Internet (this means that ssh provisioners will not work unless you are running Terraform can send traffic to the instance's network (e.g. via tunnel or because it is running on another cloud instance on that network). This block can be repeated multiple times.",
      "required": false
    },
    "network_interface.access_config.nat_ip": {
      "description": "The IP address that will be 1:1 mapped to the instance's network ip. If not given, one will be generated.",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "Assigns an external IPv4 address to the instance through an access configuration, increasing Internet exposure. A policy can restrict or prohibit explicit public IP assignment."
    },
    "network_interface.access_config.network_tier": {
      "description": "The networking tier used for configuring this instance template. This field can take the following values: PREMIUM, STANDARD, FIXED_STANDARD. If this field is not specified, it is assumed to be PREMIUM.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Selects the network service tier, such as PREMIUM or STANDARD. This primarily controls routing performance and cost characteristics rather than acting as a generic security control."
    },
    "network_interface.alias_ip_range": {
      "type": "block",
      "description": "An array of alias IP ranges for this network interface. Can only be specified for network interfaces on subnet-mode networks.",
      "required": false
    },
    "network_interface.alias_ip_range.ip_cidr_range": {
      "description": "The IP CIDR range represented by this alias IP range. This IP CIDR range must belong to the specified subnetwork and cannot contain IP addresses reserved by system or used by other network interfaces. At the time of writing only a netmask (e.g. /24) may be supplied, with a CIDR format resulting in an API error.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "Specifies an alias IP address range allocated from the selected subnetwork. Address ranges are environment- and team-specific IPAM choices rather than generic platform security policy targets."
    },
    "network_interface.alias_ip_range.subnetwork_range_name": {
      "description": "The subnetwork secondary range name specifying the secondary range from which to allocate the IP CIDR range for this alias IP range. If left unspecified, the primary range of the subnetwork will be used.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "References the named secondary range from which alias IP addresses are allocated. It is an environment-specific resource pointer rather than a generic security control."
    },
    "network_interface.ipv6_access_config": {
      "type": "block",
      "description": "An array of IPv6 access configurations for this interface. Currently, only one IPv6 access config, DIRECT_IPV6, is supported. If there is no ipv6AccessConfig specified, then this instance will have no external IPv6 Internet access.",
      "required": false
    },
    "network_interface.ipv6_access_config.network_tier": {
      "description": "The service-level to be provided for IPv6 traffic when the subnet has an external subnet. Only PREMIUM tier is valid for IPv6",
      "required": true,
      "type": "string",
      "security_impact": true,
      "rationale": "This leaf exists inside an IPv6 access configuration that provides external IPv6 Internet access. A policy can reject this configuration where instances must remain privately addressed."
    },
    "network_performance_config": {
      "type": "block",
      "description": "Configures network performance settings for the instance. If not specified, the instance will be created with its default network performance configuration.",
      "required": false
    },
    "network_performance_config.total_egress_bandwidth_tier": {
      "description": "The egress bandwidth tier to enable. Possible values:TIER_1, DEFAULT",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "Controls network performance capacity. It is an operational/performance setting rather than a security control."
    },
    "reservation_affinity": {
      "type": "block",
      "description": "Specifies the reservations that this instance can consume from.",
      "required": false
    },
    "reservation_affinity.type": {
      "description": "The type of reservation from which this instance can consume resources.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "Controls how the VM consumes reservations. It is a capacity-placement choice rather than a security control."
    },
    "reservation_affinity.specific_reservation": {
      "type": "block",
      "description": "Specifies the label selector for the reservation to use.",
      "required": false
    },
    "reservation_affinity.specific_reservation.key": {
      "description": "Corresponds to the label key of a reservation resource. To target a SPECIFIC_RESERVATION by name, specify compute.googleapis.com/reservation-name as the key and specify the name of your reservation as the only value.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "Identifies a reservation selector key. The correct selector is organisation-specific metadata."
    },
    "reservation_affinity.specific_reservation.values": {
      "description": "Corresponds to the label values of a reservation resource.",
      "required": true,
      "type": "list(string)",
      "security_impact": false,
      "rationale": "Identifies reservation selector values. The correct values are organisation-specific."
    },
    "scheduling": {
      "type": "block",
      "description": "The scheduling strategy to use.",
      "required": false
    },
    "scheduling.automatic_restart": {
      "description": "Specifies whether the instance should be automatically restarted if it is terminated by Compute Engine (not terminated by a user). This defaults to true.",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "Controls availability behaviour after infrastructure termination. It is an operational resilience choice rather than a security control."
    },
    "scheduling.availability_domain": {
      "description": "Specifies the availability domain, which this instance should be scheduled on.",
      "required": false,
      "type": "number",
      "security_impact": false,
      "rationale": "Controls placement for availability. It is an operational scheduling choice."
    },
    "scheduling.instance_termination_action": {
      "description": "Specifies the action GCE should take when SPOT VM is preempted.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Controls what happens when a Spot VM is preempted. It is an operational lifecycle choice."
    },
    "scheduling.on_host_maintenance": {
      "description": "Defines the maintenance behavior for this instance.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Controls maintenance behaviour. The appropriate action depends on workload availability requirements rather than a universal security rule."
    },
    "scheduling.preemptible": {
      "description": "Allows instance to be preempted. This defaults to false.",
      "required": false,
      "type": "bool",
      "security_impact": false,
      "rationale": "Controls whether instances may be preempted by Compute Engine. Preemptibility primarily affects workload availability and operational reliability rather than acting as a generic security control."
    },
    "scheduling.provisioning_model": {
      "description": "Whether the instance is spot. If this is set as SPOT.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Selects the provisioning model, such as standard or Spot. This is primarily an operational, availability, and cost-management choice rather than a generic security control."
    },
    "scheduling.termination_time": {
      "description": "Specifies the timestamp, when the instance will be terminated,\nin RFC3339 text format. If specified, the instance termination action\nwill be performed at the termination time.",
      "required": false,
      "type": "string",
      "security_impact": false,
      "rationale": "Specifies an automatic instance termination time. Instance lifetime and retirement timing are operational lifecycle choices rather than generic platform security controls."
    },
    "scheduling.local_ssd_recovery_timeout": {
      "type": "block",
      "description": "Specifies the maximum amount of time a Local Ssd Vm should wait while\n  recovery of the Local Ssd state is attempted. Its value should be in\n  between 0 and 168 hours with hour granularity and the default value being 1\n  hour.",
      "required": false
    },
    "scheduling.local_ssd_recovery_timeout.nanos": {
      "description": "Span of time that's a fraction of a second at nanosecond\nresolution. Durations less than one second are represented\nwith a 0 seconds field and a positive nanos field. Must\nbe from 0 to 999,999,999 inclusive.",
      "required": false,
      "type": "number",
      "security_impact": false,
      "rationale": "Controls sub-second precision for Local SSD recovery timing. It is an operational availability and recovery-tuning parameter rather than a generic security policy target."
    },
    "scheduling.local_ssd_recovery_timeout.seconds": {
      "description": "Span of time at a resolution of a second.\nMust be from 0 to 315,576,000,000 inclusive.",
      "required": true,
      "type": "number",
      "security_impact": false,
      "rationale": "Controls Local SSD recovery timeout duration. This is an operational availability and recovery-tuning setting rather than a generic security control."
    },
    "scheduling.max_run_duration": {
      "type": "block",
      "description": "The timeout for new network connections to hosts.",
      "required": false
    },
    "scheduling.max_run_duration.nanos": {
      "description": "Span of time that's a fraction of a second at nanosecond\nresolution. Durations less than one second are represented\nwith a 0 seconds field and a positive nanos field. Must\nbe from 0 to 999,999,999 inclusive.",
      "required": false,
      "type": "number",
      "security_impact": false,
      "rationale": "Controls sub-second precision for the maximum instance run duration. This is an operational workload-lifecycle setting rather than a useful generic security policy target."
    },
    "scheduling.max_run_duration.seconds": {
      "description": "Span of time at a resolution of a second.\nMust be from 0 to 315,576,000,000 inclusive.",
      "required": true,
      "type": "number",
      "security_impact": false,
      "rationale": "Controls maximum workload run duration. Instance lifetime limits are operational workload-management choices rather than generic security controls."
    },
    "scheduling.node_affinities": {
      "type": "block",
      "description": "Specifies node affinities or anti-affinities to determine which sole-tenant nodes your instances and managed instance groups will use as host systems.",
      "required": false
    },
    "scheduling.node_affinities.key": {
      "description": "Corresponds to the label key of a reservation resource. To target a SPECIFIC_RESERVATION by name, specify compute.googleapis.com/reservation-name as the key and specify the name of your reservation as the only value.",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "Node-affinity selector keys are placement metadata and references specific to tenant isolation or hardware-allocation needs, making them unsuitable for a generic multi-tenant platform policy."
    },
    "scheduling.node_affinities.operator": {
      "description": "N/A",
      "required": true,
      "type": "string",
      "security_impact": false,
      "rationale": "Defines how node-affinity selector values are matched for instance placement. It is a functional scheduling and placement configuration element rather than a generic security control."
    },
    "scheduling.node_affinities.values": {
      "description": "Corresponds to the label values of a reservation resource.",
      "required": true,
      "type": "set(string)",
      "security_impact": false,
      "rationale": "Specifies node-affinity values that reference particular tenant groups, labels, or hardware pools. These are environment-specific placement references rather than generic platform security-policy targets."
    },
    "service_account": {
      "type": "block",
      "description": "Service account to attach to the instance.",
      "required": false
    },
    "service_account.email": {
      "description": "The service account e-mail address. If not given, the default Google Compute Engine service account is used.",
      "required": false,
      "type": "string",
      "security_impact": true,
      "rationale": "Controls the run-as service account attached to the instance. A platform policy can require an explicit, non-default service account to support least privilege while avoiding pinning a specific email address."
    },
    "service_account.scopes": {
      "description": "A list of service scopes. Both OAuth2 URLs and gcloud short names are supported. To allow full access to all Cloud APIs, use the cloud-platform scope.",
      "required": true,
      "type": "set(string)",
      "security_impact": true,
      "rationale": "OAuth scopes define authorization boundaries alongside IAM. Overly broad scopes such as unrestricted cloud-platform access can grant excessive access, so a platform policy can restrict broad scopes and enforce least-privilege scope configurations."
    },
    "shielded_instance_config": {
      "type": "block",
      "description": "Enable Shielded VM on this instance. Shielded VM provides verifiable integrity to prevent against malware and rootkits. Defaults to disabled. Note: shielded_instance_config can only be used with boot images with shielded vm support.",
      "required": false
    },
    "shielded_instance_config.enable_integrity_monitoring": {
      "description": "Compare the most recent boot measurements to the integrity policy baseline and return a pair of pass/fail results depending on whether they match or not. Defaults to true.",
      "required": false,
      "type": "bool",
      "security_impact": true,
      "rationale": "Enables Shielded VM integrity monitoring so boot measurements can be checked against an integrity baseline. This is a direct VM-hardening control and can be required by policy."
    },
    "shielded_instance_config.enable_secure_boot": {
      "description": "Verify the digital signature of all boot components, and halt the boot process if signature verification fails. Defaults to false.",
      "required": false,
      "type": "bool",
      "security_impact": true,
      "rationale": "Enables Secure Boot, which verifies boot-component signatures and helps prevent untrusted boot software from loading. This is a direct security-hardening control."
    },
    "shielded_instance_config.enable_vtpm": {
      "description": "Use a virtualized trusted platform module, which is a specialized computer chip you can use to encrypt objects like keys and certificates. Defaults to true.",
      "required": false,
      "type": "bool",
      "security_impact": true,
      "rationale": "Enables the virtual Trusted Platform Module used by Shielded VM for measured boot and protection of keys and integrity data. This is a direct security-hardening control."
    }
  }
}
