## 🛡️ Policy Deployment Engine: `cloud_run_service`

This section provides a concise policy evaluation for the `cloud_run_service` resource in GCP.

Reference: [Terraform Registry – cloud_run_service](https://registry.terraform.io/providers/hashicorp/google/latest/docs/resources/cloud_run_service)

---

## Argument Reference  

| Argument | Description | Required | Security Impact | Rationale | Compliant | Non-Compliant |
|----------|-------------|----------|-----------------|-----------|-----------|---------------|
| `name` | Name must be unique within a Google Cloud project and region. Is required when creating resources. Name is primarily intended for creation idempotence and configuration definition. Cannot be updated. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names | true | false | The service name is mainly used for identification and does not directly control access, networking, encryption, or runtime security. | None | None |
| `location` | The location of the cloud run instance. eg us-central1 | true | true | Location affects data residency, compliance, and whether the service is deployed in an approved region. | australia-southeast1 | us-central1 |
| `traffic` | Traffic specifies how to distribute traffic over a collection of Knative Revisions and Configurations Structure is [documented below](#nested_spec_traffic). | false | true | Traffic settings control how requests are routed between revisions and can affect whether users reach approved or unapproved versions. | Refer to child argument | Refer to child argument |
| `template` | template holds the latest specification for the Revision to be stamped out. The template references the container image, and may also include labels and annotations that should be attached to the Revision. To correlate a Revision, and/or to force a Revision to be created when the spec doesn't otherwise change, a nonce label may be provided in the template metadata. For more details, see: https://github.com/knative/serving/blob/main/docs/client-conventions.md#associate-modifications-with-revisions Cloud Run does not currently support referencing a build that is responsible for materializing the container image from source. Structure is [documented below](#nested_spec_template). | false | true | The template defines the revision configuration, including runtime identity, container image, resources, secrets, and annotations. | Refer to child argument | Refer to child argument |
| `metadata` | Metadata associated with this Service, including name, namespace, labels, and annotations. Structure is [documented below](#nested_metadata). | false | true | Metadata can include restricted annotations that affect ingress, Binary Authorization, custom audiences, encryption, networking, and scaling. | Refer to child argument | Refer to child argument |
| `project` | If it is not provided, the provider project is used. | false | true | Using approved projects helps maintain IAM boundaries, environment separation, billing control, and governance. | my-gcp-project | wrong-project-id |
| `autogenerate_revision_name` | autogenerated by Cloud Run. This cannot be set to `true` while `template.metadata.name` is also set. (For legacy support, if `template.metadata.name` is unset in state while this field is set to false, the revision name will still autogenerate.) | false | false | Revision name generation is operational and does not directly affect access, networking, encryption, or secret handling. | None | None |
| `spec` |  | true | true | The spec defines the runtime security posture of the Cloud Run revision. | Refer to child argument | Refer to child argument |
| `containers` |  | true | true | Container configuration affects image trust, secrets handling, exposed ports, compute limits, and mounted volumes. | Refer to child argument | Refer to child argument |
| `env_from` |  | false | true | Environment sources can load configuration or secrets into the container and should be reviewed when used. | Refer to child argument | Refer to child argument |
| `config_map_ref` |  | false | false | Config map references are deprecated and not supported by the Cloud Run API. | None | None |
| `local_object_reference` |  | false | false | Local object reference is mainly an identifier within deprecated config map or secret reference structures. | None | None |
| `secret_ref` |  | false | true | Secret references can expose sensitive values to the container and should use approved secrets only. | Refer to child argument | Refer to child argument |
| `env` |  | false | true | Environment variables may contain sensitive values and should use Secret Manager references where appropriate. | Refer to child argument | Refer to child argument |
| `value_from` |  | false | true | Value sources can retrieve sensitive values and should use approved Secret Manager references. | Refer to child argument | Refer to child argument |
| `secret_key_ref` |  | false | true | Secret references must point to approved secrets and approved versions. | Refer to child argument | Refer to child argument |
| `ports` |  | false | true | Port settings affect how the container receives traffic. | Refer to child argument | Refer to child argument |
| `resources` |  | false | true | CPU and memory settings help control cost, capacity, stability, and abuse risk. | Refer to child argument | Refer to child argument |
| `volume_mounts` |  | false | true | Volume mounts expose files or secrets inside the container and should be limited to approved paths. | Refer to child argument | Refer to child argument |
| `startup_probe` |  | false | false | Startup probe configuration mainly affects availability and startup behaviour, not core security posture. | None | None |
| `tcp_socket` |  | false | false | TCP socket is used for health checking and was not treated as a core Cloud Run security policy. | None | None |
| `http_get` |  | false | false | HTTP probe settings are used for health checking and were not treated as core security policies. | None | None |
| `http_headers` |  | false | false | HTTP headers here are used for probes and are application-specific. | None | None |
| `grpc` |  | false | false | gRPC probe settings are health check related and were not treated as core security policies. | None | None |
| `liveness_probe` |  | false | false | Liveness probe configuration mainly affects availability and restart behaviour, not core security posture. | None | None |
| `volumes` |  | false | true | Volumes can expose secrets or external file systems to the container. | Refer to child argument | Refer to child argument |
| `secret` |  | false | true | Secret volume settings affect which secrets are mounted and how readable the mounted files are. | Refer to child argument | Refer to child argument |
| `items` |  | false | true | Secret item configuration affects the secret version, mounted file path, and file permissions. | Refer to child argument | Refer to child argument |
| `empty_dir` |  | false | false | EmptyDir is ephemeral storage and was not included in the implemented security policy scope. | None | None |
| `csi` |  | false | true | CSI volumes can expose external storage to the service and should be reviewed when used. | None | None |
| `nfs` |  | false | true | NFS volumes can expose external file systems to the service and should be reviewed when used. | None | None |

### traffic Block

| Argument | Description | Required | Security Impact | Rationale | Compliant | Non-Compliant |
|----------|-------------|----------|-----------------|-----------|-----------|---------------|
| `revision_name` | RevisionName of a specific revision to which to send this portion of traffic. | false | false | Revision name is mainly operational unless a separate approved revision naming standard is enforced. | None | None |
| `percent` | Percent specifies percent of the traffic to this Revision or Configuration. | true | true | Traffic percentage must stay within a valid approved range to avoid invalid or unintended traffic routing. | 80 | 150 |
| `tag` | Tag is optionally used to expose a dedicated url for referencing this target exclusively. | false | false | Traffic tags are mainly used for routing and testing and do not directly control core security settings. | None | None |
| `latest_revision` | LatestRevision may be optionally provided to indicate that the latest ready Revision of the Configuration should be used for this traffic target. When provided LatestRevision must be true if RevisionName is empty; it must be false when RevisionName is non-empty. | false | true | Routing traffic to the latest approved revision helps avoid sending users to outdated or unapproved revisions. | true | false |
| `url` | (Output) URL displays the URL for accessing tagged traffic targets. URL is displayed in status, and is disallowed on spec. URL must contain a scheme (e.g. http://) and a hostname, but may not contain anything else (e.g. basic auth, url path, etc.) | false | false | URL is an output-only status value and is not configured directly in the Terraform spec. | None | None |

### template Block

| Argument | Description | Required | Security Impact | Rationale | Compliant | Non-Compliant |
|----------|-------------|----------|-----------------|-----------|-----------|---------------|
| `metadata` | Optional metadata for this Revision, including labels and annotations. Name will be generated by the Configuration. To set minimum instances for this revision, use the "autoscaling.knative.dev/minScale" annotation key. To set maximum instances for this revision, use the "autoscaling.knative.dev/maxScale" annotation key. To set Cloud SQL connections for the revision, use the "run.googleapis.com/cloudsql-instances" annotation key. Structure is [documented below](#nested_spec_template_metadata). | false | true | Template metadata can include revision-level annotations for scaling, Cloud SQL, VPC access, Binary Authorization, encryption, and custom audiences. | {"autoscaling.knative.dev/maxScale":"10", "run.googleapis.com/binary-authorization":"default"} | {"autoscaling.knative.dev/maxScale":"1000", "run.googleapis.com/binary-authorization-breakglass":"emergency-bypass"} |
| `spec` | RevisionSpec holds the desired state of the Revision (from the client). Structure is [documented below](#nested_spec_template_spec). | true | true | The revision spec controls runtime identity, timeout, concurrency, containers, resources, ports, and secret volumes. | Refer to child argument | Refer to child argument |

### metadata Block

| Argument | Description | Required | Security Impact | Rationale | Compliant | Non-Compliant |
|----------|-------------|----------|-----------------|-----------|-----------|---------------|
| `labels` | Map of string keys and values that can be used to organize and categorize (scope and select) objects. May match selectors of replication controllers and routes. **Note**: This field is non-authoritative, and will only manage the labels present in your configuration. Please refer to the field `effective_labels` for all of the labels present on the resource. | false | true | Approved labels support ownership tracking, environment separation, and governance. | {"env":"prod"} | {"env":"unknown"} |
| `generation` | (Output) A sequence number representing a specific generation of the desired state. | false | false | Generation is an output value used for state tracking and is not a user-controlled security setting. | None | None |
| `resource_version` | (Output) An opaque value that represents the internal version of this object that can be used by clients to determine when objects have changed. May be used for optimistic concurrency, change detection, and the watch operation on a resource or set of resources. They may only be valid for a particular resource or set of resources. | false | false | Resource version is an output value used for change detection and is not a user-controlled security setting. | None | None |
| `self_link` | (Output) SelfLink is a URL representing this object. | false | false | Self link is an output value representing the resource path and is not a user-controlled security setting. | None | None |
| `uid` | (Output) UID is a unique id generated by the server on successful creation of a resource and is not allowed to change on PUT operations. | false | false | UID is generated by the platform and does not directly affect the configured security posture. | None | None |
| `namespace` | In Cloud Run the namespace must be equal to either the project ID or project number. | false | true | Namespace should match the approved project ID or project number to keep the service inside the expected project boundary. | my-gcp-project | wrong-project-id |
| `annotations` | Annotations is a key value map stored with a resource that may be set by external tools to store and retrieve arbitrary metadata. More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations **Note**: The Cloud Run API may add additional annotations that were not provided in your config. If terraform plan shows a diff where a server-side annotation is added, you can add it to your config or apply the lifecycle.ignore_changes rule to the metadata.0.annotations field. Annotations with `run.googleapis.com/` and `autoscaling.knative.dev` are restricted. Use the following annotation keys to configure features on a Service: - `run.googleapis.com/binary-authorization-breakglass` sets the [Binary Authorization breakglass](https://cloud.google.com/sdk/gcloud/reference/run/deploy#--breakglass). - `run.googleapis.com/binary-authorization` sets the [Binary Authorization](https://cloud.google.com/sdk/gcloud/reference/run/deploy#--binary-authorization). - `run.googleapis.com/client-name` sets the client name calling the Cloud Run API. - `run.googleapis.com/custom-audiences` sets the [custom audiences](https://cloud.google.com/sdk/gcloud/reference/alpha/run/deploy#--add-custom-audiences) that can be used in the audience field of ID token for authenticated requests. - `run.googleapis.com/description` sets a user defined description for the Service. - `run.googleapis.com/ingress` sets the [ingress settings](https://cloud.google.com/sdk/gcloud/reference/run/deploy#--ingress) for the Service. For example, `"run.googleapis.com/ingress" = "all"`. - `run.googleapis.com/launch-stage` sets the [launch stage](https://cloud.google.com/run/docs/troubleshooting#launch-stage-validation) when a preview feature is used. For example, `"run.googleapis.com/launch-stage": "BETA"` - `run.googleapis.com/minScale` sets the [minimum number of container instances](https://cloud.google.com/sdk/gcloud/reference/run/deploy#--min) of the Service. - `run.googleapis.com/scalingMode` sets the type of scaling mode for the service. The supported values for scaling mode are "manual" and "automatic". If not provided, it defaults to "automatic". - `run.googleapis.com/manualInstanceCount` sets the total instance count for the service in manual scaling mode. This number of instances is divided among all revisions with specified traffic based on the percent of traffic they are receiving. **Note**: This field is non-authoritative, and will only manage the annotations present in your configuration. Please refer to the field `effective_annotations` for all of the annotations present on the resource. | false | true | Annotations can control ingress, Binary Authorization, custom audiences, VPC access, Cloud SQL, encryption, and scaling. | {"run.googleapis.com/ingress":"internal", "run.googleapis.com/binary-authorization":"default", "run.googleapis.com/custom-audiences":"trusted-client", "run.googleapis.com/vpc-access-egress":"private-ranges-only"} | {"run.googleapis.com/ingress":"all", "run.googleapis.com/binary-authorization-breakglass":"emergency-bypass", "run.googleapis.com/custom-audiences":"*", "run.googleapis.com/vpc-access-egress":"all-traffic"} |
| `name` | Name must be unique within a Google Cloud project and region. Is required when creating resources. Name is primarily intended for creation idempotence and configuration definition. Cannot be updated. | false | false | Metadata name is mainly used for identification and does not directly control access, encryption, or network exposure. | None | None |
| `terraform_labels` | (Output) The combination of labels configured directly on the resource and default labels configured on the provider. | false | false | Terraform labels are output-derived label data and are not directly enforced as a security control in this policy set. | None | None |
| `effective_labels` | (Output) All of labels (key/value pairs) present on the resource in GCP, including the labels configured through Terraform, other clients and services. | false | false | Effective labels are output data showing all labels present on the resource and are not directly configured as a security control. | None | None |
| `effective_annotations` | (Output) All of annotations (key/value pairs) present on the resource in GCP, including the annotations configured through Terraform, other clients and services. | false | false | Effective annotations are output data showing all annotations present on the resource and are not directly configured as a security control. | None | None |

### spec Block

| Argument | Description | Required | Security Impact | Rationale | Compliant | Non-Compliant |
|----------|-------------|----------|-----------------|-----------|-----------|---------------|
| `containers` | Containers defines the unit of execution for this Revision. Structure is [documented below](#nested_spec_template_spec_containers). | true | true | Containers define the runtime image, environment variables, ports, resources, and mounted volumes. | Refer to child argument | Refer to child argument |
| `node_selector` | Node Selector describes the hardware requirements of the resources. Use the following node selector keys to configure features on a Revision: - `run.googleapis.com/accelerator` sets the [type of GPU](https://cloud.google.com/run/docs/configuring/services/gpu) required by the Revision to run. | false | false | Node selector is mainly used for hardware selection and was not treated as a core Cloud Run security control in this policy set. | None | None |
| `container_concurrency` | ContainerConcurrency specifies the maximum allowed in-flight (concurrent) requests per container of the Revision. If not specified or 0, defaults to 80 when requested CPU >= 1 and defaults to 1 when requested CPU < 1. | false | true | Excessive concurrency can affect service stability, resource use, and availability. | 80 | 1000 |
| `timeout_seconds` | TimeoutSeconds holds the max duration the instance is allowed for responding to a request. | false | true | Excessive timeout values can tie up resources for too long and increase cost or abuse risk. | 300 | 4000 |
| `service_account_name` | Email address of the IAM service account associated with the revision of the service. The service account represents the identity of the running revision, and determines what permissions the revision has. If not provided, the revision will use the project's default service account. | false | true | The service account controls the permissions available to the running service. A dedicated least-privilege account should be used. | secure-sa@my-gcp-project.iam.gserviceaccount.com | default |
| `volumes` | Volume represents a named volume in a container. Structure is [documented below](#nested_spec_template_spec_volumes). | false | true | Volumes can expose secrets or external storage to the container and should use approved sources and permissions. | Refer to child argument | Refer to child argument |
| `serving_state` | (Output, Deprecated) ServingState holds a value describing the state the resources are in for this Revision. It is expected that the system will manipulate this based on routability and load. ~> **Warning:** `serving_state` is deprecated and will be removed in a future major release. This field is not supported by the Cloud Run API. | false | false | Serving state is output and deprecated. It is not a supported user-controlled security setting. | None | None |

### containers Block

| Argument | Description | Required | Security Impact | Rationale | Compliant | Non-Compliant |
|----------|-------------|----------|-----------------|-----------|-----------|---------------|
| `name` | Name of the container | false | false | Container name is mainly an identifier and does not directly affect security posture. | None | None |
| `working_dir` | , Deprecated) Container's working directory. If not specified, the container runtime's default will be used, which might be configured in the container image. ~> **Warning:** `working_dir` is deprecated and will be removed in a future major release. This field is not supported by the Cloud Run API. | false | false | Working directory is deprecated and not supported by the Cloud Run API. | None | None |
| `args` | Arguments to the entrypoint. The docker image's CMD is used if this is not provided. | false | false | Container arguments are application-specific and were not treated as a general security policy in this scope. | None | None |
| `env_from` | , Deprecated) List of sources to populate environment variables in the container. All invalid keys will be reported as an event when the container is starting. When a key exists in multiple sources, the value associated with the last source will take precedence. Values defined by an Env with a duplicate key will take precedence. Structure is [documented below](#nested_spec_template_spec_containers_containers_env_from). ~> **Warning:** `env_from` is deprecated and will be removed in a future major release. This field is not supported by the Cloud Run API. | false | true | Environment sources can affect how configuration and secrets are loaded into the container. | Refer to child argument | Refer to child argument |
| `image` | Docker image name. This is most often a reference to a container located in the container registry, such as gcr.io/cloudrun/hello | true | true | Only approved image registries should be used to reduce the risk of deploying untrusted or malicious images. | us-docker.pkg.dev/cloudrun/container/hello | docker.io/random/unsafe-image:latest |
| `command` | Entrypoint array. Not executed within a shell. The docker image's ENTRYPOINT is used if this is not provided. | false | false | Entrypoint command is application-specific and was not treated as a general Cloud Run security baseline policy. | None | None |
| `env` | List of environment variables to set in the container. Structure is [documented below](#nested_spec_template_spec_containers_containers_env). | false | true | Environment variables can expose sensitive values if secrets are hardcoded in plaintext. | Refer to child argument | Refer to child argument |
| `ports` | List of open ports in the container. Structure is [documented below](#nested_spec_template_spec_containers_containers_ports). | false | true | Only approved container ports should be exposed to reduce unintended service exposure. | Refer to child argument | Refer to child argument |
| `resources` | Compute Resources required by this container. Used to set values such as max memory Structure is [documented below](#nested_spec_template_spec_containers_containers_resources). | false | true | Resource settings help control cost, capacity, abuse risk, and service stability. | Refer to child argument | Refer to child argument |
| `volume_mounts` | Volume to mount into the container's filesystem. Only supports SecretVolumeSources. Structure is [documented below](#nested_spec_template_spec_containers_containers_volume_mounts). | false | true | Volume mounts can expose secrets or storage paths to the container and should be limited to approved mounts. | name = secret-volume, mount_path = /secrets | /tmp/random |
| `startup_probe` | Startup probe of application within the container. All other probes are disabled if a startup probe is provided, until it succeeds. Container will not be added to service endpoints if the probe fails. Structure is [documented below](#nested_spec_template_spec_containers_containers_startup_probe). | false | false | Startup probes are mainly availability controls and do not directly control access, encryption, secrets, or network exposure. | None | None |
| `liveness_probe` | Periodic probe of container liveness. Container will be restarted if the probe fails. Structure is [documented below](#nested_spec_template_spec_containers_containers_liveness_probe). | false | false | Liveness probes are mainly availability controls and do not directly control access, encryption, secrets, or network exposure. | None | None |

### env_from Block

| Argument | Description | Required | Security Impact | Rationale | Compliant | Non-Compliant |
|----------|-------------|----------|-----------------|-----------|-----------|---------------|
| `prefix` | An optional identifier to prepend to each key in the ConfigMap. | false | false | Prefix is mainly a naming convenience and does not directly control access or secret exposure. | None | None |
| `config_map_ref` | The ConfigMap to select from. Structure is [documented below](#nested_spec_template_spec_containers_containers_env_from_env_from_config_map_ref). | false | false | Config map references are deprecated and not supported by the Cloud Run API. | None | None |
| `secret_ref` | The Secret to select from. Structure is [documented below](#nested_spec_template_spec_containers_containers_env_from_env_from_secret_ref). | false | true | Secret references can expose secrets to the container and should point only to approved secrets. | Refer to child argument | Refer to child argument |

### config_map_ref Block

| Argument | Description | Required | Security Impact | Rationale | Compliant | Non-Compliant |
|----------|-------------|----------|-----------------|-----------|-----------|---------------|
| `optional` | Specify whether the ConfigMap must be defined | false | false | This setting is related to deprecated config map behaviour and was not included as a security policy. | None | None |
| `local_object_reference` | The ConfigMap to select from. Structure is [documented below](#nested_spec_template_spec_containers_containers_env_from_env_from_config_map_ref_local_object_reference). | false | false | This setting belongs to deprecated config map behaviour and was not included as a security policy. | None | None |

### local_object_reference Block

| Argument | Description | Required | Security Impact | Rationale | Compliant | Non-Compliant |
|----------|-------------|----------|-----------------|-----------|-----------|---------------|
| `name` | Name of the referent. | true | false | Name is only an identifier in this context and was not treated as a direct security control. | None | None |

### secret_ref Block

| Argument | Description | Required | Security Impact | Rationale | Compliant | Non-Compliant |
|----------|-------------|----------|-----------------|-----------|-----------|---------------|
| `local_object_reference` | The Secret to select from. Structure is [documented below](#nested_spec_template_spec_containers_containers_env_from_env_from_secret_ref_local_object_reference). | false | true | The referenced secret object should be approved before being exposed to the container. | approved-secret | unknown-secret |
| `optional` | Specify whether the Secret must be defined | false | false | Optional controls whether the secret must exist. It is operational and was not treated as a core security control. | None | None |

### env Block

| Argument | Description | Required | Security Impact | Rationale | Compliant | Non-Compliant |
|----------|-------------|----------|-----------------|-----------|-----------|---------------|
| `name` | Name of the environment variable. | false | false | Environment variable names are usually identifiers and are not directly sensitive unless they disclose application design. | None | None |
| `value` | Defaults to "". | false | true | Plaintext environment variable values can expose passwords, tokens, keys, and other secrets. | production | password |
| `value_from` | Source for the environment variable's value. Only supports secret_key_ref. Structure is [documented below](#nested_spec_template_spec_containers_containers_env_env_value_from). | false | true | Using Secret Manager references is safer than hardcoding plaintext values when approved secrets and versions are used. | Refer to child argument | Refer to child argument |

### value_from Block

| Argument | Description | Required | Security Impact | Rationale | Compliant | Non-Compliant |
|----------|-------------|----------|-----------------|-----------|-----------|---------------|
| `secret_key_ref` | Selects a key (version) of a secret in Secret Manager. Structure is [documented below](#nested_spec_template_spec_containers_containers_env_env_value_from_secret_key_ref). | true | true | Secret key references must point to approved secrets and approved secret versions. | Refer to child argument | Refer to child argument |

### secret_key_ref Block

| Argument | Description | Required | Security Impact | Rationale | Compliant | Non-Compliant |
|----------|-------------|----------|-----------------|-----------|-----------|---------------|
| `key` | A Cloud Secret Manager secret version. Must be 'latest' for the latest version or an integer for a specific version. | true | true | Only approved secret versions should be used to avoid referencing old or unmanaged versions. | latest | old-version |
| `name` | The name of the secret in Cloud Secret Manager. By default, the secret is assumed to be in the same project. If the secret is in another project, you must define an alias. An alias definition has the form: {alias}:projects/{project-id|project-number}/secrets/{secret-name}. If multiple alias definitions are needed, they must be separated by commas. The alias definitions must be set on the run.googleapis.com/secrets annotation. | true | true | Only approved Secret Manager secret names should be referenced by Cloud Run services. | prod-db-secret | random-secret |

### ports Block

| Argument | Description | Required | Security Impact | Rationale | Compliant | Non-Compliant |
|----------|-------------|----------|-----------------|-----------|-----------|---------------|
| `name` | If specified, used to specify which protocol to use. Allowed values are "http1" (HTTP/1) and "h2c" (HTTP/2 end-to-end). Defaults to "http1". | false | false | The protocol name is mainly operational and was not treated as a core security policy. | None | None |
| `protocol` | Protocol for port. Must be "TCP". Defaults to "TCP". | false | false | Cloud Run requires TCP, so this was not treated as a custom security policy in this scope. | TCP | None |
| `container_port` | Port number the container listens on. This must be a valid port number (between 1 and 65535). Defaults to "8080". | false | true | Only approved ports should be exposed to reduce unintended service exposure. | 8080 | 22 |

### resources Block

| Argument | Description | Required | Security Impact | Rationale | Compliant | Non-Compliant |
|----------|-------------|----------|-----------------|-----------|-----------|---------------|
| `limits` | Limits describes the maximum amount of compute resources allowed. The values of the map is string form of the 'quantity' k8s type: https://github.com/kubernetes/kubernetes/blob/master/staging/src/k8s.io/apimachinery/pkg/api/resource/quantity.go | false | true | Excessive limits can increase cost and resource abuse risk. | {"cpu":"1"} | {"cpu":"8"} |
| `requests` | Requests describes the minimum amount of compute resources required. If Requests is omitted for a container, it defaults to Limits if that is explicitly specified, otherwise to an implementation-defined value. The values of the map is string form of the 'quantity' k8s type: https://github.com/kubernetes/kubernetes/blob/master/staging/src/k8s.io/apimachinery/pkg/api/resource/quantity.go | false | true | Excessive requests can reduce scheduling efficiency and increase reserved resource usage. | {"cpu":"1"} | {"cpu":"8"} |

### volume_mounts Block

| Argument | Description | Required | Security Impact | Rationale | Compliant | Non-Compliant |
|----------|-------------|----------|-----------------|-----------|-----------|---------------|
| `mount_path` | Path within the container at which the volume should be mounted.  Must not contain ':'. | true | true | Mount paths should be controlled so secrets are exposed only at expected locations. | /secrets | /tmp/random |
| `name` | This must match the Name of a Volume. | true | false | The volume mount name is mainly used to match a declared volume and is not directly security-sensitive. | None | None |

### startup_probe Block

| Argument | Description | Required | Security Impact | Rationale | Compliant | Non-Compliant |
|----------|-------------|----------|-----------------|-----------|-----------|---------------|
| `initial_delay_seconds` | Number of seconds after the container has started before the probe is initiated. Defaults to 0 seconds. Minimum value is 0. Maximum value is 240. | false | false | Initial Delay Seconds is an availability or health-check setting and was not treated as a core security policy. | None | None |
| `timeout_seconds` | Number of seconds after which the probe times out. Defaults to 1 second. Minimum value is 1. Maximum value is 3600. Must be smaller than periodSeconds. | false | false | Timeout Seconds is an availability or health-check setting and was not treated as a core security policy. | None | None |
| `period_seconds` | How often (in seconds) to perform the probe. Default to 10 seconds. Minimum value is 1. Maximum value is 240. | false | false | Period Seconds is an availability or health-check setting and was not treated as a core security policy. | None | None |
| `failure_threshold` | Minimum consecutive failures for the probe to be considered failed after having succeeded. Defaults to 3. Minimum value is 1. | false | false | Failure Threshold is an availability or health-check setting and was not treated as a core security policy. | None | None |
| `tcp_socket` | TcpSocket specifies an action involving a TCP port. Structure is [documented below](#nested_spec_template_spec_containers_containers_startup_probe_tcp_socket). | false | false | Tcp Socket is an availability or health-check setting and was not treated as a core security policy. | None | None |
| `http_get` | HttpGet specifies the http request to perform. Structure is [documented below](#nested_spec_template_spec_containers_containers_startup_probe_http_get). | false | false | Http Get is an availability or health-check setting and was not treated as a core security policy. | None | None |
| `grpc` | GRPC specifies an action involving a GRPC port. Structure is [documented below](#nested_spec_template_spec_containers_containers_startup_probe_grpc). | false | false | Grpc is an availability or health-check setting and was not treated as a core security policy. | None | None |

### tcp_socket Block

| Argument | Description | Required | Security Impact | Rationale | Compliant | Non-Compliant |
|----------|-------------|----------|-----------------|-----------|-----------|---------------|
| `port` | Port number to access on the container. Number must be in the range 1 to 65535. If not specified, defaults to the same value as container.ports[0].containerPort. | false | false | Probe port is part of health checking. Container port exposure is handled separately by the container_port policy. | None | None |

### http_get Block

| Argument | Description | Required | Security Impact | Rationale | Compliant | Non-Compliant |
|----------|-------------|----------|-----------------|-----------|-----------|---------------|
| `path` | Path to access on the HTTP server. If set, it should not be empty string. | false | false | Health check path is operational and application-specific. | None | None |
| `port` | Port number to access on the container. Number must be in the range 1 to 65535. If not specified, defaults to the same value as container.ports[0].containerPort. | false | false | Health check port is operational. Container port exposure is handled separately. | None | None |
| `http_headers` | Custom headers to set in the request. HTTP allows repeated headers. Structure is [documented below](#nested_spec_template_spec_containers_containers_liveness_probe_http_get_http_headers). | false | false | Health check headers are application-specific and were not treated as a general Cloud Run security policy. | None | None |

### http_headers Block

| Argument | Description | Required | Security Impact | Rationale | Compliant | Non-Compliant |
|----------|-------------|----------|-----------------|-----------|-----------|---------------|
| `name` | The header field name. | true | false | Probe header name is application-specific and not a core Cloud Run security control. | None | None |
| `value` | The header field value. | false | true | Header values can contain sensitive data, but probe headers were not included in the implemented policy scope. | None | password |

### grpc Block

| Argument | Description | Required | Security Impact | Rationale | Compliant | Non-Compliant |
|----------|-------------|----------|-----------------|-----------|-----------|---------------|
| `port` | Port number to access on the container. Number must be in the range 1 to 65535. If not specified, defaults to the same value as container.ports[0].containerPort. | false | false | Probe port is operational. Container port exposure is handled separately. | None | None |
| `service` | The name of the service to place in the gRPC HealthCheckRequest (see https://github.com/grpc/grpc/blob/master/doc/health-checking.md). If this is not specified, the default behavior is defined by gRPC. | false | false | gRPC probe service name is operational and application-specific. | None | None |

### liveness_probe Block

| Argument | Description | Required | Security Impact | Rationale | Compliant | Non-Compliant |
|----------|-------------|----------|-----------------|-----------|-----------|---------------|
| `initial_delay_seconds` | Number of seconds after the container has started before the probe is initiated. Defaults to 0 seconds. Minimum value is 0. Maximum value is 3600. | false | false | Initial Delay Seconds is an availability or health-check setting and was not treated as a core security policy. | None | None |
| `timeout_seconds` | Number of seconds after which the probe times out. Defaults to 1 second. Minimum value is 1. Maximum value is 3600. Must be smaller than period_seconds. | false | false | Timeout Seconds is an availability or health-check setting and was not treated as a core security policy. | None | None |
| `period_seconds` | How often (in seconds) to perform the probe. Default to 10 seconds. Minimum value is 1. Maximum value is 3600. | false | false | Period Seconds is an availability or health-check setting and was not treated as a core security policy. | None | None |
| `failure_threshold` | Minimum consecutive failures for the probe to be considered failed after having succeeded. Defaults to 3. Minimum value is 1. | false | false | Failure Threshold is an availability or health-check setting and was not treated as a core security policy. | None | None |
| `http_get` | HttpGet specifies the http request to perform. Structure is [documented below](#nested_spec_template_spec_containers_containers_liveness_probe_http_get). | false | false | Http Get is an availability or health-check setting and was not treated as a core security policy. | None | None |
| `grpc` | GRPC specifies an action involving a GRPC port. Structure is [documented below](#nested_spec_template_spec_containers_containers_liveness_probe_grpc). | false | false | Grpc is an availability or health-check setting and was not treated as a core security policy. | None | None |

### volumes Block

| Argument | Description | Required | Security Impact | Rationale | Compliant | Non-Compliant |
|----------|-------------|----------|-----------------|-----------|-----------|---------------|
| `name` | Volume's name. | true | false | Volume name is mainly used to match the volume with the related mount. | None | None |
| `secret` | The secret's value will be presented as the content of a file whose name is defined in the item path. If no items are defined, the name of the file is the secret_name. Structure is [documented below](#nested_spec_template_spec_volumes_volumes_secret). | false | true | Secret volumes must use approved secrets, restrictive file permissions, and safe item paths. | Refer to child argument | Refer to child argument |
| `empty_dir` | Ephemeral storage which can be backed by real disks (HD, SSD), network storage or memory (i.e. tmpfs). For now only in memory (tmpfs) is supported. It is ephemeral in the sense that when the sandbox is taken down, the data is destroyed with it (it does not persist across sandbox runs). Structure is [documented below](#nested_spec_template_spec_volumes_volumes_empty_dir). | false | false | EmptyDir is ephemeral storage and was not included in the implemented high-impact Cloud Run security policy set. | None | None |
| `csi` | A filesystem specified by the Container Storage Interface (CSI). Structure is [documented below](#nested_spec_template_spec_volumes_volumes_csi). | false | true | CSI volumes can expose external storage to the service. They were considered optional advanced controls because they depend on workload storage design. | None | None |
| `nfs` | A filesystem backed by a Network File System share. This filesystem requires the run.googleapis.com/execution-environment annotation to be unset or set to "gen2" Structure is [documented below](#nested_spec_template_spec_volumes_volumes_nfs). | false | true | NFS volumes can expose external file systems to the service. They were considered optional advanced controls because they depend on workload storage design. | None | None |

### secret Block

| Argument | Description | Required | Security Impact | Rationale | Compliant | Non-Compliant |
|----------|-------------|----------|-----------------|-----------|-----------|---------------|
| `secret_name` | The name of the secret in Cloud Secret Manager. By default, the secret is assumed to be in the same project. If the secret is in another project, you must define an alias. An alias definition has the form: {alias}:projects/{project-id|project-number}/secrets/{secret-name}. If multiple alias definitions are needed, they must be separated by commas. The alias definitions must be set on the run.googleapis.com/secrets annotation. | true | true | Only approved secrets should be mounted into Cloud Run services. | prod-db-secret | random-secret |
| `default_mode` | Mode bits to use on created files by default. Must be a value between 0000 and 0777. Defaults to 0644. Directories within the path are not affected by this setting. This might be in conflict with other options that affect the file mode, like fsGroup, and the result can be other mode bits set. | false | true | Mounted secrets should use restrictive file permissions to reduce accidental exposure. | 256 | 511 |
| `items` | If unspecified, the volume will expose a file whose name is the secret_name. If specified, the key will be used as the version to fetch from Cloud Secret Manager and the path will be the name of the file exposed in the volume. When items are defined, they must specify a key and a path. Structure is [documented below](#nested_spec_template_spec_volumes_volumes_secret_items). | false | true | Secret items should use approved versions, safe file paths, and restrictive permissions. | Refer to child argument | Refer to child argument |

### items Block

| Argument | Description | Required | Security Impact | Rationale | Compliant | Non-Compliant |
|----------|-------------|----------|-----------------|-----------|-----------|---------------|
| `key` | The Cloud Secret Manager secret version. Can be 'latest' for the latest value or an integer for a specific version. | true | true | Only approved secret versions should be mounted. | latest | old-version |
| `path` | The relative path of the file to map the key to. May not be an absolute path. May not contain the path element '..'. May not start with the string '..'. | true | true | Secret item paths should be approved and should not use unsafe or unexpected file names. | db-password | random-path |
| `mode` | Mode bits to use on this file, must be a value between 0000 and 0777. If not specified, the volume defaultMode will be used. This might be in conflict with other options that affect the file mode, like fsGroup, and the result can be other mode bits set. | false | true | Secret item files should use restrictive permissions to prevent unnecessary access. | 256 | 511 |

### empty_dir Block

| Argument | Description | Required | Security Impact | Rationale | Compliant | Non-Compliant |
|----------|-------------|----------|-----------------|-----------|-----------|---------------|
| `medium` | The medium on which the data is stored. The default is "" which means to use the node's default medium. Must be an empty string (default) or Memory. | false | false | Storage medium is operational and workload-specific. | None | None |
| `size_limit` | Limit on the storage usable by this EmptyDir volume. The size limit is also applicable for memory medium. The maximum usage on memory medium EmptyDir would be the minimum value between the SizeLimit specified here and the sum of memory limits of all containers in a pod. This field's values are of the 'Quantity' k8s type: https://kubernetes.io/docs/reference/kubernetes-api/common-definitions/quantity/. The default is nil which means that the limit is undefined. More info: https://kubernetes.io/docs/concepts/storage/volumes/#emptydir. | false | true | Size limits can help control resource use, but this was not included in the implemented high-impact policy set. | None | None |

### csi Block

| Argument | Description | Required | Security Impact | Rationale | Compliant | Non-Compliant |
|----------|-------------|----------|-----------------|-----------|-----------|---------------|
| `driver` | Unique name representing the type of file system to be created. Cloud Run supports the following values: * gcsfuse.run.googleapis.com: Mount a Google Cloud Storage bucket using GCSFuse. This driver requires the run.googleapis.com/execution-environment annotation to be unset or set to "gen2" | true | true | Only approved CSI drivers should be used when CSI volumes are enabled. | gcsfuse.run.googleapis.com | unknown-driver |
| `read_only` | If true, all mounts created from this volume will be read-only. | false | true | Read-only mounts reduce write risk to mounted storage. | true | false |
| `volume_attributes` | Driver-specific attributes. The following options are supported for available drivers: * gcsfuse.run.googleapis.com * bucketName: The name of the Cloud Storage Bucket that backs this volume. The Cloud Run Service identity must have access to this bucket. | false | true | Volume attributes can identify external storage such as bucket names and should reference approved storage only. | {"bucketName":"approved-bucket"} | {"bucketName":"unknown-bucket"} |

### nfs Block

| Argument | Description | Required | Security Impact | Rationale | Compliant | Non-Compliant |
|----------|-------------|----------|-----------------|-----------|-----------|---------------|
| `server` | IP address or hostname of the NFS server | true | true | Only approved internal NFS servers should be mounted. | 10.0.0.5 | 203.0.113.10 |
| `path` | Path exported by the NFS server | true | true | Only approved exported NFS paths should be mounted. | /exports/app | / |
| `read_only` | If true, mount the NFS volume as read only in all mounts. Defaults to false. | false | true | Read-only NFS mounts reduce the risk of unwanted writes to shared storage. | true | false |
