## 🛡️ Policy Deployment Engine: `dataflow_flex_template_job`

This section provides a concise policy evaluation for the `dataflow_flex_template_job` resource in GCP.

Reference: [Terraform Registry – dataflow_flex_template_job](https://registry.terraform.io/providers/hashicorp/google/latest/docs/resources/dataflow_flex_template_job)

---

## Argument Reference  

| Argument | Description | Required | Security Impact | Rationale | Compliant | Non-Compliant |
|----------|-------------|----------|-----------------|-----------|-----------|---------------|
| `name` | A unique name for the resource, required by Dataflow. | true | false | A unique name for the resource, required by Dataflow. | None | None |
| `container_spec_gcs_path` | The GCS path to the Dataflow Flex Template spec file. | true | false | The GCS path to the Dataflow Flex Template spec file. | None | None |
| `additional_experiments` | List of experiments that should be used by the job. | false | false | List of experiments that should be used by the job. | None | None |
| `additional_pipeline_options` | Additional pipeline options to pass to the Flex Template job. | false | false | Additional pipeline options to pass to the Flex Template job. | None | None |
| `autoscaling_algorithm` | The algorithm to allow Dataflow to scale between minWorkers and maxWorkers. | false | false | The algorithm to allow Dataflow to scale between minWorkers and maxWorkers. | None | None |
| `parameters` | Key/value pairs to be passed to the Dataflow job (as used in the template). Keys are case-sensitive based on the language on which the pipeline is coded, mostly Java. Note: do not configure Dataflow options here in parameters. | false | false | Key/value pairs to be passed to the Dataflow job (as used in the template). Keys are case-sensitive based on the language on which the pipeline is coded, mostly Java. Note: do not configure Dataflow options here in parameters. | None | None |
| `enable_streaming_engine` | Indicates if the job should use the streaming engine feature. When enabled, the Dataflow service runs the pipeline steps on the Dataflow service backend, reducing the number of persistent disks and worker CPUs needed. | false | true | Streaming Engine offloads pipeline execution to the managed Dataflow service backend, reducing the number of worker VMs and persistent disks. This minimizes the attack surface and improves resource efficiency. | true | false |
| `ip_configuration` | The configuration for VM IPs. Options are 'WORKER_IP_PUBLIC' or 'WORKER_IP_PRIVATE'. | false | true | Assigning public IPs to Dataflow workers increases the attack surface by exposing VMs to the internet. Using private IPs ensures workers communicate only within the VPC, reducing the risk of unauthorized access. | WORKER_IP_PRIVATE | WORKER_IP_PUBLIC |
| `kms_key_name` | The name for the Cloud KMS key for the job. Key format is: projects/PROJECT_ID/locations/LOCATION/keyRings/KEY_RING/cryptoKeys/KEY. | false | true | Customer-managed encryption keys (CMEK) ensure that data processed by Dataflow is encrypted with keys controlled by the organization, meeting compliance requirements and providing an additional layer of data protection. | projects/my-project/locations/us-central1/keyRings/my-keyring/cryptoKeys/my-key | (not set â€” uses Google-managed keys) |
| `labels` | User labels to be specified for the job. Keys and values should follow the restrictions specified in the labeling restrictions page. Note: This field is non-authoritative, and will only manage the labels present in your configuration. Please refer to the field effective_labels for all of the labels present on the resource. | false | false | User labels to be specified for the job. Keys and values should follow the restrictions specified in the labeling restrictions page. Note: This field is non-authoritative, and will only manage the labels present in your configuration. Please refer to the field effective_labels for all of the labels present on the resource. | None | None |
| `terraform_labels` | The combination of labels configured directly on the resource and default labels configured on the provider. | false | false | The combination of labels configured directly on the resource and default labels configured on the provider. | None | None |
| `effective_labels` | All of labels (key/value pairs) present on the resource in GCP, including the labels configured through Terraform, other clients and services. | false | false | All of labels (key/value pairs) present on the resource in GCP, including the labels configured through Terraform, other clients and services. | None | None |
| `launcher_machine_type` | The machine type to use for launching the job. The default is n1-standard-1. | false | false | The machine type to use for launching the job. The default is n1-standard-1. | None | None |
| `machine_type` | The machine type to use for the job. | false | false | The machine type to use for the job. | None | None |
| `max_workers` | The maximum number of Google Compute Engine instances to be made available to your pipeline during execution, from 1 to 1000. | false | false | The maximum number of Google Compute Engine instances to be made available to your pipeline during execution, from 1 to 1000. | None | None |
| `network` | The network to which VMs will be assigned. If it is not provided, 'default' will be used. | false | true | The default VPC network lacks proper firewall rules and network segmentation. Using a dedicated VPC ensures that Dataflow workers operate within a controlled network boundary with appropriate security controls. | projects/my-project/global/networks/approved-vpc | default |
| `num_workers` | The initial number of Google Compute Engine instances for the job. | false | false | The initial number of Google Compute Engine instances for the job. | None | None |
| `on_delete` | One of 'drain' or 'cancel'. Specifies behavior of deletion during terraform destroy. | false | false | One of 'drain' or 'cancel'. Specifies behavior of deletion during terraform destroy. | None | None |
| `project` | The project in which the resource belongs. If it is not provided, the provider project is used. | false | false | The project in which the resource belongs. If it is not provided, the provider project is used. | None | None |
| `region` | The region in which the created job should run. | false | false | The region in which the created job should run. | None | None |
| `sdk_container_image` | Docker registry location of container image to use for the 'worker harness'. Default is the container for the version of the SDK. | false | false | Docker registry location of container image to use for the 'worker harness'. Default is the container for the version of the SDK. | None | None |
| `service_account_email` | The Service Account email used to create the job. This should be just an email e.g. myserviceaccount@myproject.iam.gserviceaccount.com. | false | true | Specifying a dedicated service account enforces the principle of least privilege. Without it, the default Compute Engine service account is used, which typically has overly broad permissions. | dataflow-sa@my-project.iam.gserviceaccount.com | (not set â€” uses default Compute Engine SA) |
| `skip_wait_on_job_termination` | If true, treat DRAINING and CANCELLING as terminal states and do not wait for further changes before removing from terraform state and moving on. | false | false | If true, treat DRAINING and CANCELLING as terminal states and do not wait for further changes before removing from terraform state and moving on. | None | None |
| `staging_location` | The GCS path for staging files. A random subdirectory will be created under the bucket for job staging. | false | false | The GCS path for staging files. A random subdirectory will be created under the bucket for job staging. | None | None |
| `subnetwork` | The subnetwork to which VMs will be assigned. Should be of the form 'regions/REGION/subnetworks/SUBNETWORK'. | false | false | The subnetwork to which VMs will be assigned. Should be of the form 'regions/REGION/subnetworks/SUBNETWORK'. | None | None |
| `temp_location` | The GCS path for temporary files. A random subdirectory will be created under the bucket for temp data. | false | false | The GCS path for temporary files. A random subdirectory will be created under the bucket for temp data. | None | None |
| `transform_name_mapping` | Only applicable when updating a pipeline. Map of transform name prefixes of the job to be replaced with the corresponding name prefixes of the new job. | false | false | Only applicable when updating a pipeline. Map of transform name prefixes of the job to be replaced with the corresponding name prefixes of the new job. | None | None |
