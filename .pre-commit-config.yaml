repos:
  - repo: local
    hooks:
      - id: policy-linter
        name: Policy Linter (Multi-CSP)
        entry: python scripts/linters/run_precommit_linter.py
        language: system
        pass_filenames: false
        always_run: true
        files: ^(policies|inputs)/