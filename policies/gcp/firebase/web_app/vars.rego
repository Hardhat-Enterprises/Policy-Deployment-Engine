package terraform.gcp.security.firebase.web_app.vars

variables := {
    "friendly_resource_name": "Firebase Web App",   # Human-readable resource name
    "resource_type": "google_firebase_web_app",     # Terraform resource type
    "resource_value_name": "display_name"           # Attribute used to identify the resource
}
