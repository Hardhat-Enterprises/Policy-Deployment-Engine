{
  "server_ca_mode": {
    "keys": [
      "server_ca_pool"
    ],
    "reason": "Google Cloud requires server_ca_pool when server_ca_mode is SERVER_CA_MODE_CUSTOMER_MANAGED_CAS_CA, and requires server_ca_pool to be omitted for non-customer-managed CA modes. The fixture difference is therefore required by the provider/API semantics."
  }
}
