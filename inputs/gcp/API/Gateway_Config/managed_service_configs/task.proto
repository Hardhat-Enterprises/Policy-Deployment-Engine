syntax = "proto3";

package demo;

service DemoService {
  rpc GetSomething (DemoRequest) returns (DemoResponse);
}

message DemoRequest {
  string id = 1;
}

message DemoResponse {
  string result = 1;
}
