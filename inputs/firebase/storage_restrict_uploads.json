{
  "version": 1,
  "resource": "firebase.storage.objects",
  "bucket": "ankita-project-files",
  "policy": {
    "effect": "allow",
    "action": ["upload"],
    "condition": "request.auth != null && request.resource.contentType.matches('image/.*')"
  }
}
